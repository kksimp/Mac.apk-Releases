# Mac.apk flags reference

Every environment variable and command-line flag the shipped Mac.apk reads, with its default,
its accepted values and what it does. The handful a user is likely to want are summarised in
the README's "Advanced: flags and settings" section and repeated in the first table below.

The audience column classifies each row as `user`, `dev`, `diag` (diagnostic output), `bisect`
(a gate that restores an older behaviour for an A/B) or `experiment`. Flags marked `bisect` or
`experiment` are not supported configurations; they exist so a regression can be attributed,
and a future release may remove them without notice. Switches that only matter when building
Mac.apk from source, the maintainers' test tooling, and the diagnostics that exist only for the
maintainers' investigation of one specific app are left out of this copy.

---

# Every environment variable and command-line flag the shipped Mac.apk reads, grouped by subsystem, with default, accepted values, effect and audience for each.

---

## How to set a flag

- **Running the runtime directly:** environment variables go before the command: `MACAPK_LOG_LEVEL=7 /Applications/Mac.apk.app/Contents/Resources/macapk-runtime --apk path/to/app.apk`. CLI flags (`--orientation`, `--android-root`, ...) go on the command line as shown in the CLI table below.
- **An installed app:** `MACAPK_X=1 /Applications/<App>.app/Contents/MacOS/<AppStub>` from a Terminal -- the per-app stub `execv`s the staged shared runtime and inherits whatever environment it was started with, exactly like any other macOS executable. `open` does NOT forward a shell's environment to the launched app (macOS launch services strips it); use `open --env MACAPK_X=1 -a /Applications/<App>.app` (per-key) or launch the binary inside `Contents/MacOS/` directly if you need this from a script.
- **The launcher GUI does three separate things**, in increasing order of specificity (later wins):
  1. Its own Settings panel translates screen-size / orientation / verbose-logging pickers into the matching `--width/--height`, `--orientation`, and `--verbose` CLI flags, and a few Dev-menu toggles (revealed by 5-tapping the launcher's title) into specific env vars -- see "The Mac.apk window's settings" below for the exact mapping.
  2. **Settings > Default environment** is a freeform key=value editor applied to *every* launch -- this is the general escape hatch for typing any `MACAPK_*` var (or anything else) without touching a Terminal at all.
  3. An individual app's **Get Info sheet** holds a *per-app* freeform key=value editor that is applied on top of (and overrides) the global Default environment for that one app only.

---

## The 12 flags a user actually needs

*(Everything else in this document exists for diagnosis; these are the ones worth knowing about for normal use.)*

| Flag | What it does |
| --- | --- |
| `--android-root <dir>` (CLI) / `MACAPK_ANDROID_ROOT` | Where the runtime creates the Android filesystem mirror (`data/data/<pkg>/...`) for the app being launched -- saves live here. Default (bare dev invocation only; an installed app's stub always passes its own bundle path) is `~/Library/Caches/Mac.apk/scratch/android`; also settable from the launcher's Settings panel folder picker. |
| `--orientation auto\|portrait\|landscape` (CLI) | Window orientation. `auto` reads the launchable activity's `android:screenOrientation` from the manifest and falls back to portrait. Exposed as a Picker in the launcher. |
| `--width N --height N` (CLI) | Explicit window pixel size, overriding the orientation default. Exposed as a size-preset Picker in the launcher (an "Auto" preset omits these and uses the biggest canvas the display supports). |
| `--fullscreen` / `MACAPK_FULLSCREEN=1` | Launches the app in a fullscreen window instead of a titled one. |
| `--hide-dock-icon` / `MACAPK_HIDE_DOCK_ICON=1` | Hides the runtime's own Dock icon/tile for this launch. |
| `MACAPK_FOLLOW_SYSTEM_APPEARANCE=0` | Forces light mode regardless of the Mac's current appearance. Unset (default) follows macOS's light/dark setting. |
| `--force-abi armeabi-v7a` (CLI) / `MACAPK_FORCE_ABI` | Forces the ARM32 lifter/recompiler path even when the APK ships an `arm64-v8a` library it would otherwise prefer. This is the launcher's "Force ARM32" Dev-menu toggle. |
| `MACAPK_LOG_LEVEL=<0-7>` | Runtime log verbosity floor (mirrors AOSP's own per-tag priority filter; unset defaults to INFO, AOSP's own default floor). Level 7 is needed to unlock the lowest-level `NativeLog.print` diagnostics. |
| `--dev-trace` (CLI) / `MACAPK_DEV_TRACE=1` | Turns on the broad diagnostic-tracing master switch (JNI, bionic-shim trace points, ...) in one step. Expect roughly 1 fps on a big game while it's on. This is the launcher's "Verbose trace (very slow)" Dev-menu toggle. |
| `MACAPK_ALLOW_UNSAFE_X18=1` | Diagnosis-only escape hatch that lets an arm64 guest load even when the host's x18-preservation capability probe fails. Never a fix for a real failure: macOS 26.4 is a hard floor for 64-bit native code. |
| `MACAPK_INSTALL_NOUI=1` | Skips the install confirmation dialog -- useful for a scripted/unattended install (`open --env MACAPK_INSTALL_NOUI=1 -a /Applications/Mac.apk.app <apk>`). |
| `MACAPK_EXTRACT_ASSETS=1` | Restores the pre-v1.0.1585 behaviour of extracting an app's assets onto disk instead of reading them from the installed APK in place. A bisect gate; only worth trying if an app that used to work stops finding its own assets. |

---

## CLI flags: `macapk-runtime` (`/Applications/Mac.apk.app/Contents/Resources/macapk-runtime`)

| Flag | Required/optional | Effect |
| --- | --- | --- |
| `--apk <path>` | Required unless `--peek-manifest` or `--gms` is given | The APK to launch. Made absolute (not symlink-resolved) before use, because `ApplicationInfo.sourceDir`/`getPackageCodePath()` etc. must answer with an absolute path the way a device does. Also exported as `MACAPK_APK_PATH` so `PresentedSdk` can read `targetSdkVersion` from the binary manifest before the framework's `Build.<clinit>` runs -- apktool strips `<uses-sdk>` from every decoded manifest, so the binary manifest is the only reliable source at that point. |
| `--width N` / `--height N` | Optional | Explicit window pixel size; overrides whatever the resolved orientation would have picked. |
| `--orientation auto\|portrait\|landscape` | Optional, default `auto` | `auto` reads `android:screenOrientation` off the launchable activity and falls back to portrait; `portrait`/`landscape` force it unconditionally. `--portrait`/`--landscape` are bare aliases for the same thing. |
| `--android-root <dir>` | Optional | User-visible directory for the Android filesystem mirror. Sets `MACAPK_ANDROID_ROOT` so every independent root-resolver downstream (the installer's cache fallback, the boot code's env chain, the GLES3.1 verdict cache) agrees. Falls back to the `MACAPK_ANDROID_ROOT` env var if the flag is absent, then to a compiled-in default expanded once `$HOME` is known. |
| `--verbose` | Optional | Sets the CLI-level verbose flag. |
| `--extract-only` | Optional | Non-launching mode: runs `apk_unpacker` + the android-root asset extract to backfill manifest/icon/`meta.json` for the launcher's Installed-Apps panel, then exits 0 -- no JVM, no Cocoa window, no native libs loaded. |
| `--peek-manifest <apk>` | Optional | Prints one JSON line (`package`, `versionCode`, `versionName`, `label`) read from the **binary** manifest, then exits. No extraction, no JVM, no window -- used so the install confirm dialog can show identity before paying for any of that. |
| `--gms <subcommand> [args...]` | Optional | Drives the GMS/Play-Services layer (accounts, sign-in, tokens, licensing) with no APK loaded. Everything after `--gms` is passed through verbatim as the subcommand's own args. This is the only path that can put the Google sign-in page on screen for the launcher's Accounts panel, and the only way an installed app (no launcher process to delegate to) gets one. Subcommands are handled by `com.macapk.gms.GmsCli` on the Java side (not further enumerated here; out of scope for a flag reference). |
| `--fullscreen` | Optional | Sets `MACAPK_FULLSCREEN=1` for this process -- see the env var's own row in "Launch & window" below. |
| `--hide-dock-icon` | Optional | Sets `MACAPK_HIDE_DOCK_ICON=1`. |
| `--dev-trace` / `--verbose-trace` | Optional | Sets `MACAPK_DEV_TRACE=1`, `MACAPK_TRACE_BL=1`, `MACAPK_TRACE_JNI_HANDLES=1` in one step -- the CLI/headless equivalent of the launcher's "Verbose Trace" Dev-menu toggle. Warns that it costs roughly 1 fps on HCR-class games. |
| `--force-abi <abi>` | Optional | Sets `MACAPK_FORCE_ABI=<abi>` -- the CLI equivalent of the launcher's "Force ARM32" toggle, so a headless run doesn't need a separate shell-env layer. |
| `-h` / `--help` | -- | Prints the usage banner and exits 0. |

Any unrecognized flag, or no `--apk`/`--peek-manifest`/`--gms` at all, prints usage and exits 2.

---

## The Mac.apk window's settings

The GUI translates a small set of pickers/toggles into the CLI flags and env vars above, then layers freeform user text on top:

| Setting | Maps to |
| --- | --- |
| Canvas-size preset Picker | `--width`/`--height` (omitted for the "Auto" preset, which lets the runtime pick the biggest size the display supports for the resolved orientation) |
| Orientation Picker (Auto/Portrait/Landscape) | `--orientation <value>` |
| "Show live log panel (verbose)" toggle | `--verbose` |
| "Write log to folder" toggle | Launcher-local file logging; not an env var passed to the runtime. |
| Android-root / app-storage folder picker ("where app files are stored") | `MACAPK_APP_CACHE_DIR` (the runtime treats this as authoritative when set) |
| App icon / label (derived automatically per installed app, not user-typed) | `MACAPK_APP_ICON`, `MACAPK_APP_LABEL`, `MACAPK_DEFAULT_ICON` (fallback icon bundled next to the runtime) |
| Dev menu -> "Force ARM32" toggle (menu revealed by 5-tapping the launcher's title) | `MACAPK_FORCE_ABI=armeabi-v7a` |
| Dev menu -> "Verbose trace (very slow)" toggle | `MACAPK_DEV_TRACE=1`, `MACAPK_TRACE_BL=1`, `MACAPK_TRACE_JNI_HANDLES=1` |
| "Verbose logging (all classloads)" toggle | When **off** (the default, for a fast/quiet UI run): sets `MACAPK_LOG_LEVEL=4` + `MACAPK_QUIET_CLASSLOAD=1`. When **on**: sets neither, so the run stays at AOSP's own INFO-level default floor with the classloader pair unsuppressed. A direct CLI run (no launcher involved) sets neither and is verbose by default. |
| "Show app versions in Applications" toggle | Cosmetic UI only (adds the version string to the Applications-panel row label); not an env var. |
| **Settings > Default environment** | Freeform key=value list, applied to every launch. |
| Per-app **Get Info** sheet environment | Freeform key=value list for that one app, applied on top of (overriding) the global Default environment. |

Order of application: derived/automatic vars first, then Dev-menu toggles, then the quiet-logging default, then `model.defaultEnv` (global user text), then the selected app's own `app.env` (per-app user text) -- so a per-app override always wins.

Separately, the tiny per-app stub every installed app actually runs reads `MACAPK_STUB_LOG` (`=0` disables its own stub-level logging), `MACAPK_STUB_NO_UI` (`=1` suppresses its error alert panel, so an unattended harness doesn't block on a dialog forever) and its own internal `MACAPK_STUB_REFRESHED` re-exec marker (v1.0.1592: a stub that differs from the installed Mac.apk's template replaces itself and re-executes), and the installer honours `MACAPK_RUNTIME_ROOT` and `MACAPK_CANONICAL_APP` overrides for where the staged shared runtime and the one canonical installed app live.

---

## Subsystem reference tables

Every `MACAPK_*` environment variable the shipped runtime, launcher or per-app stub reads, grouped by the subsystem that reads it.

Diagnostic/trace flags are kept with the subsystem whose file reads them (e.g. `MACAPK_TRACE_SQL` is under Install & storage, `MACAPK_TRACE_TOUCH` is under Launch & window) rather than pooled into one giant trace table. "Logging & tracing" below holds the flags whose reader is a cross-cutting hub file with no more specific subsystem home, plus the two master switches (`MACAPK_LOG_LEVEL`, `MACAPK_DEV_TRACE`).

---
## Launch & window

*(Diagnostic/trace flags whose primary reader happens to live in a launch/window/activity-lifecycle file are kept here with their subsystem rather than pooled into "Logging & tracing" below -- e.g. `MACAPK_TRACE_TOUCH` sits with touch/activity dispatch, not in the generic trace table.)*

| Flag | Default (unset) | Accepted values | Effect | Audience |
| --- | --- | --- | --- | --- |
| MACAPK_ACTIVITYTHREAD_INSTANTIATE | Reflective `newInstance()` component construction | `mh` (MethodHandle-based construction) | Selects the primitive `AppComponentFactory` uses to instantiate Activities/Applications. `=mh` is an A/B-only alternate path (2 fewer java.base frames in a launch-chain trace). | experiment |
| MACAPK_ACTIVITYTHREAD_LAUNCH | Activity/Application construction goes through today's `Class.forName` path | `1` / `true` | Routes Application construction through an AOSP-shaped `ActivityThread` chain instead of the direct path. Off = today's behavior. | experiment |
| MACAPK_ACTIVITY_CLEAR_TASK | Off | `1` | Extra handling for an intent carrying `NEW_TASK\|CLEAR_TASK` flags together. Default off; comment notes the common combination this addresses. | bisect |
| MACAPK_ACTIVITY_RECREATE | ON: a real `Activity.recreate()` happens on a resource-affecting config change | `0` | `=0` restores the pre-v1.0.996-era notify-only behavior (no actual recreate) instead of really recreating the Activity. | bisect |
| MACAPK_ACTIVITY_THEME_ANCHOR | ON (current AOSP-faithful anchor behavior) | `0` | `=0` reverts to the pre-change theme-anchor resolution for an Activity. | bisect |
| MACAPK_ALLOW_HOST_URL_AT_LAUNCH | Off: a host URL open requested during launch is refused (with the reason logged) | `1` | `=1` restores the old "always open immediately" behavior for a URL an app tries to open the host browser with right at launch. | bisect |
| MACAPK_AOSP_ACTIVITY_THEME | ON (current forwarding behavior, since v914) | `0` | `=0` restores the pre-v914 theme-forwarding behavior for an Activity's resolved theme. | bisect |
| MACAPK_APP_CACHE_DIR | ANGLE's blob cache and the launcher's own cache root fall back to their own defaults | A directory path | The runtime's own cache root (used e.g. by the ANGLE shader-blob cache); the launcher sets this per-app so caches land under its own app-storage tree. | user |
| MACAPK_APP_ICON | The bundled Mac.apk default icon is used for the Dock tile | A filesystem path to a PNG | The launched app's own icon, applied to its Dock tile and window; set automatically by the launcher/stub from the app's install record, not typically hand-typed. | dev |
| MACAPK_APP_LABEL | Falls back to the Activity's own title | A display-name string | Window title / Dock tile label the launcher passes for the app being launched; `Activity` reads it before falling back to the activity title. | dev |
| MACAPK_AUDIO_SELFTEST | Off (no selftest at boot) | `1` | Drives the audio backend self-test at boot (tone/PCM roundtrip); combine with `MACAPK_AUDIO_DUMP=<path>` to capture the output. | diag |
| MACAPK_AWT_PREWARM | Off (gate inverted on purpose -- prewarming is opt-IN) | `1` | Opts in to prewarming AWT during boot; the comment notes the gate was deliberately inverted (default off) after measuring the prewarm's cost against a baseline. | experiment |
| MACAPK_CLINIT_PAD | 0 (no padding) | An integer *n* | Diagnostic-only: inserts *n* recursive stack frames before running an app's static initializers, to test how deep the launch call stack can safely get. ARM32-path only. | diag |
| MACAPK_CONFIG_REPORT_GATE | ON (current, since v1.0.1047) | `0` | `=0` restores the pre-v1.0.1047 behavior for whether a resource-only configuration change (vs. one the app's `configChanges` manifest entry actually declares handling) gets reported to the Activity. | bisect |
| MACAPK_DECOR_FITS_HONOUR | Off: window-insets "fits system windows" decor requests are ignored (comment says honouring them by default would currently be WORSE than not gating at all) | `1` | Restores honouring `WindowInsets`/`fitsSystemWindows` decor requests instead of ignoring them; explicitly a one-run kill switch, not a recommended default. | experiment |
| MACAPK_DEFAULT_ICON | No fallback icon is available if the app has none of its own | A filesystem path to a PNG | The bundled Mac.apk fallback icon path, handed to the runtime so an app with no icon of its own is never blank/generic. Set automatically by the launcher/stub, not typically hand-typed. | dev |
| MACAPK_DISPLAY_DECOR | ON: `Display` reports a decor-adjusted (real == app) size ladder | `0` | `=0` collapses `Display`'s size-reporting ladder back to a single (real==app) answer instead of the AOSP-faithful decor-aware one. | bisect |
| MACAPK_FCM_TEST_PUSH | Off (no synthetic push) | `k=v,k2=v2` (or `1` for a default payload) | Injects one synthetic push through the real FCM delivery path at boot, for testing an app's push handling without a real server round-trip. | diag |
| MACAPK_FCM_TEST_PUSH_DELAY_MS | 20000 (20s) | An integer (milliseconds) | Overrides the delay before `MACAPK_FCM_TEST_PUSH` fires, so the app's boot/registration has time to finish first. | diag |
| MACAPK_FIT_WINDOW_TO_ASPECT | ON: the window is nudged to the app's aspect ratio rather than sitting in a letterbox band | `0` | `=0` restores the pre-change behavior of leaving a letterbox band instead of fitting the window to the content's aspect ratio. | bisect |
| MACAPK_FOLLOW_SYSTEM_APPEARANCE | ON: light/dark mode follows the current macOS appearance | `0` | `=0` forces light mode regardless of the Mac's appearance setting; the safe default when unset is to follow macOS. | user |
| MACAPK_FULLSCREEN | Off: normal titled window | `1` | Launches into a fullscreen window. Set by `--fullscreen` on the CLI. | user |
| MACAPK_HIDE_DOCK_ICON | Off: the runtime shows its own Dock icon | `1` | Hides the runtime's own Dock icon/tile for this launch. Set by `--hide-dock-icon` on the CLI. | user |
| MACAPK_HOST_URL_GRACE_MS | A built-in default grace period | An integer (milliseconds) | Overrides the grace period `MACAPK_ALLOW_HOST_URL_AT_LAUNCH`'s launch-time URL-open check uses. | dev |
| MACAPK_INSTALL_NOUI | Off: the install confirm dialog is shown | `1` | Skips the install confirmation dialog -- how an unattended script (`open --env MACAPK_INSTALL_NOUI=1 -a ...`) installs an APK without a human clicking through a dialog. | user |
| MACAPK_KEYBIND_PROFILE | The launcher auto-picks (and remembers) a keybind profile for the package on first run | A profile name string | Forces a specific keyboard-input profile instead of the auto-picked/remembered one for this package. | dev |
| MACAPK_KILL_ON_UNCAUGHT | Gated behavior (see source; default is the safer of the two, flip-then-land shape) | a truthy value | Controls whether the process is actually killed after an uncaught exception is reported, vs. reported-only. | bisect |
| MACAPK_LAUNCH_ACTION | `ACTION_MAIN` | An Intent action string | Overrides the action of the synthetic launch Intent Boot builds for the app's launchable component. | dev |
| MACAPK_LIFECYCLE_TRACE | Off | any non-null value | Traces Activity/Application lifecycle callback order (onCreate/onStart/onResume/...). | diag |
| MACAPK_MEDIA_SELFTEST | Off | `1` | Runs the media-decode self-test at boot. | diag |
| MACAPK_MENU_TRACE | Off | any non-null value | Traces options-menu inflate/dismiss and the XML-resource-inflate path it shares with `Resources`' menu handling, plus locating hosted androidx Fragments. | diag |
| MACAPK_PROVIDER_PROCESS_GATE | Gated behavior for protected-fleet apps (see source) | `1` | Flips a ContentProvider process-affinity check used by some protected-fleet apps. | bisect |
| MACAPK_RESTART_INTENTS | No restart-intents file; a relaunch after the previous process exits starts fresh | A filesystem path | Names the file the previous process's exit handler writes pending relaunch Intents into, so `Boot` can hand them to the newly-started process -- the mechanism behind an app-initiated "restart the app" flow. | dev |
| MACAPK_REWIRE_KEEP_CANVAS (bisect gate, default ON since v1.0.997) | ON: a rewired root view keeps its Canvas-tier driver attached | `0` | `=0` restores the pre-v1.0.997 behavior where a rewired root view lost its Canvas-tier rendering driver. | bisect |
| MACAPK_SELF_CAPTURE_WINDOW_MS | A built-in default window | An integer (milliseconds) | Reads at the source; effect not further documented at the site beyond being a self-capture time window (see the Rendering section's `MACAPK_SELF_CAPTURE*` family for the mechanism this tunes). | diag |
| MACAPK_SKIP_QOS_BOOST | Off: the QoS boost call runs | `1` | Skips a QoS-class boost call during boot, as a targeted workaround (see `docs/runtime_2026-07-23.md`) for whatever cost that call was found to have. | bisect |
| MACAPK_STACK_PAD | Off (0) | An integer *n* | Diagnostic: pads the launch dispatch stack by *n* frames, ARM32-path-only companion to `MACAPK_CLINIT_PAD` for isolating "the launch stack got deeper" symptoms. | diag |
| MACAPK_STUB_LOG | On: the per-app stub logs its own diagnostic lines | `0` | `=0` turns the per-app stub's own logging off outright. | dev |
| MACAPK_STUB_REFRESHED | unset | `1` (set by the stub itself) | INTERNAL re-exec marker, never for a user to set: when a per-app stub finds its own file differs from the installed Mac.apk's stub template it swaps the template in and re-executes itself with this set, so the fresh copy knows it was just refreshed (it logs "re-executed") and a copy that still differs does not loop; the stub unsets it before handing over to the runtime (v1.0.1592, `StubRefresh` in InstallCore.swift). | dev |
| MACAPK_TOUCH_DIAG | Off | any non-null value | Traces where a touch lands in the Activity/View dispatch chain (Java side); the native counterpart is the ARM32-side half of this same env var (see the Rendering/Native tables' `MACAPK_TOUCH_DIAG` cross-reference). | diag |
| MACAPK_TRACE_APPINFO | Off | any non-null value | Traces `ContextWrapper`'s `ApplicationInfo` resolution path. | diag |
| MACAPK_TRACE_DEFSTYLE | Off (zero cost when unset) | any non-null value | Traces the 4-arg `obtainStyledAttributes`/theme `defStyleAttr` resolution chain across `Context`, `Resources`, `ContextThemeWrapper`, and `View` -- the flag the theme-anchor investigations cite repeatedly. | diag |
| MACAPK_TRACE_GETSVC | Off (except `jobscheduler`, always traced) | `1` | Traces `ContextWrapper.getSystemService()` lookups; `jobscheduler` is always traced regardless of this flag. | diag |
| MACAPK_TRACE_JNI | Off | any non-null value | Patches the process-wide JNIEnv function-pointer table to log every JNI call made through it. | diag |
| MACAPK_TRACE_LAUNCH_STACK | Off | `1` | Dumps the call stack of the app's own launch path at the point Application construction begins. | diag |
| MACAPK_TRACE_LIFECYCLE | Off | `1` | Traces `ActivityHost`'s own lifecycle-dispatch bookkeeping (a narrower sibling of `MACAPK_LIFECYCLE_TRACE`). | diag |
| MACAPK_TRACE_TITLE | Off | any non-null value | Traces the ActionBar/Toolbar title-propagation chain from `Activity.setTitle()`. | diag |
| MACAPK_TRACE_TOUCH | Off | `1` | Logs the first few dispatched `MotionEvent`s (Java side: `MotionEvent`, `View`). | diag |
| MACAPK_UITHREAD_TRACE | Off (read once, cached) | any non-null value | Traces `Handler.post()`/`Looper` queueing: which thread queued what, and how often -- read once at class-init instead of per-call, per the comment, because a prior version paid a `System.getenv` cost on every `post()`. | diag |
| MACAPK_UNITY_FORCE_RESUME (⚠ diagnostic only, NOT A FIX per its own comment) | Off | `1` | Forces a Unity Activity's resume path unconditionally. Marked in-source as diagnostic-only, not a real fix. | diag |
| MACAPK_USER_ROT_RESTORE (bisect gate, since v1.0.996) | ON | `0` | `=0` restores the pre-v1.0.996 user-rotation-restore behavior. | bisect |
| MACAPK_VIDEO_IMAGE_SELFTEST | Off | `1` | Runs the video/image self-test at boot (mirrors `MACAPK_MEDIA_SELFTEST`'s gating shape). | diag |
## Install & storage

| Flag | Default (unset) | Accepted values | Effect | Audience |
| --- | --- | --- | --- | --- |
| MACAPK_ANDROID_ROOT | Falls back to `$HOME/Documents/android` (native resolver); a Java-side `~/Library/Caches/Mac.apk/scratch/android` fallback is inert in real runs since the runtime always passes an already-resolved path | A filesystem path (tilde expanded) | Overrides the root of the user-visible Android filesystem mirror (data/app, sdcard, etc.) that the boot code, Environment, SettingsStore and the native path-remap shim all resolve against. Consulted after a CLI `--android-root` value and before the `$HOME/Documents/android` default. | user |
| MACAPK_CANONICAL_APP | `/Applications/Mac.apk.app` | A filesystem path to a .app bundle | Overrides which installed app bundle the build/promote/doctor/restage scripts treat as the one canonical installed Mac.apk, for staging, promotion, and staleness checks. | dev |
| MACAPK_COMPONENT_GATING | ON: component/application enabled-state gating (AOSP's PackageUserStateUtils.isEnabled) is enforced | "0" disables; anything else is ON | Default-on gate for enforcing manifest/runtime component-enabled state on provider and component construction. Setting it to 0 degrades the check to the pre-v1.1.0 always-manifestEnabled behavior for A/B. | bisect |
| MACAPK_DEBUG_REWRITER | Off; boot logs stay quiet | "1" | Logs every class ClassFileRewriter detects and rewrites (e.g. constructor/supertype fixups), used to verify byte-identical rewritten output when other rewriter gates are toggled. | diag |
| MACAPK_DEX_FRAME_DEBUG | Off | Any non-null value (presence check) | Names the bytecode offset that caused CodeWriter to drop a StackMapTable during DEX conversion, since a dropped table is otherwise invisible until a much later "expecting a stackmap frame" verifier error. | diag |
| MACAPK_DEX_FRAME_SELFCHECK | Off, saving the doubled frame-encoding work | Any non-null value | Re-decodes every StackMapTable the writer produces and fails loudly if it does not read back as the frames it was given, turning the frame encoder into a self-checked component. | diag |
| MACAPK_DEX_INIT_REPOINT | ON: the plan that re-synthesizes R8-deleted constructors is built and applied | "0" disables it | Controls the fix that undoes R8's constructor re-pointing (calling a superclass constructor directly), which ART tolerates but the JVM's split verifier rejects per JVMS 4.9.2. "0" is an A/B escape hatch that turns the whole fix off. | bisect |
| MACAPK_DEX_NARROW_DEBUG | Off | Any non-null value | Diagnostic-only flag naming methods whose type inference fell back to the ART exception-edge rule, where residual handler-frame mismatches concentrate. | diag |
| MACAPK_DEX_OUTLINE_DEBUG | Off | Any non-null value | Logs the specific reason MethodOutliner refused to hoist a region out of an oversized method; refusals are otherwise invisible since the class just fails to translate as it did before this transform existed. | diag |
| MACAPK_DEX_OUTLINE_FORCE | -1/off; the real 65535-byte JVM code_length cap is the only trigger | An integer (a pretend byte-length threshold) | Lowers the "method too large" threshold so ordinary small test methods get outlined too, letting the maintainers' semantic test diff outliner output against javac (javac itself cannot compile a method that actually hits 65535 bytes). | experiment |
| MACAPK_DEX_SPLIT_DEBUG | Off | Any non-null value | Logs why MethodSplitter refused to split an oversized method, and how many parts a split method produced. | diag |
| MACAPK_DEX_SPLIT_FORCE | -1/off; the real 65535-byte code limit is the only trigger | An integer (bytes; a pretend code-size limit) | Lowers MethodSplitter's size threshold so ordinary methods get split, letting the semantic gate diff split output against javac on small cases. | experiment |
| MACAPK_DEX_SPLIT_NO_LIVENESS | Liveness-based trimming is on, so a split part only receives registers actually live at its entry | Any non-null value disables it | Escape hatch that makes MethodSplitter pass every DEFINED register to a split part instead of only the LIVE ones, so a suspected liveness-analysis bug can be A/B'd in one run. | bisect |
| MACAPK_DEX_SUPER_IFACE | The super-interface substitution plan is built and used | "0" disables it | Escape hatch to turn off the plan that substitutes default-interface-method call targets during DEX conversion. | bisect |
| MACAPK_DEX_THREADS | `min(available processor count, a fixed max)` worker threads | An integer (thread count); values below 1 ignored | Overrides the DEX-to-class conversion worker-thread count. Output is byte-identical regardless of thread count, so this mainly exists to A/B the parallel and sequential paths. | experiment |
| MACAPK_DEX_WIDE_HANDLERS | ON when the method has wide handler edges: exception-handler edges are computed from every instruction rather than only ones that can throw | "0" disables it | Reconciles a Dalvik/JVM disagreement about which instructions route to an exception handler; "0" restricts edges to instructions canThrow() recognizes. | bisect |
| MACAPK_EXTRACT_ASSETS | Off; assets are read directly from the APK in place, and any previously-extracted asset tree is reclaimed (deleted) on launch | "1" | Restores the legacy behavior of tar-extracting assets/* from the APK into the app's data directory instead of reading them from the APK in place. It is the bisect gate for the "assets read from the APK" change. | bisect |
| MACAPK_EXTRACT_ONLY | The unpacker assumes this process will boot the app afterward, so it waits for any in-flight background DEX conversion to finish | "1" (set internally) | Internal signal, set by the runtime when the launcher's --extract-only install-mode CLI flag is used, telling the unpacker to return to the installer immediately rather than waiting on a background DEX conversion. | dev |
| MACAPK_FD_TRACE | Off | Any non-null value | Logs AssetFileDescriptor construction (with a stack trace) and getParcelFileDescriptor() calls, to debug native consumers (e.g. Unity) that mmap an asset slice. | diag |
| MACAPK_FORCE_ANIM_SCALE_0 | Off; Settings.Global animation-scale reads return their normal value | "1" | Forces the animator/transition/window animation duration scales to 0 so Compose's animateTo() snaps instantly, as a decisive test for animations that appear frozen. Explicitly marked not a shipping flag. | diag |
| MACAPK_FORCE_REEXTRACT | Off; a fresh asset tar-extraction or native-library unzip is skipped whenever the on-disk freshness stamp already matches the current APK identity | "1" | Forces re-extraction of assets (when MACAPK_EXTRACT_ASSETS=1) and of the install directory's native libraries, ignoring the freshness stamp that would otherwise skip the work. | diag |
| MACAPK_FW_RES_APK | ⚠ CORRECTED 2026-09-20 (Phase 5 step 9, v1.0.1590): the two fallbacks after the env-var/next-to-jar lookup (apktool's `~/Library/apktool/framework/1.apk` and a cwd-relative `thirdparty/framework-res/*.apk`) were deleted along with apktool retirement. An unset/invalid value with no `tools/framework-res.apk` staged next to the jars is now a loud error (`NativeLog.e`: "framework resource apk NOT FOUND"), not a silent fallback. | A filesystem path to a framework-res.apk | Pins an explicit framework-res.apk as the source for both the framework asset mount (AssetManager) and the framework resource table (ResourceIndex), ahead of every other candidate. Set only by the maintainers' answer-probe harness to match its API-30 reference emulator. | diag |
| MACAPK_GLIDE_HTTPFETCHER_SHIM | Off; Glide's DownloadRequestLoader class is left untouched | "1" | Opt-in bytecode rewrite that redirects F-Droid's Glide-based DownloadRequestLoader to a framework HttpFetcher_Macdr shim. Still opt-in because of a cross-classloader visibility issue with Glide's own interfaces. | experiment |
| MACAPK_INSTALLHELPER | Resolves macapk-installhelper next to the running runtime binary's bundle, else the canonical installed app | A filesystem path to an executable | Picks which macapk-installhelper binary the install path invokes; it does not change what the helper demands, since the helper alone owns the install consent/authorization gate. | dev |
| MACAPK_PM_ALWAYS_METADATA | Off; the GET_META_DATA Bundle is only populated when the caller passes the GET_META_DATA flag | "1" | Forces getApplicationInfo/getPackageInfo to always populate the metadata Bundle regardless of the caller's flags, restoring the pre-v1.0.923 unconditional behavior for A/B. | bisect |
| MACAPK_PM_REGISTRY | ON; another Mac.apk-installed app's PackageInfo/ApplicationInfo is reported as installed (subject to package-visibility filtering) | "0" disables it | Default-on gate for consulting the cross-app installed_apps.json registry so getPackageInfo/getInstalledPackages can see other Mac.apk-installed apps; "0" is the explicit A/B opt-out. | bisect |
| MACAPK_PM_REGISTRY_FILE | Reads `~/Library/Application Support/Mac.apk/installed_apps.json` | A filesystem path to a JSON registry file | Points the installed-apps registry reader at an alternate file, so a regression test can drive a poisoned or synthetic registry without touching the user's real one. Only changes what PackageManager reports, not install authorization. | diag |
| MACAPK_PM_SELF_ONLY | ON; PackageManager truthfully reports only the running app (plus visible registry entries) as installed, and denies permission checks for other packages | "0" restores the legacy behavior | Default-on gate for AOSP-correct "not installed"/"not granted" answers about other packages; "0" restores the legacy blanket lie (every package looks installed, every permission reads GRANTED) for A/B. | bisect |
| MACAPK_REWRITER_PRESCAN | ON; a cheap byte-needle prescan skips the full structured class parse for classes that cannot match any rewrite pattern | "0" disables it | Rollback escape hatch that forces ClassFileRewriter to always do the full structured parse, used to verify the prescan produces byte-identical rewritten classes to the always-full-parse baseline. | bisect |
| MACAPK_REWRITER_PRESCAN_STATS | Off; no skip/full-parse tally is logged | "1" | Logs a running skip-vs-full-parse tally every 500 classes seen, to measure the prescan's skip ratio during rollout verification. | diag |
| MACAPK_RUNTIME_ROOT | The launcher resolves and self-heals the shared runtime staged at `~/Library/Application Support/Mac.apk` | A filesystem path to a runtime install root (containing Resources/macapk-runtime) | Points the launcher at an explicit alternate runtime tree instead of the normally staged/self-healing one, short-circuiting both resolveRuntime() and ensureStaged() so an A/B target is never overwritten or healed. | dev |
| MACAPK_SQLITE_JOURNAL_MODE | "delete" (WAL is coerced off process-wide) | One of delete/truncate/persist/memory/wal/off (whitelisted; invalid values fall back to delete with a warning) | Global (not per-APK) override of the SQLite journal mode used on every open and re-applied after any SQL that touches journal_mode; exists so the WAL-vs-heap-corruption question can be A/B'd without a rebuild. | bisect |
| MACAPK_SQLITE_TX_PER_THREAD | ON; each thread gets its own transaction stack, matching AOSP's SQLiteSession model | "0" restores the legacy shared stack | Bisect gate for the per-thread transaction-stack fix (which fixed an Aurora Store bug); "0" restores the old single shared TxState stack across all threads. | bisect |
| MACAPK_TRACE_CURSOR | Off | "1" | Logs each SQLiteCursorImpl step call (statement handle and sqlite3_step return code) during cursor movement, to localize cursor-iteration bugs. | diag |
| MACAPK_TRACE_DEVICEINFO | Off | "1" | Logs the real per-call answers from PackageManager's device/feature-profile queries (hasSystemFeature, getSystemAvailableFeatures, getSystemSharedLibraryNames, getSharedLibraries), added while auditing what Aurora Store's device-properties provider reads. | diag |
| MACAPK_TRACE_SQL | Off | "1" | Logs every native SQL exec/prepare call with its SQL text and return code, plus SQLiteDatabase's cursor-factory selection, to localize bugs where an INSERT appears to succeed but a table stays empty. | diag |
| MACAPK_TRACE_TX | Off | "1" | Logs transaction begin/end depth per thread and database, to trace transaction interleaving across threads. | diag |
## Resources & theme

| Flag | Default (unset) | Accepted values | Effect | Audience |
| --- | --- | --- | --- | --- |
| MACAPK_ANIMATOR_INFLATE | ON: real animator/state-list-animator XML inflation runs | 0/1 (only "0" disables) | Real inflation of animator and state-list-animator XML runs instead of the old stub; =0 restores the pre-fix stub, a labelled bisect gate since real inflation can throw NotFoundException where the stub never could. | bisect |
| MACAPK_APK_PATH | per-app SDK clamp can't read the manifest; falls back to the flat DEFAULT_SDK (29) with a warning logged | a filesystem path to the running APK | Exported automatically by the native launcher so PresentedSdk can open the APK's manifest and read targetSdkVersion; without it, MACAPK_SDK_FROM_TARGET stays inert. | dev |
| MACAPK_ARSC_INDEX | (DELETED at v1.0.1590 step 9b, same day this doc was written) | -- | Until v1.0.1590 the switch between apktool's decoded text tree (`=0`, the pre-v1.0.1589 default) and the resources.arsc ingest (`=1`, default from v1.0.1589). Phase 5 step 9 retired apktool and step 9b deleted the text loaders and this gate: `ResourceIndex` ingests every installed member's resources.arsc unconditionally (an ingest failure leaves the index EMPTY with a `NativeLog.e`), so the variable is no longer read anywhere. | bisect (retired) |
| MACAPK_RES_FILES_IN_PLACE | (DELETED at v1.0.1590 step 9b, same day it was introduced) | -- | Introduced with Phase 5 step 7 as the bisect back to the decoded-tree resolver for file resources; step 9b deleted that resolver and the gate. Every file-based resource is served from the installed set's table rows (`ResourceIndex.openRaw` / `openRawByName`); the variable is no longer read anywhere. | bisect (retired) |
| MACAPK_ATTRSTYLE | ON: style="?attr"/"@style" refs resolve, plus framework EditText style rows and the underline drawable | 0/1 (only "0" disables) | Bundles three related fixes (style-ref resolution, Widget.Material.EditText rows, the edit_text_material underline) behind one switch so a fleet A/B always comes from a single binary; =0 restores the pre-lane behaviour of all three. | bisect |
| MACAPK_ATTR_NAME_RESOURCE | ON: getAttributeNameResource answers the real per-namespace attribute id | 0/1 (only "0" disables) | Lets androidx's ColorStateListInflaterCompat (and similar AttributeSet consumers) key selector rows on the real attribute id instead of always 0; =0 opts back out to the old always-0 answer. | bisect |
| MACAPK_ATTR_NS_EXACT | ON: an app-package attr slot reads only the res-auto namespace | 0/1 (only "0" disables) | Namespace-aware lookup for app-declared styleable attribute slots, matching how AOSP fills slots by attribute resource id; =0 reverts to the old fuzzy any-namespace match, a labelled bisect gate. | bisect |
| MACAPK_ATTR_RESID_THEME | ON: a copied theme cascade (via Theme.setTo) is consulted for id-keyed ?attr resolution | 0/1 (only "0" disables) | Lets a theme cascade copied by setTo answer an id-keyed attribute-reference lookup instead of dead-ending; =0 restores the pre-fix static-anchor answer, a bisect gate for a 23-app recapture rather than a supported mode. | bisect |
| MACAPK_DIAG_INFLATE_IDS | off: no logging | 1 | Logs the resource ids of every view in a subtree right after it is inflated. | diag |
| MACAPK_DRAWABLE_CACHE_TRACE | off: no logging | any non-null value | Logs every hit/miss of the per-Resources DrawableCache, for verifying or debugging the cache; never on the hot path otherwise. | diag |
| MACAPK_FW_SIMPLE_VALUES | ON (wide): getValue() reports any resolved simple type for a framework id, not just colours | 0/1 (only "0" narrows) | Widens which resolved value types (ints, bools, dimensions, fractions, floats, strings) Resources.getValue() reports for a framework resource id; =0 narrows it back to colours only, for bisecting a regression without a full revert. | bisect |
| MACAPK_FW_VIEW | ON since v1.0.1104: the platform (framework-res.apk) resolution view builds regardless of the app-table ingest gate | 0/1 (only "0" disables) | Decouples building the read-only platform-table resolution view from whether the app's own resources.arsc is being ingested, so framework style/attr lookups can hit the real platform table either way; =0 restores the pre-v1103 coupling. | bisect |
| MACAPK_NAV_BAR_SIDE | ON: navBarPosition() can report the nav bar on the left/right edge in landscape | "0" or "false" disables; anything else is on | Bisect gate for the side-navigation-bar geometry work; =0 forces navBarPosition() back to NAV_BAR_BOTTOM, restoring the exact pre-change geometry in one run. | bisect |
| MACAPK_SCREENLAYOUT_DERIVE | ON: Configuration's screenLayout size/long bits are recomputed live from current dp metrics | 0/1 (only "0" disables) | Recomputes screenLayout from the Configuration's live dp metrics the way AOSP derives it (seeded optimistically, then reduced), instead of leaving the static value; =0 is the kill switch back to the static value. | bisect |
| MACAPK_SDK_CEILING | the compiled ceiling, 36 | an SDK integer, clamped to [29, MAX_KNOWN_SDK=37] | Overrides the compiled ceiling on the per-app targetSdk clamp without a rebuild, so each candidate ceiling can be A/B'd in one run instead of a rebuild-plus-sweep cycle. | experiment |
| MACAPK_SDK_FROM_TARGET | ON since v1.0.1012: presented SDK = clamp(targetSdkVersion, FLOOR, CEILING) per app | 0/1 (only "0" disables) | Presents each app the highest Android version its own targetSdkVersion supports instead of a flat level; =0 disables the per-app clamp and restores the flat DEFAULT_SDK (29) for every app. | bisect |
| MACAPK_SDK_INT | unset: the level comes from MACAPK_SDK_FROM_TARGET's clamp (or the flat default) | an SDK integer in AOSP's [1,10000], capped at MAX_KNOWN_SDK=37 | Absolute override of the presented Android version that wins over every other gate, used to force one specific API level per run for testing. | experiment |
| MACAPK_SKIP_ALL_IMAGE_DRAW | off: ImageView draws normally | 1 | Skips onDraw entirely for every ImageView, a diagnostic for isolating whether an ImageView's own drawing is causing a rendering problem. | diag |
| MACAPK_SKIP_IMAGEVIEW_DRAW | off: no class is skipped | a simple class name (e.g. "FeatureImage") | Skips onDraw only for ImageView subclasses whose simple class name matches the given string, a narrower version of MACAPK_SKIP_ALL_IMAGE_DRAW for isolating one widget type. | diag |
| MACAPK_STATUSBAR_BLEND | the status/nav bar uses the AOSP DecorView.calculateBarColor path; the retired blend never fires | 0/1 | =1 restores the retired behaviour of blending the bar with the sampled colour of the content row beneath it (the sense flipped from opt-out to opt-in once measurement showed the blend path never actually fired on the fleet); =0 explicitly forces the plain colorBackground strip. | bisect |
| MACAPK_STYLE_PARENT_EMPTY | an explicit parent="" on a style suppresses dotted-name parent inference (the aapt2-correct behaviour), on the text-decoded ingest path | 0/1 (only "0" disables) | A text-decode repair knob; =0 disables the parent="" suppression so a dotted style name always infers a parent, restoring the older (aapt2-incorrect) behaviour for bisecting a regression. | bisect |
| MACAPK_TEXT_SP | ON: TextView's default text size is the density-scaled equivalent of 14sp | 0/1 (only "0" disables) | Scales TextView's default text size to match AOSP's 14sp-equivalent default instead of a static raw-pixel value; =0 reverts to the old static 14f raw-px default so the fix can be A/B'd. | bisect |
| MACAPK_THEME_STRING_FALLBACK | ON since v1.0.920: getString()/getText() can resolve a theme-only string attribute through the Theme's cascade | 0/1 (only "0" disables) | Lets a validated theme-cascade lookup answer getString()/getText() when no XML/style/defStyle value exists (e.g. a manifest theme's viewInflaterClass string); =0 opts out, a labelled bisect gate. | bisect |
| MACAPK_THEME_TIER_WINDOWBG | off: windowBackground/android:windowBackground are excluded from the theme-tier serve | 1 (only value that re-enables) | Re-enables the theme tier's "honest" serve of windowBackground for parity work; left off by default because it broke unitconverter's dark Help dialog (AppCompat wants a darkened version we don't implement). | experiment |
| MACAPK_THEME_TIER_XML | ON: an XML-inflated TypedArray also consults the theme as tier 5 | 0/1 (only "0" disables) | Extends AOSP's five-tier attribute resolution (theme as tier 5) to the XML-inflation path, which previously stopped at tier 4; =0 reverts to the tier-4 stop, a labelled bisect gate for the fleet measurement. | bisect |
| MACAPK_TRACE_ATTR | off: no per-attribute tier tracing | an attribute name (e.g. "textAppearance") | Prints every resolution tier's answer for one named attribute on every TypedArray parse, added to find "which tier failed" while diagnosing a Material ThemeEnforcement throw. | diag |
| MACAPK_TRACE_ATTRCOLOR | off | 1 | Logs which resolution tier answered a ?attr colour reference and with what value, to tell the XML-drawable path apart from the View/TypedArray path when they disagree. | diag |
| MACAPK_TRACE_BARCOLOR | off | any non-null value | Logs the computed status/nav bar flags, declared colour, contrast-scrim flag, and the final resolved bar colour. | diag |
| MACAPK_TRACE_CL_PARAMS | off | 1 | Logs each ConstraintLayout LayoutParams field as it is set from a parsed attribute, naming the target id and whether the set succeeded. | diag |
| MACAPK_TRACE_CSL_INFLATE | off | any non-null value | Logs whether Mac.apk's own ColorStateList inflater or the app's bundled androidx inflater actually ran for a res/color selector, the load-bearing question for the state-list un-flattening work. | diag |
| MACAPK_TRACE_DEFSTYLE_APPLY | off | 1 | Prints which defStyleAttr/style resolved for a 4-arg TypedArray parse, used to localise widgets that get no default style via the fleet's INK IMBALANCE column. | diag |
| MACAPK_TRACE_FEATUREIMG | off | 1 | Logs bitmap-apply and draw details for ImageView subclasses whose simple name contains "FeatureImage", tracing Glide's rescue image-loading path. | diag |
| MACAPK_TRACE_FWSTYLE | off | 1 | Logs which style names the platform (framework-res.apk) resolution table answers that the app's own table did not, naming the exact seam where MACAPK_FW_VIEW changes behaviour. | diag |
| MACAPK_TRACE_GETVALUE | off | any non-null value | Logs the id and resolved type/name whenever Resources.getValue() can't resolve a path, e.g. to find exactly which drawable resource an app failed to load. | diag |
| MACAPK_TRACE_GUIDE | off | any non-null value | Diagnostic tracing and tree-dump accessors for a ConstraintLayout guideline and related fill/tint/stroke drawable state, used to see why a filled shape isn't painting. | diag |
| MACAPK_TRACE_IMAGEVIEW_DRAW | off | "1" (one-shot per instance) or "verbose" (every draw) | Traces ImageView.onDraw for the non-bitmap-drawable path; verbose mode catches double-traversal of the same instance within one frame that one-shot mode would hide. | diag |
| MACAPK_TRACE_IMAGEVIEW_MEASURE | off | 1 | Logs an ImageView's onMeasure spec modes/sizes, intrinsic size, and resulting output size. | diag |
| MACAPK_TRACE_IMAGEVIEW_SET | off | 1 | Logs an ImageView's bounds, drawable class, and extracted bitmap size at the moment setImageDrawable installs a new drawable. | diag |
| MACAPK_TRACE_LAYOUTREQ | off | 1 | Dumps each inflated view's isLayoutRequested() state and child count at the end of inflate(), to tell whether a ConstraintLayout's children lost that flag before or after inflation (which decides if the constraint solver ever registers them). | diag |
| MACAPK_TRACE_MTLBTN | off | any non-null value | Logs a Material button's element/style-declared backgroundTint, the value chosen, its resolved colour, and the theme-overlay container colour while inflating that widget. | diag |
| MACAPK_TRACE_RESOLVEATTR | off | an attribute name, or "*" | Traces Theme.resolveAttribute, a separate path from TypedArray that androidx's SplashScreen handoff uses; a silent false there strands an app on its splash theme for the whole session. | diag |
| MACAPK_TRACE_RL | off | 1 | Logs each RelativeLayout child's resolved rules, margins, and measured size during the first measure pass. | diag |
| MACAPK_TRACE_TA | off | any non-null value | Logs how many of a large (>40-attribute) styleable's attributes actually resolved on a TypedArray parse. | diag |
| MACAPK_TRACE_THEMEPROBE | off | any non-null value | Logs every theme-only getString() answer (attribute name and resolved value) once macapkThemeString has validated it. | diag |
| MACAPK_TRACE_TINT | off (zero cost unset) | any non-null value | Logs the effective resolved tint colour applied to an ImageView's drawable, so a wrong icon colour names its resolved ARGB instead of being guessed from a screenshot. | diag |
| MACAPK_TRACE_WINDOW_FLAGS | off | any non-null value (some call sites also require it to equal "1") | Traces window-flag/style-attribute resolution across several call sites (Theme.applyStyle, resolveAttrRaw, AlertDialog, Button's default-style probe), naming which applied style answered a window* attribute. | diag |
| MACAPK_TRACE_XMLPULL | off | any non-null value | Dumps the XmlPullParser event stream (event type, name, depth) as a consumer's parse loop sees it. | diag |
| MACAPK_TW_TRACE | off | any non-null value | Logs every addTextChangedListener call on a TextView, naming the watcher's class. | diag |
| MACAPK_VIEW_THEME | ON since 2026-08-01: ContextThemeWrapper (and Activity) owns and applies its own theme cascade | "0" or "false" disables; anything else is on | The single gate for View-level theming across the whole corpus (android:theme on a layout element, Theme.setTo actually copying a cascade); with it on, getTheme() returns the wrapper's own theme instead of forwarding to the base context. =0 reverts to the old pass-through. | bisect |
| MACAPK_WINDOW_BOX | ON: the ANGLE render layer, SurfaceView renderer surface size, and renderer-direct touch are all confined to the app-window box | "0" or "false" disables; anything else is on | Bisect gate for the v1.0.996 window-box work; =0 restores the pre-change behaviour where GL surfaces get the full display drawable and the nav strip composites over the app's own bottom rows. | bisect |
| MACAPK_WINDOW_FLAGS_FROM_THEME | ON since v1.0.894: the app's real theme decides windowNoTitle/windowActionBar/etc. | 0/1 (only "0" disables) | Reads the theme (not just the manifest style name) for window* attributes, which is what lets AppCompat pick the decor action-bar screen layout it should; =0 opts out, a labelled bisect gate. | bisect |
| MACAPK_XML_DRAWABLE | ON: getDrawable() builds a real drawable for <layer-list>/<shape>/<selector> roots | 0/1 (only "0" disables) | Makes XML drawables other than <vector> actually paint, a strictly additive change; =0 disables it, reverting to the old empty-VectorDrawable-stub behaviour. | bisect |

**Note on MACAPK_ANDROID_ROOT** (appears in this table via the Install & storage agent, corrected here): its declaration comment at the source ("cli value > MACAPK_ANDROID_ROOT env > $HOME/Documents/android > ./android") is itself stale. The ACTUAL implementation, `ResolveAndroidRootDir()` in the source, resolves to `$HOME/Library/Caches/Mac.apk/scratch/android` when both the CLI flag and the env var are unset (moved off `~/Documents/android` on 2026-08-12; see the comment at the source for why -- an installed app never reaches this default at all, since its stub always passes an explicit `--android-root <bundle>/Contents/android`, so this default only matters for the bare dev `macapk-runtime --apk X` invocation). the source's comment agrees with the code; `config.h`'s does not. Trust the implementation over either comment.
## Rendering & GLES/Metal

| Flag | Default (unset) | Accepted values | Effect | Audience |
| --- | --- | --- | --- | --- |
| MACAPK_AGDK_TRACE | Off -- no log of GameActivity's OnGlobalLayoutListener registration/dispatch | `1` | Confirms which ViewTreeObserver instance receives GameActivity's global-layout listener and that it keeps firing, so a boot stall waiting on APP_CMD_CONTENT_RECT_CHANGED can be localized. | diag |
| MACAPK_ANGLE_BACKEND | Metal backend (or auto-detected Vulkan when the guest needs GLES 3.1) | `metal` or `vulkan` | Selects ANGLE's rendering backend; `vulkan` forces creation through eglGetPlatformDisplayEXT with EGL_PLATFORM_ANGLE_TYPE_VULKAN_ANGLE so RenderDragon-class engines get a real GLES 3.1 context via MoltenVK. | user |
| MACAPK_ANGLE_BLOB_CACHE | On -- ANGLE shader/program blob cache enabled when a cache dir resolves | `0` | Disables the ANGLE shader-compile blob cache entirely, so every shader recompiles from source each run instead of being served from disk. | user |
| MACAPK_ANGLE_BLOB_DIR | `<MACAPK_APP_CACHE_DIR>/.angle_blob` | a directory path | Overrides where the per-ANGLE-build shader blob cache is stored on disk. | user |
| MACAPK_ANGLE_BLOB_MAX_MB | 256 | an integer (MB) | Sets the disk-space budget for the ANGLE blob cache directory. | user |
| MACAPK_ANGLE_BLOB_TRACE | Off -- no per-blob cache log lines | `1` | Logs each individual shader-blob cache put/get. | diag |
| MACAPK_BG_NULL_CLEARS_COLOR | On -- `setBackground(null)` also clears the tracked background color | `0` | Reverts to the pre-v1.0.923 behavior where clearing a View's background drawable left its separate background-color field untouched. | bisect |
| MACAPK_BLOB_MADVISE | On -- fully-covered interior pages of a loaded Stage-6 blob get anonymous-remapped (discarded) | `0` | Falls back to a plain MADV_DONTNEED deactivation instead of replacing the blob's interior pages with anonymous PROT_READ memory after load. | bisect |
| MACAPK_CARD_CLIP | On -- a CardView's children are clipped to its rounded outline before dispatchDraw | `0` | Disables the save/clip-to-rounded-outline applied around a CardView's children. | bisect |
| MACAPK_CARD_GAP_PX | 16 (px, 8dp at density 2) | a non-negative integer (px) | Sets the inter-card gap used by the manual grid-layout fallback for RecyclerView grids, for A/B testing the spacing. | bisect |
| MACAPK_CHOREO_THREADDIAG | Off -- no thread-identity log for the Compose window recomposer's Choreographer | any value (presence check) | Confirms which thread a Compose AndroidUiFrameClock's Choreographer lives on and whether its postFrameCallback takes the drained main-vsync path or the dead per-thread timer path. | diag |
| MACAPK_CHOREO_TIME | Off, unless MACAPK_UHOOK is also set | any value (presence check) | Logs the frameTimeNanos delta Unity actually receives each frame, since Unity derives Time.deltaTime from it. | diag |
| MACAPK_CLIP_CHILDREN | On -- ViewGroup clips overflowing children to its own bounds in dispatchDraw | `0` | Disables the per-child clip so children positioned past a ViewGroup's edge (e.g. an overflowing RecyclerView card) are no longer bounded, restoring the pre-v1.0.129 behavior. | bisect |
| MACAPK_CLIP_OUTLINE | On -- `clipToOutline` + a round-rect provider clips the view during layout() | `0` | Reverts the outline-clip short-circuit so clipToOutline views (e.g. circular icon buttons) stop being clipped, painting a full square instead. | bisect |
| MACAPK_COMPOSE_FALLBACK | On -- the Compose text/tree overpaint fallback runs | `0` | Disables the ComposeViewFallback overpaint mechanism entirely. | bisect |
| MACAPK_DEBUG_DUMPFRAME | Off -- no frame dumps | a comma-separated list of frame numbers (or one number) | glReadPixels the frame at each listed frame number and writes a raw PPM to /tmp, for headless visual diagnosis when screen capture isn't available. | diag |
| MACAPK_DIRECTOR_OFF | On -- the per-frame Unity DirectorManager update tick runs | any value (presence check) | Disables the per-frame DirectorManager update driver for A/B isolation. | bisect |
| MACAPK_DIRECTOR_RUNNING_SCENE_OFF | 0x94 | a hex/decimal integer (byte offset) | Overrides the CCDirector field offset used to read the running-scene pointer for the scene-tree/touch diagnostics. | diag |
| MACAPK_DIRECTOR_VADDR | 0 -- feature inert, no guest memory touched | a hex/decimal guest vaddr | Supplies the per-build CCDirector instance address the scene-tree dumper and touch diagnostic walk from; deliberately has no baked-in default so it can never fire on the wrong binary. | diag |
| MACAPK_DISPATCH_TOP_INSET | 0 -- dispatched top inset is always 0 | `1` | Dispatches the real status-bar height as the WindowInsets top inset instead of always 0; measured a no-op for F-Droid since the AppBarLayout inset machinery doesn't consume it. | dev |
| MACAPK_DRAW_LETTER_SPACING | On -- letter-spaced text draws as separately-advanced glyph runs | `0` | Reverts to drawing text as one unspaced AWT run, letting a fleet delta be attributed to this change specifically. | bisect |
| MACAPK_DUMP_BITMAP_AT | Off -- no PNG dump | a frame number, or `0` for every frame | Writes the current canvas bitmap to `/tmp/macapk_frame_<N>.png` (plus a downscaled `_chat.png` companion) for visual verification. | diag |
| MACAPK_DUMP_CL_WIDGETS | Off -- no widget-graph dump | any value (presence check) | Dumps every ConstraintLayout's solved widget graph (position/size/constraint targets) at the checkpoint dump frames. | diag |
| MACAPK_DUMP_MOTION | Off -- no MotionLayout dump | any value (presence check) | At frame 60, reports each MotionLayout ConstraintSet's constraint-map size to show whether ConstraintSet.readFallback populated it. | diag |
| MACAPK_DUMP_SLOTS | Off -- no Compose SlotTable/navstack verbose dump | `1` | Decodes and dumps Compose composition SlotTables (per-group class names) plus the Reddit navstack entry-visibility probe. | diag |
| MACAPK_DUMP_TREE_EVERY | View-tree dump only at the fixed checkpoint frames (1/60/300) | an integer N (frames) | Also dumps the full view tree every N frames, for inspecting screens reached long after boot (e.g. after a tap navigates deeper). | diag |
| MACAPK_EDGE_TO_EDGE | Off -- legacy SYSTEM_UI_FLAG edge-to-edge bits are never synthesized | `1` | Synthesizes SYSTEM_UI_FLAG_LAYOUT_HIDE_NAVIGATION/FULLSCREEN on the legacy systemUiVisibility path; kept opt-in because enabling it by default previously measured a catastrophic regression. | dev |
| MACAPK_EGL_LAYER_IDLE_HIDE | Off -- an idle guest EGL layer is still re-asserted visible | `1` | Hides the guest's native EGL layer instead of forcing it visible when it hasn't presented a drawable recently, so it stops occluding the framework canvas underneath. | bisect |
| MACAPK_EGL_LAYER_IDLE_SECS | 2.0 (clamped 0.2-30.0) | a float (seconds) | Sets how long the guest EGL layer must be idle before MACAPK_EGL_LAYER_IDLE_HIDE hides it. | dev |
| MACAPK_FAIR_SWAPPY_LATCH | On -- the mutex-fairness boot window latches closed once Mac.apk becomes the guest's Swappy pacer | `0` | Reverts to the pure wall-clock 90s fairness boot window instead of closing it early the moment Swappy-shaped pacing goes live. | bisect |
| MACAPK_FILTER_BITMAP | On -- bitmap blits use bilinear filtering when Paint.isFilterBitmap() | `0` | Restores nearest-neighbour filtering everywhere so the fleet effect of bilinear filtering can be A/B'd. | bisect |
| MACAPK_FONT_REAL_STYLE | On -- real per-style face lookup is used | `0` | Falls back to serving every text request from one bundled Roboto-Regular face with AWT's algorithmic bold/italic, while still keeping family lookup for Typeface weight. | bisect |
| MACAPK_FORCE_REMEASURE | Off -- no forced remeasure | `1` | Forces every attached AndroidComposeView's root LayoutNode to re-measure+re-place each frame, confirming whether a stalled Compose animation is a missed requestRemeasure. | diag |
| MACAPK_FORCE_REMEASURE_MODE | Only the root LayoutNode is marked dirty | `all` | Marks every LayoutNode in the subtree dirty instead of just the root, so deep animating nodes aren't skipped. | diag |
| MACAPK_FRAMEWORK_PRESENT_WHEN_GUEST_IDLE | Off -- the framework canvas present stays skipped once any guest EGL surface goes live | `1` | Resumes presenting the framework's own canvas when the guest's EGL surface is live but has stopped actually producing frames, so an app whose real UI is drawn by ViewCanvasDriver (not the guest engine) isn't left blank. | dev |
| MACAPK_FRAME_DT | Off -- no per-frame dt trace | any value (presence check) | Logs any frame whose delta exceeds a threshold (a dropped-frame hitch) plus a 5s worst-dt summary, since the 5s-averaged fps line hides single-frame hitches. | diag |
| MACAPK_FRAME_DT_MS | 20 (ms) | a float (ms) | Sets the per-frame delta threshold that counts as a "hitch" for MACAPK_FRAME_DT. | diag |
| MACAPK_FRAME_INTERVAL | 0 -- the auto-swap-interval ratchet decides | an integer 1-8 | Pins the Swappy-shaped swap interval (1=locked 60fps, 2=locked 30fps) and disables the ratchet, isolating "the grid quantizer works" from "the ratchet picked the right interval". | diag |
| MACAPK_FRAME_PACING | On -- the Swappy-shaped frame-pacing grid wait is active | `0` | Disables the frame-pacing grid wait outright, which otherwise blocks eglSwapBuffers until the pacing grid allows the next frame. | bisect |
| MACAPK_FRAME_PHASE_TRACE | Off -- no per-phase timing trace | `1` | Traces measure/layout/draw phase timings within a single traversal, at zero cost when unset. | diag |
| MACAPK_FRAME_SCORE_EVERY | 120 | an integer (0-999999) | Sets the frame cadence at which the self-capture harness scores the presented frame. | diag |
| MACAPK_FRAME_STATS | Off -- no guest-present pacing probe | `1` | Logs the guest's real presents-per-second (and worst/min frame delta) at the one shim every guest-owned EGL present passes through, to diagnose an engine running proportionally too fast. | diag |
| MACAPK_GLES1_CONTEXT | Decided by the runtime's own scan of the guest's imported GLES1 symbols | `0` or `1` | Overrides the auto-detected choice between an ES1 and ES2 EGL context, which are mutually exclusive. | dev |
| MACAPK_GLTHREAD_DRAIN | On -- a dead GLSurfaceView render thread's queued Runnables are still drained on the main thread | `0` | Reverts the drain-on-death behavior for an A/B. | bisect |
| MACAPK_GL_EMPTY_EXT | Off -- glGetString(GL_EXTENSIONS) returns ANGLE's real extension string | `1` | Restores the pre-v682 behavior of reporting an empty GL_EXTENSIONS string, to test whether an engine's extension-driven feature probing caused a regression. | diag |
| MACAPK_GL_OFF | Every v682 GLES bridge group is active | a comma-separated list of group names (state/stencil/fbo/del/uniform/tex/query) | Makes the named GLES bridge groups behave like their pre-v682 empty-body stubs, so a rendering regression can be bisected at runtime without a rebuild. | bisect |
| MACAPK_GL_RENDERER_THROW_FATAL | Off -- a renderer that threw is still called again next frame | `1` | Adopts AOSP's "GLThread is dead" semantics: after a renderer's first uncaught throw, it is marked dead and never driven again. | dev |
| MACAPK_GROW_FITS_APPBAR | On -- a fitsSystemWindows AppBar in a CoordinatorLayout grows by the status-bar height | `0` | Reverts the AppBar-growth fixup, leaving the appbar one status-bar-height shorter than the emulator reference. | bisect |
| MACAPK_HIT_TRACE | Off -- no touch hit-dispatch trace | any value (presence check) | Gates a diagnostic trace of ViewGroup's touch hit-testing/dispatch decisions. | diag |
| MACAPK_INSETS_DISPATCH_LEGACY | The legacy-vs-modern insets dispatch choice is computed per app from its compat target SDK | `1` | Forces the legacy (pre-fix) insets-dispatch answer for every app in one run, so a regression can be A/B'd without a bisect. | bisect |
| MACAPK_ISSHOWN_DETACHED | On -- isShown()/getLocationInWindow()/getWindowVisibility()/transformFromViewToWindowSpace answer AOSP's real detached-tree contract | `0` | Restores the pre-fix answers (e.g. "top of the View chain is always shown") for a one-run A/B, covering the known cost that overlay roots aren't marked attached. | bisect |
| MACAPK_KEEP_ANGLE_LAYER | Off -- the framework's own ANGLE layer is hidden once a guest EGL window surface goes live | `1` | Keeps the framework angleLayer visible even after a guest EGL surface goes live, for apps whose real UI is drawn through ViewCanvasDriver rather than by the guest engine itself. | bisect |
| MACAPK_KEEP_HACKS | Every hack in the built-in default-retired set stays retired | a comma-separated list of hack names | Re-enables a named, normally-default-retired geometry hack, as a rollback lever. | bisect |
| MACAPK_KEEP_NAV_CENTER | Off -- the nav-item icon+label centering fixup is not applied | `1` (also needs MACAPK_REAL_LOOPER) | Restores the old nav-item content-centering fixup as a bisect gate against Material's own (correct) centering. | bisect |
| MACAPK_LAYER_REASSERT | Off -- the periodic native-EGL-layer reassert timer doesn't install | any value (presence check) | Periodically re-commits the guest EGL layer's composited/parented state on the main thread; measured to NOT fix the 54% present-thread stall it was written for, so it stays opt-in. | diag |
| MACAPK_LAYER_VIS_PROBE | Off -- no visible border drawn | any value (presence check) | Draws a thick magenta border on the native EGL layer so you can visually confirm whether it is on-screen and composited. | diag |
| MACAPK_LEGACY_ANIM | On -- a VISIBLE-only-or-still-animating child keeps being drawn and pumped in dispatchDraw | `0` | Disables the legacy-Animation draw/pump entirely, collapsing dispatchDraw's child test to visibility-only (a labelled bisect gate, not a supported mode). | bisect |
| MACAPK_M6_FORCE_DRAW | Off -- the M6 idle-perf dirty-traversal skip stays active | `1` | Forces a full traversal+draw every frame, bypassing the dirty-gate skip, to test whether an animating Compose subtree simply never schedules a traversal. | diag |
| MACAPK_MATERIAL_CHROME | On -- plain Views get a painted Material-chrome background approximation | `0` | Disables the synthetic Material-chrome background paint for plain (non-Material) Views. | bisect |
| MACAPK_MVP_SHIFT_X | 0.0 | a float | Applies a debug X shift to the MVP matrix on a cocos2d-x v2 glUniformMatrix4fv call; kept as a diagnostic knob, not needed since the lifter's VCMP/FPSCR fix. | diag |
| MACAPK_MVP_SHIFT_Y | 0.0 | a float | Same as MACAPK_MVP_SHIFT_X for the Y axis. | diag |
| MACAPK_NAVSTACK_FORCE | Off -- navstack Boolean MutableStates keep their real composed value | `1` | Confirmation hack: forces every navstack-related Boolean MutableState (and the navstack host's own booleans) to TRUE, to test whether a visibility-latch gate is blocking Reddit's welcome screen. | diag |
| MACAPK_NAVSTACK_HOST_PROBE | Off -- no one-shot host dump | `1` | Finds the navstack host by package and dumps its live boolean methods, State fields and entry list, to show which input to the visible-entry gate is empty. | diag |
| MACAPK_NAV_MEASURE_CLAMP | On -- a wrap_content NavigationBarView is clamped to the standard nav-bar height | `0` (also needs MACAPK_REAL_LOOPER) | Disables the clamp, letting a wrap_content bottom-nav bar fill its whole available height and collapse a RelativeLayout sibling to zero height. | bisect |
| MACAPK_NO_HEARTBEAT_LAYOUT | Off -- the periodic M6 heartbeat still forces a full re-layout | `1` | Drops the periodic heartbeat's full re-layout, keeping it as a draw-only refresh, to test whether that re-layout cancels an in-flight Material bottom-sheet animation. | diag |
| MACAPK_NO_OFFMAIN_EGL_LAYER | Off -- an off-main-thread native-EGL-layer request is bounced to the main thread | any value (presence check) | Restores the old behavior of failing outright (returning null) instead of hopping to the main thread, an A/B lever for the on-demand EGL layer path. | bisect |
| MACAPK_NO_PRESENT_CAP | Present pacing follows the decouple-active/PRESENT_CAP auto logic | any value (presence check) | Instant escape hatch that force-disables the present pacer regardless of the decouple ring's state. | bisect |
| MACAPK_OVERLAY_DIAG | Off -- no rects dumped | `1` | Fire-once dump of the exact overlay-stack layer and Button rects presentForGl computes, so a click target can be derived precisely. | diag |
| MACAPK_OWNER_TRACE | Off -- no trace | any value (presence check) | Gates a diagnostic trace related to View's androidx view-tree owner-family resolution/fallback path. | diag |
| MACAPK_PAINT_AA_DEFAULT | Off -- `new Paint()` has no flags set, matching AOSP (no antialiasing by default) | `1` | Restores the pre-v1.0.929 behavior of antialiasing every bare `new Paint()` by default, as a labelled bisect gate since it changes edge treatment fleet-wide. | bisect |
| MACAPK_PRESENT_CAP | Present pacing follows the decouple-active auto logic (K defaults to 2 only if forced) | an integer 1-3 (K), or just being set | Forces the old opt-in wall-clock present-credit pacer on with pool size K, on any Vulkan (MoltenVK) run. | bisect |
| MACAPK_PRESENT_CAP_TIMEOUT_MS | 34 (ms) | an integer 1-1000 | Sets the present-pacer's credit-wait timeout, so a missed drive tick degrades to unthrottled rather than hanging. | dev |
| MACAPK_PRESENT_CAP_WITH_RING | The wall-clock PresentPacer stands down once the decouple ring already applies backpressure | any value (presence check) | Restores the old stacked (pacer + ring) behaviour for a one-run A/B, which otherwise double-pays the backpressure wait inside the guest's render-mutex critical section. | bisect |
| MACAPK_PROBE_SHEET | Off -- no live sheet-state dump | `1` | Finds any BottomSheetState/SwipeableState reachable from the tree and logs its live offset/currentValue/targetValue/anchors each tick. | diag |
| MACAPK_PUMP_AFTER_DRIVE | Off -- no extra pump call | `1` | EXPERIMENT: pumps the Compose global snapshot again after the drive step, in addition to the normal per-frame pump. | experiment |
| MACAPK_PUNCH_PROPAGATE | On -- Canvas.punchHole propagates through an interposing RenderNode to the frame canvas | `0` | Keeps a punch-hole local to whatever canvas it was issued on, restoring the pre-v1.0.997 behavior where the hole never reached the frame if a RenderNode interposed. | bisect |
| MACAPK_PUNCH_TRACE | Off -- no punch-application log | `1` | Logs the first 16 punch-hole applications with the canvas they landed on and the device rect, tracing propagation from a SurfaceView hole up to the frame canvas. | diag |
| MACAPK_REAL_CLIP | On -- public Canvas.clipRect actually clips | `0` | Reverts clipRect to a no-op stub for emergency rollback if the real clip introduces a regression. | bisect |
| MACAPK_RENDERNODE_CHILD_INVALIDATE | On -- a child RenderNode invalidation makes the parent report no display list, forcing a re-record | `0` | Restores the old snapshot-replay behavior for a one-run A/B. | bisect |
| MACAPK_RENDERNODE_STATS | Off -- no aggregate dump | any value (presence check) | Prints one-line aggregate record/replay tallies (records/empty/tiny/replays/clipped/ok), separating "Compose recorded no draws" from "it recorded but never replayed". | diag |
| MACAPK_RENDERNODE_TRACE | Off -- no per-recording trace | any value (presence check) | Prints a 30-frame stack per beginRecording plus the blit-chain (which recording bitmap belongs to which node), and gates a primitive draw-op counter in Canvas. | diag |
| MACAPK_RETIRE_HACKS | Only the built-in default-retired hack set is off | a comma-separated list of hack names, or `all` | Force-retires additional named geometry hacks (or all of them) for testing. | bisect |
| MACAPK_RIPPLE_PRESS | Off -- RippleDrawable paints only its content layers | `1` | Adds a separate AOSP-style pressed-ripple background fill (shaped by the mask) on top of View's own existing press-ripple, so both can be compared before picking one. | dev |
| MACAPK_RTL_RESOLVE | On -- a grid cell's child tree is RTL-resolved before ConstraintLayout measures it | `0` | Disables the RTL pre-resolution pass, which otherwise leaves every start/end constraint unset and the CL solver zeros the whole subtree. | bisect |
| MACAPK_RV_NATURAL | On -- RecyclerView is left to its own LayoutManager | `0` | Restores the old v123 force-bind/layoutGridManually workaround instead of the natural AOSP RV dispatch flow. | bisect |
| MACAPK_RV_VISIBLE_INDEX | 0 | a non-negative integer | Selects which single page is VISIBLE (rest GONE) on a RecyclerView that looks like a tab-pager, for previewing tabs without wiring real touch dispatch. | dev |
| MACAPK_SAVE_CLIP | On -- Canvas.save()/restore() snapshot the clip alongside the transform | `0` | Reverts to saving only the transform (not the clip) as an emergency rollback. | bisect |
| MACAPK_SCENE_TREE | Off -- the cocos2d-x v2 scene-graph dumper never runs | any non-`0` value | Recursively walks and prints the running scene's CCNode tree (class, position, size, anchor, z-order) from the resolved CCDirector instance. | diag |
| MACAPK_SCENE_TREE_ANCHOR_OFF | 0x5c | a hex/decimal integer (byte offset) | Overrides the CCNode anchor-point field offset the scene-tree dumper reads. | diag |
| MACAPK_SCENE_TREE_AT | Dumps on a periodic cadence (every ~300 glClear calls, up to 12 times) | an integer glClear-call count | Fires the scene-tree dump exactly once at the given glClear counter instead of periodically. | diag |
| MACAPK_SCENE_TREE_CHILDREN_OFF | 0x4c | a hex/decimal integer (byte offset) | Overrides the CCNode children-array field offset the scene-tree dumper reads. | diag |
| MACAPK_SCENE_TREE_CONTENTSIZE_OFF | 0x94 | a hex/decimal integer (byte offset) | Overrides the CCNode contentSize field offset the scene-tree dumper reads. | diag |
| MACAPK_SCENE_TREE_DEPTH | 10 (clamped 1-16) | an integer | Sets how many levels deep the scene-tree dumper recurses. | diag |
| MACAPK_SCENE_TREE_POSITION_OFF | 0x28 | a hex/decimal integer (byte offset) | Overrides the CCNode position field offset the scene-tree dumper reads. | diag |
| MACAPK_SCENE_TREE_VISIBLE_OFF | 0x58 | a hex/decimal integer (byte offset) | Overrides the CCNode visible-flag field offset the scene-tree dumper reads. | diag |
| MACAPK_SCENE_TREE_ZORDER_OFF | 0x14 | a hex/decimal integer (byte offset) | Overrides the CCNode z-order field offset the scene-tree dumper reads. | diag |
| MACAPK_SCROLL_DRAW_ONLY | On -- a scroll/animation-only frame redraws without a full remeasure/relayout | `0` | Forces every traversal to be a full measure+layout+draw (the pre-M6-split behavior), for A/B against the draw-only scroll-perf optimization. | bisect |
| MACAPK_SIMPLE_PATH_REGION | On -- a same-direction addRect-only even-odd path draws as a Region (fill type discarded) | `0` | Always honours the path's real fill type instead of taking the Region fast path, for A/B against the two rows it fixes. | bisect |
| MACAPK_SKIA_FILL_RULE | Off -- Java2D's native span/fill rule is used | `1` | Applies a small sub-pixel geometry offset so Java2D's fill-span rule matches Skia's; ships off pending a fuller sweep + eyes-on verification before flipping the default. | dev |
| MACAPK_SKIP_DRAW | Every child draws normally in dispatchDraw | a comma-separated list of simple class names | Skips drawing children whose simple class name is in the list, to isolate which widget is responsible for an artifact. | diag |
| MACAPK_SNAPSHOT_CLASS | The AOSP-shaped Snapshot.sendApplyNotifications auto-resolve pump runs instead | a fully-qualified class name | Manually overrides which class's static no-arg method is reflectively pumped each frame as the Compose global-snapshot flush, for a fully-stripped build where auto-resolve fails. | diag |
| MACAPK_SNAPSHOT_METHOD | `a` | a method name | Names the method MACAPK_SNAPSHOT_CLASS's pump calls; only consulted when MACAPK_SNAPSHOT_CLASS is set. | diag |
| MACAPK_STUB_UNITY_ONLOAD | Off -- dlsym("JNI_OnLoad") on libunity returns the real implementation | `1` | Intercepts dlsym(JNI_OnLoad) and returns a stub reporting success, working around a crash deep in libunity's real JNI_OnLoad allocator chain; a diagnostic patch pending a real fix. | diag |
| MACAPK_SWAPPY_PIN | On -- the Swappy-shaped auto-swap-interval is pinned; only the frame counter advances | `0` | Disables the interval pin (pure +1 frame-counter pacing) for diagnosis. | bisect |
| MACAPK_TEXT_FLOAT_METRICS | On -- ascent/descent/line-spacing metrics are floats | `0` | Restores the integer-truncated metrics path; the blast radius is every TextView in the fleet since it feeds getFontMetrics/getFontSpacing/Layout line geometry. | bisect |
| MACAPK_TEXT_FRACTIONAL_METRICS | On -- AWT's FRACTIONALMETRICS hint is used for text | `0` | Restores AWT's integer (non-fractional) text metrics mode. | bisect |
| MACAPK_TEXT_GLYPH_QUANTIZE | On -- glyph advances snap to cumulative integer-quantized positions | `0` | Restores v1.0.935's whole-string float-sum glyph advance instead of per-glyph quantization. | bisect |
| MACAPK_TRACE_BARRIER | Off -- no barrier dump | `1` | Dumps layout barriers alongside the checkpoint-frame view-tree dump. | diag |
| MACAPK_TRACE_BGTINT | Off -- no log | `1` | Logs a short stack trace every time setBackgroundTintList is called, naming the caller that overrides a view's intended solid-color background. | diag |
| MACAPK_TRACE_CHROME | Off -- no log | `1` | One-shot per-class log of which Material widgets actually reach the synthetic Material-chrome paint path. | diag |
| MACAPK_TRACE_COMPOSE | Off -- no log | `1` | Logs a Compose host's bounds and child count just before its dispatchDraw runs, plus Compose-related color/text tracing in Paint and Layout. | diag |
| MACAPK_TRACE_COMPOSE_FALLBACK | Off -- no log | `1` (or any value in BitmapFactory) | Logs per-frame which ComposeViews the fallback walked and how much text it harvested, plus a matching decodeResource outcome log. | diag |
| MACAPK_TRACE_COMPOSE_INSERT | Off -- no trace | any value (presence check) | Traces requestLayout()/invalidate()/attach events on AndroidComposeView hosts, distinguishing "composer never emitted an insert" from "insert reached the tree but measure/layout dropped it". | diag |
| MACAPK_TRACE_DECODERES | Off -- no log | `1` | Logs every BitmapFactory.decodeResource call and its outcome (null vs decoded size). | diag |
| MACAPK_TRACE_EDITMODE | Off -- no log | any value (presence check) | Logs every isInEditMode() call, confirming whether ConstraintLayout's constraint-wiring pass ran for a given instance. | diag |
| MACAPK_TRACE_FULLFILL | Off -- no log | any value (presence check) | Logs any fill covering >=60% of the destination together with a readback of the pixel it just wrote, distinguishing a wrong value, a lost write, and a landed-but-later-overwritten fill. | diag |
| MACAPK_TRACE_GAP_RELAYOUT | Off -- no log | `1` | Logs when a gap between visible children's summed heights triggers the re-equal-divide relayout fallback. | diag |
| MACAPK_TRACE_GL_EXT | Off -- GL_EXTENSIONS is never dumped | any value (presence check) | Dumps the EGL context's advertised GL_EXTENSIONS string, to tell whether a missing extension is absent from the ANGLE backend or just not exposed on this context. | diag |
| MACAPK_TRACE_IME | Off -- no log | `1` | Logs IME/focus/keystroke routing across TextView, View, InputMethodManager and TouchDispatcher, to confirm whether a keystroke reaches any text target at all. | diag |
| MACAPK_TRACE_INSETS | Off -- no log | `1` (or any value, depending on call site) | Logs window-insets dispatch calls (setDecorFitsSystemWindows, setSystemUiVisibility, dispatchApplyWindowInsets, root-inset callers). | diag |
| MACAPK_TRACE_LAYOUT | Off -- no log | a comma-separated list of simpleName substrings | Prints the final bounds a parent assigns to every view whose class matches, for chasing a layout-position (not layout-padding) bug. | diag |
| MACAPK_TRACE_MEASURE | Off -- no log | any value (presence check) | Traces which classes the measure() recursion reaches and with what MeasureSpecs. | diag |
| MACAPK_TRACE_NAV | Off -- no log | `1` | Traces BaselineLayout bounds and nav-item icon/label centering computation at fixed checkpoint frames. | diag |
| MACAPK_TRACE_OFFSET | Off -- no log | a simpleClassName substring | Logs who calls offsetLeftAndRight/offsetTopAndBottom on a matching view, and by how much, with a caller stack trace. | diag |
| MACAPK_TRACE_RIPPLE | Off -- no log | `1` | Logs a ripple-effect's start position, max radius and color. | diag |
| MACAPK_TRACE_RV | Off -- no log | `1` | Logs when a forced RecyclerView child's measure() call throws. | diag |
| MACAPK_TRACE_RV_LAYOUT | Off -- no log | `1` | Logs when the natural-layout cascade re-fires layout on a RecyclerView child, confirming RV.onLayout is being driven. | diag |
| MACAPK_TRACE_SETBG | Off -- no log | any value (presence check) | Logs a stack trace whenever setBackground(null) is called on a Button, since that path clears both the drawable and the tracked background color. | diag |
| MACAPK_TRACE_SHADOW | Off -- no log | `1` | One-shot-per-class log of the M12 elevation-shadow radius computed for a view. | diag |
| MACAPK_TRACE_SHAPEFILL | Off -- no log | `1` | Logs which of a GradientDrawable's solid fill color vs. resolved tint wins, and with what value. | diag |
| MACAPK_TRACE_SURFACE_DISPATCH | Off -- no log | any value (presence check) | Logs which arm decided a SurfaceView callback dispatch, but only on a state transition (never per-frame, so the trace itself doesn't perturb the timing-sensitive race it diagnoses). | diag |
| MACAPK_TRACE_VECTORDRAW | Off -- no log | `1` | Logs a VectorDrawable's draw-time bounds/viewport/monochrome test (producer side) and the RenderNode/Canvas blit-chain consumer trace, to see if a rasterized vector ever reaches a real surface. | diag |
| MACAPK_UHOOK | Off -- no native diagnostic hooks installed | any value (presence check) | Installs a family of native Unity/il2cpp diagnostic hooks (worker thread-name resolution, null-MethodInfo invoke logging, GetCurrentJobHandle checks, PlayableGraph::Evaluate counting) plus the matching Choreographer Unity-render-init trace. | diag |
| MACAPK_UNITY_DRIVE_HZ | 0 -- falls back to the measured panel refresh rate | a float 1.0-480.0 | Force-overrides the Hz used to drive Unity's per-frame Update loop, for diagnosis (must match what the guest is told or game speed breaks). | diag |
| MACAPK_UNITY_NOABS_OFF | Off -- the libunity GetResolvedPath absolutize-branch NOP patch is applied | any value (presence check, kill-switch) | Disables the signature-anchored patch that keeps libunity's asset-path resolver relative (matching the data.unity3d archive VFS mount), which otherwise fails engine init. | bisect |
| MACAPK_UNITY_PRESENT | Off -- Unity's own eglSwapBuffers fallback is relied on | `1` | Installs present-injection that swaps Unity's window surface on unbind and on a steady-state frame boundary, for engines where Unity binds+renders+unbinds without ever calling eglSwapBuffers itself. | dev |
| MACAPK_UNITY_RESERVE_STRICT | Off -- a huge libunity arena reserve with no low-address hole is still handed a high address | `1` | Refuses (returns MAP_FAILED) instead of handing back a high address above the 16 GiB ceiling the guest cannot represent, to measure whether the guest degrades gracefully. | dev |
| MACAPK_UNITY_SWAPPY_PRESENT_OFF | Off -- the libunity Swappy-present branch is patched unconditional | any value (presence check, kill-switch) | Disables the patch that rewrites libunity's conditional Swappy-present branch to always take the direct eglSwapBuffers path, which otherwise jumps through a NULL Swappy function pointer and hangs the render thread. | bisect |
| MACAPK_VIDEO_SINK_TRACE | Off -- no periodic dump | `1` | Periodically dumps the video sink's player subtree (visibility/alpha/bounds/text of every ancestor/sibling/descendant), separating a still-visible app shutter from a stale-repaint bug. | diag |
| MACAPK_VSYNC_DRIVE_CB | Off -- a Unity native-vsync pipe fd is not fed from the owner thread's poll loop | any value (presence check) | Writes a vsync event to a Unity native-vsync pipe so the owner thread's poll loop drives Unity's registered FD callback on that thread, for A/B during bring-up of looper-orphaned vsync fds. | bisect |
| MACAPK_VSYNC_MATCH_PANEL | On -- MTKView's preferredFramesPerSecond and the drive cadence use the screen's measured refresh rate | `0` | Restores the old unconditional 120Hz; measured neutral (not a frame-pacing fix), kept as defence for a panel whose refresh isn't a multiple of 60Hz. | bisect |
## Audio & media

| Flag | Default (unset) | Accepted values | Effect | Audience |
| --- | --- | --- | --- | --- |
| MACAPK_AMEDIA_SELFTEST | self-test does not run | any value (presence-only check) | When set, drives the NDK AMedia* C API (AMediaExtractor + AMediaCodec) through the shipped bionic shim exactly as an NDK decode loop would, decoding MACAPK_AMEDIA_SELFTEST_FILE and logging the resulting frame count as PASS (>0 frames) or FAIL. | diag |
| MACAPK_AMEDIA_SELFTEST_FILE | falls back to /tmp/mdtest.m4a | path to an .m4a media file | Selects the media file MACAPK_AMEDIA_SELFTEST decodes; only read when that self-test is enabled. | diag |
| MACAPK_AUDIOTRACK_TIMESTRETCH | time-stretch splicing enabled | 0 disables; unset/other enables | Default-on bisect gate for AudioTrack.setPlaybackParams's time stretch: while enabled a non-1.0 speed splices an AVAudioUnitTimePitch node into the live AVAudioEngine graph, and =0 instead rejects a non-1.0 speed with BAD_VALUE rather than silently ignoring it. | bisect |
| MACAPK_AUDIO_DUMP | no WAV tap installed | path to a .wav output file | Installs a tap on the AVAudioEngine mixer's output bus and writes the exact mixed PCM the device receives as 16-bit WAV, so a reviewer can verify non-silent audio actually reached the output. | diag |
| MACAPK_AUDIO_PROBE | probe disabled, zero cost | 1 enables | Aggregates a machine-readable "is sound actually playing" signal across all voices (render calls with a real peak, plus the window peak) and prints it about every 5 seconds; separate from the far chattier per-voice MACAPK_AUDIO_TRACE. | diag |
| MACAPK_AUDIO_RMS | RMS measurement skipped | any value (presence-only check) | In the AAudio feeder callback, measures peak/RMS of the guest mixer's (FMOD's) own output buffer to bisect whether inaudible music is FMOD producing silence (RMS~=0) or a downstream loss further in audio_backend.mm (RMS>0). | bisect |
| MACAPK_AUDIO_TRACE | tracing off | any value (presence-only check) | Enables permanent, verbose audio-subsystem tracing, both in Java (AudioTrace's per-tag hit counts for AudioTrack/SoundPool/MediaPlayer) and natively (OpenSL/AAudio dlsym and IID-resolution logging), mirroring the MACAPK_NET_TRACE convention. | dev |
| MACAPK_CAMERA_TRACE | camera tracing off | any value (presence-only check) | Turns on the whole camera2 subsystem's narration, in both the Java layer (CameraBackend/CameraCaptureSessionImpl, "[camera2]") and the native AVFoundation layer (camera_capture.mm, "[camera]"), so the two interleave in one log. | dev |
| MACAPK_CAM_VIDEO_RANGE | full-range '420f' capture format | 1 selects '420v'; unset/other = '420f' | Selects the AVCaptureVideoDataOutput pixel range for camera capture: default full-range '420f' matches the Android/JFIF YUV convention apps assume, while =1 requests limited-range '420v' for a one-run A/B if a preview looks washed out. | bisect |
| MACAPK_FMOD_AAUDIO_STUB | real AAudio callback path stays active | any value (presence-only check) | Forces the AAudio output back to the old inert no-op stub (fixed sample_rate/channels/frames_per_burst, AAUDIO_OK on everything) instead of the real driven callback, as a one-run bisect point if a FMOD title starts crashing under real audio. | bisect |
| MACAPK_FMOD_AUDIO | AAudio callback output enabled (unless MACAPK_FMOD_AAUDIO_STUB is also set) | 0 disables; other/unset enables | Force-overrides in either direction whether the AAudio callback-mode output (FMOD/Unity built-in audio) is driven at all, on top of the automatic default. | bisect |
| MACAPK_FMOD_MUTE | FMOD audio unmuted (plays) | nonzero/non-empty enables mute; unset/0 = unmuted | Restores the historical mute of FMOD's createSound/createStream (patched to return FMOD_ERR_FILE_NOTFOUND) that used to be the default before the v1.0.1111 flip turned FMOD audio on by default. | bisect |
| MACAPK_FMOD_REAL | only per-image-scanned FMOD imports route to real libfmod | any value (presence-only check) | Manual override that forces every FMOD-shaped symbol on every loaded image to resolve directly to real libfmod, bypassing the stub table, regardless of the automatic per-image coverage scan; kept as an escape hatch after that scan made per-app curation unnecessary. | experiment |
| MACAPK_FMOD_SITEC | old site-C JNI patch skipped | any value (presence-only check) | Restores the old per-image "site C" binary patch to libfmod's JNI variadic-argument handling for an A/B; superseded by the generic Bionic va_list JNI bridge fix. | bisect |
| MACAPK_FMOD_STUB | FMOD resolves to real libfmod unconditionally | any value (presence-only check) | Restores the old coverage-gated behaviour (route to real libfmod only when an image's FMOD imports exceed the stub table) for an A/B, since a complete set of silent stubs passes coverage without producing audio. | bisect |
| MACAPK_MEDIA_SELFTEST_FILE | falls back to /tmp/mdtest.m4a | path to an .m4a media file | Selects the input file MediaSelfTest decodes end-to-end (MediaExtractor -> MediaCodec -> NativeMedia -> AudioToolbox) when its own MACAPK_MEDIA_SELFTEST gate is on. | diag |
| MACAPK_MIC | mic capture enabled (subject to normal TCC prompt) | 0 disables capture; unset/other = enabled | MACAPK_MIC=0 hard-disables microphone capture so opening it always fails, for an environment where opening the mic is undesirable or lacks NSMicrophoneUsageDescription. | dev |
| MACAPK_MIC_DUMP | no WAV dump written | path to a .wav output file | Writes the converted captured microphone PCM, exactly what the app reads after sample-rate conversion, to a 16-bit WAV file, the microphone analog of MACAPK_AUDIO_DUMP. | diag |
| MACAPK_MIC_PERMISSION_TIMEOUT_MS | 15000 ms timeout | a positive integer (milliseconds) | Bounds how long the first AudioRecord open blocks waiting for the user to answer the system microphone permission prompt; on timeout the open fails instead of hanging, mirroring Android's STATE_UNINITIALIZED for an ungranted RECORD_AUDIO. | dev |
| MACAPK_MIC_TRACE | no lifecycle/level-meter logging | any value (presence-only check) | Enables per-microphone-stream lifecycle logging plus a roughly 2-second level meter (peak reading), the cheap machine-readable answer to whether real audio is arriving. | dev |
| MACAPK_OPENAL_STUB | OpenAL opens a real device (audio on) | any value (presence-only check) | Restores OpenAL Soft's old NULL-device (silent) alcOpenDevice behaviour, kept in case a future OpenAL build regresses against the now-default real OpenSL ES/AAudio -> CoreAudio routing. | bisect |
| MACAPK_SENSOR_ZERO_MOTION | motion sensors report a synthesized 1g-at-rest pose | exactly "1" | Forces the ACCELEROMETER/GRAVITY/LINEAR_ACCELERATION/ROTATION_VECTOR family back to literal (0,0,0) instead of the default synthesized gravity-at-rest pose, as a one-env-var bisect for whether an app's layout is driven by the accelerometer. | bisect |
| MACAPK_VIDEO_IMAGE_SELFTEST_FILE | falls back to /tmp/goi_test.h264 | path to a raw Annex-B .h264 file | Selects the raw Annex-B H.264 elementary stream VideoImageSelfTest feeds one access unit at a time into MediaCodec.getOutputImage(), exercising the real public buffer-index protocol. | diag |
| MACAPK_VIDEO_IMAGE_SELFTEST_OUT | falls back to /tmp/goi_mine.png | path to a .png output file | Where VideoImageSelfTest writes its last decoded frame, reconstructed from getOutputImage()'s own planes back to RGB, for visual inspection. | diag |
| MACAPK_VIDEO_IMAGE_SELFTEST_REF | falls back to /tmp/goi_ref.png; if absent, only the motion check runs | path to a reference .png file | Optional reference frame decoded independently by ffmpeg (not VideoToolbox) that VideoImageSelfTest compares its own derived-YUV output against, instead of only checking that decode does not crash. | diag |
| MACAPK_VIDEO_SELFTEST | self-test layer never created, zero impact | path to a video file | Creates an on-screen CAMetalLayer and loops the given clip through the real decode -> CVPixelBuffer -> MTLTexture -> present pipeline, proving that chain visually since no movie-texture app is in the protected sweep. | diag |
| MACAPK_VIDEO_TOOLBOX_SELFTEST | self-test tick never runs | path to a raw Annex-B elementary stream | Decodes one more NAL each drawn frame (looping) through the real MediaCodec-facing VideoToolbox pipeline and presents it on-screen via CocoaPresentVideoFrame, exercising the exact functions android.media.MediaCodec itself calls end to end. | diag |
| MACAPK_VIDEO_TOOLBOX_SELFTEST_MIME | defaults to video/avc | video/avc or video/hevc | Selects the MIME type MACAPK_VIDEO_TOOLBOX_SELFTEST decodes as. | diag |
| MACAPK_VIDEO_VIEW_RECT | video plane clipped to its own view rect (current behaviour) | "0" or "false" disables; unset/other enables | Bisect gate defaulting on; =0 (or "false") restores the pre-v1.0.996 behaviour of the video plane always covering the whole window instead of being pinned to the TextureView/SurfaceView's own rect. | bisect |
## Native code & ABI

| Flag | Default (unset) | Accepted values | Effect | Audience |
| --- | --- | --- | --- | --- |
| MACAPK_ABI_REMARSHAL | enabled (ABI re-marshalling ON) | 0/1 | ABI re-marshalling of JNI calls is default-ON; setting this to "0" A/B-disables it. | bisect |
| MACAPK_ALLOW_OVERSIZE_ALLOC | disabled (oversize allocs rejected, return 0/OOM) | 0/1 | Disables the guest-heap oversize-allocation rejection (>1/4 heap size) so a suspected lifter garbage-size bug can be reproduced and traced with the watchpoint infra instead of being silently caught. | diag |
| MACAPK_ALLOW_UNSAFE_X18 | refuses to load arm64 guest code when x18 preservation is unavailable | any set value | Overrides the hard x18-capability-probe refusal and continues loading arm64 guest code anyway, at the cost of silent memory corruption ("Expect corruption"). | bisect |
| MACAPK_ALU_PC_WRITE_DEAD | new behavior: a non-flag-setting ALU write to R15 lowers as an indirect branch (ALUWritePC) | 1 (any other value/unset keeps new behavior) | Restores the old, incorrect dead-store lowering for an ALU write to PC instead of treating it as an architectural indirect branch. | bisect |
| MACAPK_ANDROID_ENVIRON | enabled (guest sees a synthesized Android-shaped `environ`) | 0/1 | Serves the guest a measured, Android-platform-shaped `environ` (18-ish zygote-style entries) instead of the host developer's shell environment; "0" restores the old host-environment leak. | bisect |
| MACAPK_ARENA_PREFAULT | enabled once libunity is present (Unity guests only) | 0/1 | Touches (commits) every page of a fresh guest-heap-arena allocation so the guest's first write into it never takes the deterministic x18-losing minor-page-fault; force-overrides the Unity-presence auto-gate either way. | bisect |
| MACAPK_ARENA_PREFAULT_CAP | 64 MiB (allocations above this size are not prefaulted) | bytes (0 = no cap) | Diagnostic override of the prefault size cap, used to test whether an x18-trap loop's buffer is a large arena allocation the default cap was skipping. | diag |
| MACAPK_ARENA_WX | heal (seal + log + re-heal on a guest write fault into a W^X-sealed page) | 0 / strict / (anything else = heal) | Controls the guest-heap arena's W^X-violation recovery: "0" turns healing off (arena stays fully RW), "strict" lets the fault fall through to normal SIGSEGV handling for a deliberate hunt, anything else seals+logs+heals. | bisect |
| MACAPK_ARM32RC_COPROC_UNDEF | off (an unimplemented ARM coprocessor instruction lowers to a no-op) | 0/1 | Makes an unimplemented-coprocessor instruction (LDC/STC/CDP/MRC/MCR on a PE with no such coprocessor) lower to A64's permanently-undefined encoding (a real fault) instead of the incumbent no-op. | experiment |
| MACAPK_ARM32RC_MULTI_ENTRY | follows MACAPK_ARM32RC_TAIL_LIVE (effectively ON) | 0/1 | Mints a second, alternate entry stub for every guest address that discovery over-fragmented interior to a function but that some OTHER function actually branches/calls into, so those calls resolve to real code instead of a return-0 panic stub. | bisect |
| MACAPK_ARM32RC_PLT_LOCAL | enabled (a PIC .so's calls to its own exported functions through its own PLT resolve locally) | 0/1 | Resolves a PIC-built .so's self-referential PLT stubs (R_ARM_JUMP_SLOT naming a LOCAL symbol) to the local function instead of falling through to a silent return-0 panic stub; "0" restores the old panic-stub behaviour. | bisect |
| MACAPK_ARM32RC_SIBLING_LINK | enabled (cross-guest-library symbol resolution against sibling .so's in the same app) | 0/1 | Lets arm32rc resolve an unresolved import against a SIBLING guest .so's exported dynsyms (e.g. Firebase Analytics calling into libFirebaseCppApp) instead of a panic stub; "0" disables this resolution tier entirely. | bisect |
| MACAPK_ARM32RC_STUB_TRACE | off (a refused/un-lowered instruction is silently tallied, not named) | any set value; a path writes to that file instead of stderr | Dumps one line per instruction the arm32rc backend refused to lower, naming the address, mode, mnemonic and reason, so a class of unexplained instruction-refusal can be traced to specific sites. | diag |
| MACAPK_ARM32RC_TAIL_LIVE | enabled since v1.0.1086 (implies MULTI_ENTRY) | 0/1 | Turns a guest tail-branch into a real `bl`+epilogue+`ret` (instead of `epilogue; b target`) and lets an alternate entry inherit live guest registers rather than zeroing them, fixing shared-tail functions (e.g. zlib crc32) that used to always return 0; "0" reverts and is the bisect lever for a future deep-tail-recursion regression. | bisect |
| MACAPK_ARM32RC_TRACE_STORES | off (no per-store watchpoint instrumentation) | any set value; "multi" restricts to multi-word (STRD/STM) stores only | Emits a `__macapk_trace_store` call after each instrumented guest store so a watchpoint harness can see writes to a specific address; "multi" narrows instrumentation to multi-word store shapes to keep single-word-STR-heavy apps from timing out before reaching the bug. | diag |
| MACAPK_ARM32RC_TRACE_STUBS | off (an unresolved call target is not named) | any set value | Names, up to 400 times, the real guest address of every call arm32rc could not resolve and had to stub, since the panic trampoline itself only ever sees whatever register the guest left behind, not the true target. | diag |
| MACAPK_ARM32_ARM_EXTENT_ALIGN | enabled (an ARM-mode function's lifted extent may only end on a 4-aligned address) | 0/1 | Refuses to clip an ARM32 function's lift extent at a discovery candidate that lands on a non-4-aligned address (architecturally impossible as an ARM boundary), preventing a phantom mid-instruction Thumb "start" from truncating a real function and stranding its epilogue; "0" restores the pre-v1.0.1088 behaviour. | bisect |
| MACAPK_ARM32_DROP_VADDRS | inert (no discovered functions are suppressed) | comma/space-separated hex vaddrs | Diagnostic-only: suppresses emission of the named discovered function bodies (so calls to them get the usual return-0 panic stub instead), used to confirm whether a specific fabricated/over-eager discovery result is on the live execution path. | diag |
| MACAPK_ARM32_FNPTR_ARG_WINDOW | off (BL-argument idiom look-ahead capped at 6 instructions) | 0/1 | Widens the ARM32 discovery BL-argument idiom's look-ahead window from 6 to 7 instructions so a callee whose other arguments need one more setup instruction still gets its address-taken function discovered (measured 7-seed effect, isolated to one binary). | experiment |
| MACAPK_ARM32_FNPTR_POOL_PRED | off ("Gate P" literal-pool-predecessor relief inactive) | 0/1 | Admits a non-authoritative, ARM-mode BL-argument function-pointer seed as a real function entry when it is immediately preceded by a provable literal-pool word AND opens with `push {..,lr}`, recovering cocos2d-x v2 `CCScheduler::scheduleSelector` targets that would otherwise silently no-op. | experiment |
| MACAPK_ARM32_HEAP_MB | off (guest ARM32 heap stays the historic 256 MiB at 0x20000000) | integer MiB, clamped to [256,1024] | Relocates the ARM32 guest bump-heap UP into unused arena space (base 0x40000000) and enlarges it to the requested size, relieving the 256 MiB wall that asset-heavy ARM32 apps hit; every other address region keeps its default location. | experiment |
| MACAPK_ARM32_HEAP_RECLAIM | off (freed ARM32 guest blocks are never reused; heap only grows, eventually wraps) | 0/1 | Makes the ARM32 guest bump allocator actually reuse blocks released via free()/operator delete (best-fit, no split/coalesce) instead of leaking forever, trading a formerly-harmless guest use-after-free for silent corruption. | experiment |
| MACAPK_ARM32_JNI_SIG_DETECT | off (a signature-declared primitive JNI argument is truncated, never pointer-detected) | any set value | Restores the old (incorrect) heuristic of pointer-detecting a declared-primitive JNI argument by its high 32 bits, which can mis-mint a functor-pointer-cast-to-int as a JNI handle; kept only as an A/B escape hatch. | bisect |
| MACAPK_ARM32_MUTEX_TYPE | enabled (a statically-initialized guest mutex's Bionic type bits are honoured) | 0/1 | Reads the Bionic mutex-type bits (NORMAL/RECURSIVE/ERRORCHECK) out of a statically-initialized guest `pthread_mutex_t` word and maps them (with the Bionic-vs-Apple code inversion corrected) instead of always treating the mutex as NORMAL; "0" restores the old always-NORMAL behaviour. | bisect |
| MACAPK_ARM32_PICFN_THUMB2 | off (Thumb-2 PC-relative PIC function-pointer scan inactive) | 0/1 | Runs an additional Thumb-2 `add rX, pc` / `movt`+`movw` PIC-fnptr discovery scan and feeds its hits into the normal (non-authoritative) data_fnptr_seed filter chain, recovering address-taken Thumb-2 callbacks discovery otherwise misses. | experiment |
| MACAPK_ARM32_POOL_ESCAPE_828 | enabled (the v1.0.828 literal-pool "escape" carve-out is active) | 0/1 | Turns off the v1.0.828 discovery carve-out that admits a candidate on the strength of "the ARM literal pool does not mark it", so the escape's false-positive rate can be measured by diffing discovery sidecars with it on vs. off. | bisect |
| MACAPK_ARM32_SHARED_TAIL_ENTRY | off (a shared-tail branch target interior to another function is not published as its own entry) | 0/1 | Publishes an interior branch target as an independent function entry when linker evidence (an ARM-mode `STT_FUNC` symbol) proves it is a real shared-tail/continuation, fixing e.g. `__mulsf3`'s tail so calls into it stop resolving to a return-0 stub. | experiment |
| MACAPK_ARM32_SYNTH_TRUNCATE | off (the corrected synth-name-length truncation check does not run; a 7-weeks-silently-disabled behaviour stays disabled) | 0/1 | Re-enables (for A/B against the protected sweep) a `__macapk_synth_` name-length comparison that has been dead since 2026-06-06 due to a length-mismatch bug, restoring the "corrected" behaviour that several later literal-pool carve-outs were calibrated without. | experiment |
| MACAPK_ARM32_TERM_THUMB_SEED | off (the arm_prior veto applies uniformly regardless of the candidate's own tagged mode) | 0/1 | Suppresses the terminator-precedence veto specifically when the discovery CANDIDATE itself is Thumb-tagged, letting more Thumb candidates through the walk-back check. | experiment |
| MACAPK_ARM64_EMBED_BLOB | off (Lever-3 no-embed: wrapper stores only a relative path to the source .so) | 1 | Forces the older, pre-Lever-3 arm64 wrapper format that embeds the guest .so's raw ELF bytes into a `__TEXT,__elfblob` section instead of loading them from disk by path. | bisect |
| MACAPK_ARM64_EMIT | unset unless a bundled JRE is detected, in which case the runtime sets it to "1" | 1 | Historically selected the in-process Mach-O emitter over an external `clang` compile for the arm64 wrapper packer; as of v1.0.802 the clang path was deleted and the in-process emitter is the only one, so this export is now vestigial (no live `getenv` reads it) and kept only so an explicitly-set value still "reads consistently". | dev |
| MACAPK_ARM64_SKIP_INIT | disabled (every native library's `.init_array` runs normally) | 1 / t / T | Skips running a loaded arm64 image's C++ static initializers entirely, used to keep a hanging `.init_array` entry from blocking loader bring-up while missing Bionic-ABI shim coverage is diagnosed; real runs must leave this unset since engines rely on static-init for vtables/singletons. | diag |
| MACAPK_ARM64_SKIP_INIT_LIB | disabled (no image's `.init_array` is skipped) | substring of a loaded image's soname | Skips `.init_array` only for loaded images whose name contains the given substring (keeping the library mapped and its symbols resolvable), isolating whether one specific library's static init (e.g. an inline-hook mod) is what breaks the host app. | diag |
| MACAPK_AUTH_POOL_DRAIN | off (the WorkerPool submit-capture trap is not installed for MCPE) | any set value (also aliased as MACAPK_SUBMIT_POOL_DRAIN) | Installs a capture hook at WorkerPool::submit that captures pools submitted from Mac.apk's own host executor threads and keeps draining ONLY those pools off-MAIN via a cooperative drain loop, part of the MCPE 54%-sign-in-stall fix family. | experiment |
| MACAPK_AUTH_SEED_PROLOGUE_STRICT | off (a linker-backed authoritative seed is admitted via the evidence-based mid-instruction veto alone) | any set value | Restores the old, stricter requirement that even a linker-backed (.ARM.exidx / R_ARM_RELATIVE) authoritative discovery seed must also look like a function prologue, for bisecting a future discovery regression back to the looser v1.0.807 gate. | bisect |
| MACAPK_AUXV_RANDOM_NULL | off (AT_RANDOM returns a real, process-stable 16-byte buffer whose first 8 bytes equal the TLS stack-guard) | any set value | Restores the old, dishonest AT_RANDOM=0 answer for A/B, instead of a genuine non-null buffer self-consistent with the guest's TLS stack-guard canary. | bisect |
| MACAPK_BGPARK_ADDR | 0x1325cfe4 (MCPE's measured worker-park site) | hex address | Overrides the hardcoded file offset of the WorkerPool worker-park site the background-pool-enumeration capture hook installs at, for testing against a different build's offset. | diag |
| MACAPK_BIONIC_JNI_PASSTHROUGH | off (the bridge wrappers inspect the `...`/va_list to repack float/double args correctly) | 1 | Makes the JNI ellipsis-bridge wrappers unconditionally forward to the original `...V` variant without any va_list inspection, isolating "does merely patching the vtable break something" from "does our translation logic break something". | diag |
| MACAPK_BOOTCTRL_COUNT | off (no BootController/Awake boot-stage entry counters installed) | any set value | Entry-patches a fixed list of libil2cpp BootController/Awake boot-stage addresses with pure counters (run displaced insn, continue) to localize exactly which boot stage a Unity app's post-age-gate stall never reaches. | diag |
| MACAPK_BOOT_ACTIVATE | off (no forced SetActive toggle) | any set value | Forces a managed `SetActive(false)` then `SetActive(true)` on a captured GameObject to test whether driving Unity's own native (re)activation flips its native active-flag bits and unblocks `StartCoroutine`; diagnostic confirmation only. | diag |
| MACAPK_BOOT_ENABLE | off (BootController.enabled left as the engine set it) | any set value | Forces `BootController.enabled = true` via a managed `set_enabled` invoke, to test whether the boot stall is simply a disabled-controller reachability problem. | diag |
| MACAPK_BOOT_NATIVE_SCO | off | any set value (only reachable when MACAPK_BOOT_STARTCO is also set) | Calls Unity's native `StartCoroutineManaged2` entry point directly instead of the managed StartCoroutine wrapper; known to SIGABRT when invoked re-entrantly off the player-loop thread, so it is gated behind its own env on top of BOOT_STARTCO. | diag |
| MACAPK_BOOT_STARTCO | off (BootController.Start()'s returned IEnumerator is dropped, never auto-started) | any set value | Manually re-invokes BootController.Start() and then MonoBehaviour.StartCoroutine on the resulting IEnumerator, to test whether simply auto-starting the coroutine Mac.apk currently drops is enough to advance the boot. | diag |
| MACAPK_BUILD_JAVA_ID | not exported (build-identity string absent from the environment) | opaque build-identity string (set by the runtime, not user-supplied) | Exported by the runtime into its own process environment so the Java loader could key its own caches on the same JAVA_ID the runtime computed; per its own comment the read side ("the loader lane") is not wired up yet, so today this is set but not consumed anywhere in the tree. | dev |
| MACAPK_BUILD_NATIVE_ID | not exported; AppClassLoader then treats any cached translated dylib as fresh (never invalidates on identity alone) | opaque build-identity string (set by the runtime, not user-supplied) | Lets AppClassLoader compare a cached translated dylib's `.nativeid` sidecar stamp against the current translator+shim identity and re-translate on mismatch, so a rebuilt translator/shim doesn't silently keep serving stale output; absence is treated as "reuse and say nothing", never "re-translate everything". | dev |
| MACAPK_BUNDLE_TRACE | off (no Bundle put/get tracing) | any set value | Traces put/get of variant/splash/welcome-related keys through `android.os.BaseBundle`, for diagnosing state that flows through a Bundle. | diag |
| MACAPK_CANARY_MARKER_FIX | follows MACAPK_CANARY_WIDE_RELOAD's setting | 0/1 | Independently forces the pass-1 "neutralized marker" restoration fix on ("1") or off ("0") regardless of the wide-reload predicate's own setting, separating a correctness fix (spurious-marker erasure) from a behaviour-widening change that needs its own sweep. | bisect |
| MACAPK_CANARY_OUTLINED_MRS | enabled (an outlined-prologue helper's TPIDR_EL0 register is tracked across the `bl` into it) | 0/1 | Propagates the "this register holds TPIDR_EL0" dataflow fact across a `bl` into an LLVM-machine-outliner-hoisted canary-setup helper, fixing a guest whose canary-setup was hoisted to a higher .text address than the body that consumes it (e.g. TikTok's libpns_ttmachine_adapter.so); "0" reverts to the pre-fix behaviour for A/B. | bisect |
| MACAPK_CANARY_WIDE_RELOAD | off (only the narrow, spilled-guard-reload predicate patches sites) | 1 | Broadens the stack-guard-neutralization pass's reload predicate to patch more sites (guarded because it must clear the protected sweep before becoming default); with MACAPK_CANARY_TRACE=1 the two settings' patched-address sets can be diffed. | experiment |
| MACAPK_CD_WATCH_AFTER_MS | 0 (sampling can register from the very start of the watch window) | integer ms | Skips the boot flood before the CD-watch sampler starts registering discovered heap-object pointers, so the 32 fixed slots aren't filled by uninteresting early objects before the interesting one appears. | diag |
| MACAPK_CD_WATCH_MS | off (no background sampler thread is started) | integer ms (clamped to a 20ms floor) | Starts a background thread that samples a runtime-discovered heap object's fields every N ms and logs them, to distinguish "the flag was set and the wake never arrived" from "the flag was consumed" for the MCPE 54%-stall investigation. | diag |
| MACAPK_CD_WATCH_N | 400 samples | integer sample count | Caps how many samples the CD-watch background sampler thread takes before it stops, so a run's sample budget is bounded and known. | diag |
| MACAPK_CD_WATCH_OFFS | none (only the registered pointer's inline name string is printed) | comma-separated hex offsets | Names extra byte/qword field offsets off the sampled object that the CD-watch background thread should print on every sample. | diag |
| MACAPK_CENSUS_EXCLUDE | none (every thread's hits print) | comma-separated substrings, up to 8 | Suppresses PRINTING (never counting) of a guest-code census hit whose thread name contains a listed substring, so a hot generic site (e.g. the task-dispatch entry every pool shares) can still be censused for one specific thread family without flooding the log. | diag |
| MACAPK_CENSUS_SITES | none (no generic census sites armed) | `<libsubstr>:<hexoff>:<label>[:<logreg>],...` | Installs a runtime-named, positively-ID'd counting/logging trap at any (library, file-offset) pair without editing or rebuilding the shim, for censusing an arbitrary guest function during an investigation. | diag |
| MACAPK_CLASSINIT_LOG | off (il2cpp class-init hook installed but silent) | any set value | Logs each managed class's name (via `il2cpp_class_get_name`) as its `.cctor` runs, after CrashlyticsBootStage#1 has fired, to name the deepest blocking static-initializer in a boot stall's call chain. | diag |
| MACAPK_CLIPBOARD_DELAY_MS | 0 (no sleep in the ClipboardManager constructor) | integer ms | Sleeps in the AWT-free `ClipboardManager` constructor without doing any AWT work, reproducing the TIMING of the old eager `getDefaultToolkit()` delay to test whether that accidental delay was winning the MCPE 54%-sign-in race; a measurement instrument, not a fix. | diag |
| MACAPK_CLIPBOARD_EAGER | off (host pasteboard resolved lazily on first use, AOSP-faithful) | any set value | Restores the old eager `Toolkit.getDefaultToolkit()` call inside the ClipboardManager constructor, for bisecting a regression back to the pre-lazy-resolution behaviour. | bisect |
| MACAPK_CLK_SPIN_BREAK | off (no per-thread monotonic-clock read-rate cap) | 0/1 | Rate-caps a thread's `clock_gettime` reads via a token bucket to break a pathological monotonic-clock polling livelock; shipped believing it fixed the MCPE online-signaling stall, then REFUTED by an interleaved A/B (2/10 vs 4/10, i.e. it hurt), kept gated for other apps. | experiment |
| MACAPK_CLOCK_REBASE | follows Unity-presence auto-detection (rebased for Unity guests, epoch-consistent otherwise) | 0/1 (any nonzero = 1) | Forces the guest CLOCK_MONOTONIC family onto a process-relative rebased epoch ("1") or the true host epoch ("0"), overriding the automatic Unity-only gating; rebasing a non-Unity guest (e.g. MCPE) mismatches scheduler deadlines and hangs boot on any host with non-trivial uptime. | bisect |
| MACAPK_CLONE_FORK | enabled (a plain `clone()` shaped exactly like `fork()` succeeds via a real `fork()`) | 0/1 | Governs whether the ONE `clone()` shape Mac.apk can honour truthfully (no child_stack, SIGCHLD exit signal, no VM/THREAD/SIGHAND/FILES/FS/VFORK/SETTLS) is serviced with a real `fork()`; "0" restores the -ENOSYS default for every clone shape, since forking a process hosting a live JVM/ANGLE/Metal is inherently risky. | bisect |
| MACAPK_COCOS_FORCE_FOPEN | off (cocos2d-x v2's own `CCFileUtils::getFileData` branch is left in place; reads go through minizip against the installed base.apk) | 1 | Re-applies a historical .text patch that forced cocos2d-x v2 asset reads through a plain `fopen` remap path onto an extracted asset tree; kept only as a bisect lever now that reading assets from the APK in place is the default (v1.0.1586+). | bisect |
| MACAPK_COMPLETER_DELAY_ADDR | 0xbcbabc8 (MCPE's `deliver/resume` completion-resume hook) | hex file offset | Overrides which guest function entry the completer-core delay trap installs at, for retargeting the MCPE 54%-stall race-confirmation probe to a different binary's equivalent site. | experiment |
| MACAPK_COMPLETER_DELAY_ARGS | off (no argument/field dump on a delay-trap hit) | 1 | Dumps the trapped completer-core call's arguments and a handful of guarded pointer-chain dereferences (task selector/affinity fields) on each printed hit, instead of just a bare `HIT #N`. | diag |
| MACAPK_COMPLETER_DELAY_CHAIN | none (no extra chain walk) | `<hexbase>:<hexoff1>,<hexoff2>,...` | Follows one pointer out of the completer-delay trap's first argument (`*(x0+baseoff)`) and prints fields of that object, reaching state one hop away from the trapped call's own arguments (e.g. a TaskGroup's WorkerPool). | diag |
| MACAPK_COMPLETER_DELAY_EVERY | 0 (only the first 64 hits print) | integer N | Samples every Nth completer-delay-trap hit for printing instead of only the first 64, so a hot call site's behaviour LATER in a run (not just at arm time) can be observed. | diag |
| MACAPK_COMPLETER_DELAY_OBJSTR | none (the queued task's own name is not printed) | hex offset | Decodes and prints a libc++ std::string living inside the completer-delay trap's queued object at the given offset -- the task's own NAME, as opposed to PATHSTR's worker name. | diag |
| MACAPK_COMPLETER_DELAY_OFFS | none (no extra field dump) | comma-separated hex offsets, up to 8 | Prints extra byte/u32 fields read out of the completer-delay trap's target object at the given offsets, so the "interesting offset" can move without a rebuild. | diag |
| MACAPK_COMPLETER_DELAY_PATH | none (no pointer-path walk from x0) | `<h1,h2,...>:<f1,f2,...>` | Walks a pointer path from the completer-delay trap's x0 argument (printing every intermediate hop) and then dumps fields at the given offsets off the final object, so a mis-specified hop is visible instead of reading as a confident wrong answer. | diag |
| MACAPK_COMPLETER_DELAY_PATHSTR | none (no worker-name string print) | hex offset | Prints bytes at `path-end + offset` as ASCII, decoding the WORKER's inline name (e.g. "Streaming Pool", "REST Thread(0)") that a completer-delay-trap path landed on. | diag |
| MACAPK_COMPLETER_DELAY_THREAD | none (delay-trap hits are not filtered/printed by thread) | substring of a thread name | Filters the completer-delay trap's printed hits to only those firing on a thread whose name contains the given substring, separating a hot generic call site's interesting caller from thousands of uninteresting ones. | diag |
| MACAPK_COMPLETER_DELAY_US | disarmed (no completer-core delay trap installed) | integer microseconds | Installs a trap on MCPE's completion-resume prologue that sleeps for the given microseconds before continuing, testing whether delaying the completer core's drain lets a race-losing schedule-continuation register in time to avoid the 54% sign-in stall. | experiment |
| MACAPK_COMPLETION_PUMP | off (the guest's own per-frame Completion-port dispatch is unassisted) | any set value | Captures MCPE's HTTP-completion XTaskQueue handle and XTaskQueueDispatch veneer from the guest's own per-frame pump, then runs a dedicated host thread that keeps dispatching that Completion port even while MINECRAFT MAIN is blocked in the sign-in wait, so late HTTP completions are not stranded. | experiment |
| MACAPK_COND_TIMEDWAIT_REPOLL_MS | off (only the 30-day "effectively infinite" clamp applies) | integer ms | Clamps ANY `cond_timedwait` deadline longer than the given cap and returns a spurious wake on that timeout, letting a lost/mis-timed `notify_one` fail safe; REFUTED for the MCPE stall by a drift-controlled A/B (0/10 vs 3/10 -- made it worse), kept only for other lock-free-queue apps. | experiment |
| MACAPK_COND_WAIT_CAP_MS | 250 ms | integer ms (0 disables the cap) | Caps how long an "effectively infinite" (>30-day) `cond_timedwait` deadline (e.g. webrtc's `rtc::Event::Wait(kForever)`) actually parks before returning a POSIX-legal spurious wake, so a lost/mis-routed notify cannot hang a worker forever. | bisect |
| MACAPK_COND_WAIT_INF_MS | off (a plain, non-timed `pthread_cond_wait` parks truly infinitely) | integer ms (0 = true-infinite) | Caps every infinite (non-timed) `pthread_cond_wait` at the given bound and returns a spurious wake on timeout, so a lock-free-queue worker with a lost wakeup re-polls its queue instead of sleeping forever; shipped default-off since it did not resolve the MCPE 54% stall in testing. | experiment |
| MACAPK_CORO_PUMP | enabled (Unity's coroutine player-loop node is driven every frame) | 0 | Disables the generic, structurally-discovered Unity player-loop coroutine-node pump that makes `StartCoroutine`-based boot chains (e.g. Crossy's BootController.Start) actually resume past their first yield. | bisect |
| MACAPK_DATA_SUBDIR | not propagated (the elfexec helper falls back to its own default data-subdir resolution) | path/subdir string | Propagates the app's data subdirectory to a forked APK-native-executable helper process (via inherited environment), so the helper resolves the same per-app data directory as the parent runtime. | dev |
| MACAPK_DC_NO_BACKPRESSURE | off (real AOSP possession-based backpressure: the guest producer waits for a free slot) | any set value | Restores the old "newest-wins" decouple-ring behaviour (producer never blocks, consumer drops stale frames) instead of the real BufferQueue-style backpressure that re-imposes the guest-swap-blocks-until-a-slot-frees contract. | bisect |
| MACAPK_DEBUG_AUTOCLICK | off (no synthetic clicks) | `t[:x,y][;t2[:x2,y2]]...` | Schedules one or more synthetic mouse down/up sequences at given elapsed seconds (and optional view-point), for headless test-harness verification of touch dispatch. | diag |
| MACAPK_DEBUG_AUTOCLICK_DRAG_PX | 0 (a synthetic tap is perfectly stationary) | integer pixels, [0,100) | Adds a small move between a synthetic tap's down and up events, to defeat an engine's (e.g. cocos2d-x v2 ScrollView) dead-stationary-delta classification that would otherwise reject the synthetic tap as a scroll-cancel. | diag |
| MACAPK_DEBUG_AUTOKEY | off (no synthetic key events) | `<t>:<chars>` | Types each character in `<chars>` as a staggered synthetic key down+up sequence at elapsed time `<t>`, through the real keyDown: dispatch path, for headless keyboard-injection testing. | diag |
| MACAPK_DEBUG_AUTOROTATE | off (no synthetic rotation) | comma/space/semicolon-separated seconds | Fires `CocoaRotateRender()` once at each given elapsed time since first frame, letting Cmd+R-style rotation be verified headlessly on a non-frontmost custom-NSApplication window. | diag |
| MACAPK_DEBUG_TAP_HOLD | off (a synthetic click fires down+up in the same frame) | float seconds (non-numeric value = 0.12 default) | Splits a synthetic tap's down and up across separate frames (held for the given seconds) so a Unity uGUI button, which does not register a same-frame down+up as a click, actually sees a press/release. | diag |
| MACAPK_DECOUPLE_NO_THROTTLE | off (a bounded 2-PBO async readback throttles the pixel-readback ring under backlog) | any set value | Restores the old v612 unconditional every-swap 3-deep PBO ring, ignoring the throttle that presents a stale loading-screen frame under genuine GPU backlog; A/B lever. | bisect |
| MACAPK_DECOUPLE_PUBRATE | off (no publish-rate logging) | any set value | Logs the guest's actual unique-frame publish rate through the decouple path once every 5 seconds -- the true signal for whether the v618 throttle is dropping guest frames (the host FPS counter cannot reveal that). | diag |
| MACAPK_DECOUPLE_SYNC | off (glReadPixels into a PBO is async; a prior frame's fence is polled non-blocking) | any set value | Restores the old SYNCHRONOUS `glReadPixels` readback (blocking on `vkWaitForFences`) instead of the async PBO+fence pipeline that releases the guest GL-context mutex promptly; A/B lever for the v612 54%-wedge fix. | bisect |
| MACAPK_DIAG_SKIP_BL | none (every real decoded direct-call target inside a lifted function's own extent is admitted) | comma-separated hex vaddrs | Excludes the named mid-function `bl` targets from being lifted (reverting them to a return-0 panic stub), to confirm whether one specific thunk's body is what a crash or bad behaviour depends on. | diag |
| MACAPK_DIFF_FIRST_ONLY | off (each distinct instrumented function captures its own first invocation) | any set value | When no MACAPK_DIFF_TARGET is set, restricts the differential-capture harness's single global trace sink to only the very first function entered process-wide, instead of the first invocation of each instrumented function. | diag |
| MACAPK_DIFF_MAX_INSNS | 500000 lines | integer instruction count | Caps how many `__macapk_trace_regs` lines the differential-capture harness writes per captured function before stopping, bounding the trace file size for a long-running boot-path function. | diag |
| MACAPK_DIFF_OUT | disabled (capture/trace shims are no-ops) | directory path | Names the output directory the ARM32 Unicorn-differential capture harness writes entry snapshots, resident-memory dumps and per-instruction register traces into; unset, the whole capture mechanism costs nothing. | diag |
| MACAPK_DIFF_TARGET | none (the harness captures the first invocation of every instrumented function) | hex vaddr | Pins the differential-capture harness's single global trace sink to exactly one named function even when many functions in the dylib are instrumented, so a batch sweep's `.init_array` constructor doesn't grab the sink first. | diag |
| MACAPK_DIFF_TARGET_END | MACAPK_DIFF_TARGET + 0x8000 | hex vaddr | Bounds the address range the differential-capture harness records per-instruction register traces for, so a nested instrumented callee's trace does not pollute the target function's trace. | diag |
| MACAPK_DIFF_VADDRS | none (no functions are instrumented for the differential harness) | comma-separated hex vaddrs | Compiles a `__macapk_capture_entry` call at entry and a `__macapk_trace_regs` call after every instruction into the named ARM32 function(s), for single-stepping them against a Unicorn emulator to localize a mis-lifted instruction. | diag |
| MACAPK_DISABLE_PRIM_DETECT | off (a JNI arg is classified primitive-vs-pointer by inspecting its upper 32 bits) | 1 | Falls back to the pre-2026-05 behaviour where every non-env JNI argument is unconditionally minted as a handle, for A/B-testing whether a game's lifted code happened to render more correctly under the old "broken" dimension flow. | bisect |
| MACAPK_DISABLE_TRANSLATOR | disabled (the APK's native library is translated and loaded normally) | 1 / true (case-insensitive) | Skips native-library ABI translation and loading entirely and forces the framework's empty GLSurfaceView-stub fallback, used when a translated dylib crashes the JVM during native init and you want to see the activity's Java-side UI render instead. | bisect |
| MACAPK_DISCOVERY_SIDECAR | none (no ARM32 discovery `.disc.tsv` sidecar is written) | a path, or the literal "1"/"auto" (both mean `<output-dylib>.disc.tsv`) | Names the file the ARM32 discovery sidecar is written to, for callers (like the runtime's own translator subprocess launch) that fork the translator and cannot pass the equivalent `--emit-discovery` CLI flag. | diag |
| MACAPK_DRAIN_BASEQ | off (only the driver's derived-composite Completion queues are pumped, never the base process queue) | 0/1 (any nonzero) | Makes the MAIN-side completion pump also dispatch MCPE's BASE process XTaskQueue's distinct Completion(+Work) port -- the actual home of the GS sign-in completion that the driver's composite-queue pump can never reach. | experiment |
| MACAPK_DRAWABLE_TIMEOUT_MS | 33 ms (two vsync intervals) | integer ms, [1,1000] | Tunes the hard timeout the bounded-`nextDrawable` acquire (Vulkan-ANGLE path) waits before giving up and returning nil, part of the A/B kill/tune switch pair for the CAMetalLayer-drawable-starvation fix. | bisect |
| MACAPK_DTASK_EXEMPT | off (every contended mutex, including a DeferredTask's own, pays the MUTEX_FAIR handoff tax) | any set value | Exempts a Bedrock `Core::DeferredTask`'s own per-instance mutex from the MUTEX_FAIR boot-window handoff sleep, restoring Android-speed completion for MAIN's per-frame wait / the sign-in step's shared CAS handshake while keeping the barging tax on every other mutex; part of the MCPE 54%-stall fix family. | experiment |
| MACAPK_ELFEXEC | enabled (an APK-shipped native ELF executable is made runnable via the elfexec helper) | 0/1 | Governs whether Mac.apk installs an `elfexec`-shim stand-in for an APK's own bundled native executable (letting `ProcessBuilder`/`Runtime.exec` launches of it actually run); "0" reverts any previously-installed stand-in back to the raw guest ELF. | bisect |
| MACAPK_ELFEXEC_LOG | `<sidecar_dir>/<default log file>` | file path | Redirects the elfexec helper's diverted-stderr diagnostic log to the given path instead of the default sidecar-directory log file. | diag |
| MACAPK_ELFEXEC_SHIM_DIR | falls back to a `.shimdir` sidecar file, then MACAPK_SHIM_DIR | directory path | Tells the elfexec helper process where to find `libmacapk-bionic-shim.dylib`; normally set by the runtime and inherited by the forked helper, but can be hand-set for a manually-run helper. | dev |
| MACAPK_ELFEXEC_VERBOSE | off (silent success path, since an app may treat non-empty stderr as an error) | 1 | Logs the elfexec helper's guest-entry parameters (entry point, stack pointer, argc/envc/auxv count) to stderr on entering the guest executable. | diag |
| MACAPK_EPOLL_WAIT_CAP_MS | 250 ms (default-on liveness net) | integer ms (0 = restore true-infinite epoll_wait) | Caps an infinite `epoll_wait` (used by webrtc's `rtc::Thread`, which MCPE's cohtml/Gameface run tasks on) at a liveness bound, so a lost/missing cross-thread WakeUp cannot hang an epoll-based message loop forever; on each capped return the loop just re-checks its queues. | experiment |
| MACAPK_EXC_NATIVE_STACK | off (no native frame-pointer backtrace on a managed exception) | any set value | Walks the live x29 frame-pointer chain to print a native backtrace (`libil2cpp+0xRVA` per frame) alongside the managed-exception logger's output, working even when il2cpp's own managed-stack API is unavailable (release-build stack tracking off). | diag |
| MACAPK_EXC_STACK | off (no live managed-frame stack walk on a service-level exception) | any set value | Walks the throwing thread's LIVE il2cpp frames (via `il2cpp_current_thread_get_frame_at`) for service-level exception types (Uri/Format/NullRef/Argument/Web/Http/Aggregate/Exception), naming the method that built the bad value; skipped for spammy Socket/FileNotFound exceptions. | diag |
| MACAPK_EXIDX_DISCOVERY | off (`.ARM.exidx`-based function-start seeding does not run) | any set value | Runs a broad-net discovery pass that seeds ARM32 function starts from `.ARM.exidx` unwind-table entries, gated off by default because it is an intentionally wide net whose false-positive rate needs the protected sweep before shipping default-on. | experiment |
| MACAPK_FAIR_BASE_US | 0 (steady-state v618 behaviour: inert under incidental contention) | integer microseconds | Sets the steady-state (post-boot-window) base MUTEX_FAIR handoff sleep charged on a sustained-starvation streak, before it grows 500us/step; 0 keeps ordinary incidental contention (streak 1-2) completely free. | dev |
| MACAPK_FAIR_BOOT_MS | 90000 ms | integer ms (0 disables the boot window, reverting to pure v618 steady-state behaviour) | Sets how long after process start the MUTEX_FAIR "boot window" (an unconditional handoff sleep on every waiter-present unlock) stays active, giving boot-time producer threads the large scheduling windows a localization/sign-in race needs. | bisect |
| MACAPK_FAIR_BOOT_US | 50000 us (the proven v606 value) | integer microseconds | Sets the unconditional per-contended-unlock sleep MUTEX_FAIR charges during the boot window (see MACAPK_FAIR_BOOT_MS), so a boot-time producer thread reliably gets scheduled. | dev |
| MACAPK_FAIR_STREAK | 4 (minimum 1) | integer | Sets how many consecutive contended-unlocks-without-a-waiter-breaking-through count as "sustained starvation" before MUTEX_FAIR's steady-state handoff sleep engages, keeping ordinary incidental contention (e.g. FMOD mixer threads) from ever paying the tax. | dev |
| MACAPK_FILEBACKED_SEGMENTS | enabled (a Stage-6 guest .so's non-writable segments are `MAP_PRIVATE` file-backed, matching bionic's own linker) | 0/1 | Governs whether Stage-6 native-arm64 loading maps a guest .so's read-only PT_LOADs as clean, demand-paged file-backed memory (the Android-faithful, evictable-under-pressure behaviour) instead of memcpy'ing them into dirty anonymous RAM; "0" opts out. | bisect |
| MACAPK_FILEBACKED_VERIFY | sampled verification (ELF header + phdr table + ~4KB at 3 points per segment) | "full" (anything else keeps sampled verification) | Forces a chunked full memcmp of a candidate on-disk .so against the embedded blob before file-backing its segments, instead of the default cheap sample, for diagnosing a suspected content mismatch. | diag |
| MACAPK_FILE_HEAL | enabled (pre-existing poisoned file modes in the app's data dir are healed once at startup) | 0 (anything else keeps healing on) | Governs whether Mac.apk heals pre-existing poisoned file permission modes in the app's data directory once, before the guest opens anything. | bisect |
| MACAPK_FIND_CORO | off (no one-shot coroutine-owner scan) | any set value | Runs two one-shot diagnostic scans: naming the il2cpp class/method whose native pointer matches a specific coroutine method (to find who is supposed to start it), and probing a captured BootController's enabled/active managed state. | diag |
| MACAPK_FIRST_MOUSE | YES (the window accepts its very first click even when not key/main) | 0 | Restores the plain AppKit default of ignoring a window's first click while it is not the key/main window, for bisecting an input regression; there is normally no reason to want this. | bisect |
| MACAPK_FIX_MAINTHREAD | off (a Unity main-thread pthread MATCH/MISMATCH is only logged, never corrected) | any set value | Writes the detected UnityMain (JVM) pthread back into libunity's own main-thread-id slot when it mismatches the engine-init-time value, fixing `Object.InstantiateAsync` integration/Awake/activation that Unity gates on `IsMainThread()`. | experiment |
| MACAPK_FNPTR_ARG_SEED | enabled (the BL-argument PIC address-computation discovery idiom is active) | 0 | Disables a discovery idiom that seeds a function pointer computed via PIC address math (r0-r3) immediately followed by a `bl`/`blx`, which recovers targets like `__emutls_init_once` reached only through `pthread_once`; was previously gated off only by an unrelated env var accident. | bisect |
| MACAPK_FORCE_ABI | none (the runtime's own ABI priority order picks the native-library ABI) | arm64-v8a / armeabi-v7a / armeabi / x86_64 / x86 | Pins native-library selection (and the translated-dylib cache filename suffix) to a single requested ABI subdirectory with no fallback, so the same APK can be A/B'd across the arm64 Stage-6-native oracle path and the ARM32-lifter path. | diag |
| MACAPK_FORCE_GS_HANDLER | off (MCPE's GatheringService access-handler is only invoked if the user-session's own terminal sign-in-state signal actually fires, which it never does on Mac.apk) | integer ms | Captures the GatheringManager instance, forces its internal readiness gate to 1, and invokes the GS-access handler directly from a host thread -- reproducing the emulator's terminal-signed-out listener fire to CONFIRM that a never-invoked handler (not a build defect) is the 54%-stall root. | diag |
| MACAPK_FORCE_JOBSYNC | off (the JobQueue main-thread deferred-call-queue drain never runs, since Mac.apk's partial Unity player loop never calls the frame-sync it lives in) | any set value | Replicates the JobQueue frame-sync's deferred-call-queue-drain tail once per frame on UnityMain (before the AsyncInstantiate pump integrates), testing whether driving that missing drain lets pending InstantiateAsync completions integrate. | experiment |
| MACAPK_FORCE_LIFT_VADDRS | none (only heuristically-discovered functions are lifted) | comma/space-separated hex vaddrs, optional `:thumb` suffix | Force-lifts specific guest addresses as functions, bypassing all discovery heuristics, to test "is X a real function?" (e.g. an ARM long-call veneer or a non-canonical-prologue Thumb function) without modifying and retranslating with a broadened heuristic. | diag |
| MACAPK_FORCE_RET_ADDR | off (no forced-return trap installed) | hex file offset | Overwrites a guest function's entry so it immediately returns a fixed value without running its body, used to confirm that the MCPE 54% stall is the GS-issue readiness gate (forcing the user-session predicate to return 1 should make sign-in proceed). | diag |
| MACAPK_FORCE_RET_VAL | 1 | integer | Sets the fixed `x0` return value the MACAPK_FORCE_RET_ADDR trap installs, when something other than a truthy "1" is needed. | diag |
| MACAPK_FOREIGN_WAKE_DEFER_BCAST | off (a host-originated `pthread_cond_broadcast` wakes all parked waiters immediately) | any set value (also needs MACAPK_FOREIGN_WAKE_DEFER_US > 0) | Extends the foreign-wake defer (see MACAPK_FOREIGN_WAKE_DEFER_US) to cover `cond_broadcast` as well as `cond_signal`; REFUTED by an 18-run interleaved A/B (18% vs 22% cross -- no effect), since the real stealer busy-spins rather than waiting on the condvar. | experiment |
| MACAPK_FOREIGN_WAKE_DEFER_US | 0 (off; a host-originated cond wake is delivered immediately) | integer microseconds | Delays a HOST-thread-originated (not guest-originated) `pthread_cond_signal`'s wake-delivery by the given microseconds, reproducing the Bionic/Linux CFS wakeup-granularity window a guest producer/consumer implicitly depends on, so a running "pump" wins the queue-pop race instead of a just-woken parked stealer. | experiment |
| MACAPK_FREE_QUARANTINE_MAX_CHUNK | 65536 bytes (64 KiB) | bytes | Sets the chunk-size threshold above which a freed allocation bypasses the UAF free-quarantine (and its memset) entirely and goes straight to `real_free`, since large chunks (texture/mesh buffers) never needed the FMOD-UAF-class zeroing+holding. | dev |
| MACAPK_FREE_QUARANTINE_MAX_MB | 32 MiB | megabytes | Hard-caps the total bytes the free-quarantine backlog may hold; when adding a chunk would exceed it, the oldest entries are evicted (freed for real) even if younger than the hold window, bounding worst-case retention under heavy allocation churn. | dev |
| MACAPK_FREE_QUARANTINE_SECS | 30 seconds | seconds (floating point) | Sets how long a freed allocation is held (zeroed, in a quarantine list) before it can be recycled, defending against a known FMOD/libfmod use-after-free class; the guarantee is a hard time window, not size- or count-based. | dev |
| MACAPK_FUTEX_WAIT_CAP_MS | off (0; an infinite raw FUTEX_WAIT parks truly forever) | integer ms | Caps an infinite raw `FUTEX_WAIT` at a liveness bound (returning a futex-legal spurious wake on timeout) so a lock-free work-queue worker with a lost wakeup re-polls; default off because capping every infinite futex wait is broad (Unity Baselib parks here heavily) and did not resolve the MCPE 54% stall in limited testing. | experiment |
| MACAPK_GC_APPEND_WATCH | off (no bdwgc heap-region-add hook installed) | any set value | Patches bdwgc's free-region/block-list-add entry to log every heap-region-add call's `(start, bytes)` and flag garbage (`0xffffffff` low32) or huge (>=1GiB) regions with a stack-scanned il2cpp caller chain, localizing the caller behind a "Deallocating excessively large block" abort. | diag |
| MACAPK_GMS_CHILD_FIRST | enabled (a GMS-bundling app's classloader resolves its own R8'd GMS classes before the framework's shadow) | 0 / false (disables; anything else, including unset, keeps it on) | Governs whether the app classloader looks up GMS/Firebase classes child-first (the app's own bundled client) instead of parent-first (Mac.apk's shadow implementation), so a bundling app computes its own SERVICE_MISSING verdict rather than depending on the shadow's answer. | bisect |
| MACAPK_GPAC | enabled since v1.0.801 (a `bl` to an address inside the same lifted ARM32 function is admitted as a real synth target) | 0 | Governs whether a direct-call target that is a real decoded `bl` destination inside the SAME lifted function's own body (e.g. reusing a division routine past its zero-check) is lifted as its own synth so the call resolves correctly, instead of falling to a return-0 panic stub; "0" reverts to the old panic-stub behaviour. | bisect |
| MACAPK_GUEST_EXIT | the new MacApkProcessExit logic runs (native _exit, plus AOSP-style ActivityManager "restart myself" handling) | legacy (anything else/unset keeps the new behaviour) | Restores the raw `Runtime.exit()` call for `System.exit`/`Runtime.exit`/`Runtime.halt`, for bisecting back to the deadlock that motivated MacApkProcessExit (a re-entrant JNI static destructor blocking forever in `JavaThread::block_if_vm_exited`). | bisect |
| MACAPK_GUEST_NULL_WGUARD | off (a write through a NULL guest object silently corrupts guest page 0) | 1 | Write-protects guest page 0 so a store through a NULL "this" faults AT ITS ORIGIN (naming the first bad store) instead of surfacing hundreds of stores later as a wild-read crash; cannot be default-on because some engines' `.init_array` legitimately writes into that region. | diag |
| MACAPK_GUEST_TOTALRAM | 0 (uncapped; the guest `sysinfo()` reports the host's true physical RAM) | bytes | Caps the RAM value Mac.apk's `sysinfo()` shim reports to the guest; kept only for future per-device tuning after a v1.0.624 experiment that capped it at ~3 GiB was found to make MCPE's RenderDragon pick a low, black-rendering device-memory tier and was reverted. | dev |
| MACAPK_HIDDEN_DRIVE_HZ | 2.0 Hz | float Hz, [0.1, 30.0] | Sets the low-rate cadence at which every engine's frame-pacing pump (Swappy/Choreographer/present-bridge) is still ticked while the app's window is hidden/occluded, keeping pacing state alive without a catch-up burst on resume. | dev |
| MACAPK_HOSTWAKE_REARM | off (a host-JVM-originated `pthread_cond_signal`/`broadcast` wakes the guest only once) | any set value | Re-registers a host-thread-originated condvar wake and re-broadcasts the SAME cond a few times over a re-arm window, catching a guest worker that commits to parking in the tiny window right after the host bumped the cond counter but before it captured "seen" -- a ~40%-on-macOS-vs-100%-on-Linux host-to-guest wake-loss gap. | experiment |
| MACAPK_HOSTWAKE_REARM_MS | 25 ms | integer ms | Sets how long the HOSTWAKE_REARM re-broadcast window stays active for a given registered cond after the host's original wake. | experiment |
| MACAPK_HOST_WRITE_SKIP | off (a guest store into host executable code faults and is left to the normal SIGSEGV path, and the mprotect guard already refuses to make host pages writable) | any set value | Skips (no-ops, PC+=4) a guest store that lands on one of Mac.apk's own host executable pages, instead of letting it fault; kept only as a diagnostic escape hatch since it leaves an inline-hook installer's own trampoline bookkeeping half-written. | diag |
| MACAPK_HTTP_COMPLETION_DELAY_US | 0 (no delay at the HTTP-completion native entry) | integer microseconds | Sleeps for the given microseconds at the entry of MCPE's `OnRequestCompleted` native hook, deferring the coroutine-readiness-set itself so the completion pump parks before readiness flips -- the confirmed fix direction for the 54%-stall self-orphaning race. | experiment |
| MACAPK_ICON_ROUND_N | 5.0 (superellipse exponent) | float, [2.0, 40.0] | Overrides the superellipse exponent used to round the app's own Dock icon to macOS's icon shape (2 = ellipse, high = near-square). | user |
| MACAPK_ICON_SCALE | ~0.8047 (824/1024, macOS's standard icon-body-to-canvas ratio) | float, [0.5, 1.0] | Overrides how much an Android app icon is inset before being placed on macOS's Dock icon grid (1.0 = full bleed, the pre-v1.0.674 look). | user |
| MACAPK_IL2CPP_EXC_LOG | off (no managed-exception logging) | any set value | Entry-hooks `vm::Exception::Raise` (resolved from libil2cpp's own exported symbols, zero hardcoded RVAs) and logs every managed exception's type, message, inner exception and (optionally) live/native stack, the primary visibility tool for a silently-stuck Unity app. | diag |
| MACAPK_INPUT_FAIR_EXEMPT | enabled (the thread synchronously delivering one touch/key event is exempt from the MUTEX_FAIR handoff sleep) | 0 | Governs whether the OS thread synchronously dispatching a single input event to the guest skips the MUTEX_FAIR tax on a contended guest mutex; "0" makes it pay the tax like every other thread, for A/B comparison. | bisect |
| MACAPK_INTEG_COUNT | off (no integration/boot-stage entry counters installed) | any set value | Entry-patches a fixed list of libunity/PreloadManager async-scene-load addresses with pure counters, localizing whether Mac.apk's async scene-load completions ever integrate at all (all-zero) or stall after a specific stage. | diag |
| MACAPK_JIT_RWX_TO_RW | enabled (an RWX `mprotect` that fails on Apple Silicon falls back to RW, dropping only PROT_EXEC) | 0 | Governs the W^X-compat fallback that lets Android JIT engines (e.g. cohtml's V8, used for MCPE's Ore-UI menu) unprotect a code page as RWX -- which macOS refuses -- by retrying as RW instead, so the write phase still succeeds; "0" disables the fallback. | bisect |
| MACAPK_JIT_SERIALIZE | enabled (concurrent JIT-code-page write windows on different threads are serialized under one lock) | 0 | Governs whether opening a JIT engine's RWX "unprotect for writing" window on one thread excludes another thread from doing the same to a different host page at the same time, preventing a race where two compiler threads corrupt each other's code-page protection transitions; "0" disables serialization. | bisect |
| MACAPK_JNI_BARE_BRIDGE | off (only the `...V`/`...A` ellipsis JNI vtable slots are bridged) | 1 | Installs the bare-ellipsis (true C `...`) counterparts of the 21 marshalled JNI vtable slots, for plain-C guests that call the non-`V`/`A` forms directly; a no-op that only logs when unset, since the blast radius is the vtable every Stage-6 guest shares. | experiment |
| MACAPK_JNI_STRING_RECOVERY_OFF | off (a fault inside HotSpot's `GetString*` JNI entry, from a guest's own bad jstring handle, is recovered by returning 0) | any set value | Kill switch that disables the JNI-GetString-fault recovery, so a bad-jstring fault crashes instead of being caught and turned into a clean "fast-JNI probe failed, fall back to standard JNI" return. | bisect |
| MACAPK_JOB_CONSTRAINTS | off (a scheduled job runs regardless of requiresCharging/DeviceIdle/BatteryNotLow/StorageNotLow/network constraints) | 1 | Honours JobInfo's constraint flags by deferring a job (polling every 15s) until they are satisfied, with AOSP's override-deadline escape; shipped default-off because it can turn a job that currently always runs into one that stops running, a fleet-wide regression risk not yet cleared. | experiment |
| MACAPK_JOB_PERIODIC_WINDOW | off (a periodic job's first run fires promptly) | 1 | Places a periodic job's FIRST run inside AOSP's `[now + period - flex, now + period]` window instead of firing it immediately; same fleet-wide regression caution as MACAPK_JOB_CONSTRAINTS. | experiment |
| MACAPK_JSON_NOPARSE | off (JSONObject/JSONArray perform a real JSON parse) | 1 | Reverts `JSONObject`/`JSONArray`'s tokener constructors to the pre-v1.0.288 stub behaviour (an empty object/array, no parsing at all), an emergency rollback knob to confirm whether a regression comes from real JSON parsing without a rebuild. | bisect |
| MACAPK_JT_TRACE | off (no jump-table detection logging) | any set value | Logs, for every `rd==15` ALU operation the arm32rc core dispatch reaches, the operand shape (register/immediate/shift) so an ARM inline-jump-table (`add pc,pc,rN,lsl#k`) construct can be traced during lowering. | diag |
| MACAPK_JVM_OPTS | none (no extra JVM options) | space-separated JVM command-line options | Passes arbitrary extra options straight to `JNI_CreateJavaVM` (e.g. `-Xint`, `-XX:TieredStopAtLevel=1`, `-agentlib:jdwp=...` for a JDWP debugger), a general-purpose escape hatch for diagnosing JIT/GC/heap issues without rebuilding. | dev |
## Native code & ABI (continued)

| Flag | Default (unset) | Accepted values | Effect | Audience |
| --- | --- | --- | --- | --- |
| MACAPK_MODIMM_CARRY_CONSERVATIVE | the lifter computes the exact ARMExpandImm_C carry-out for a rotated modified immediate (a translate-time constant, free to compute) | "1" | Restores the old, always-conservative "carry unchanged" behavior for a data-processing modified-immediate instruction's carry flag. | bisect |
| MACAPK_MPROTECT_HOST_GUARD | ON: a guest mprotect() that would strip exec permission from one of Mac.apk's own host-code segments is refused (reports success, changes nothing) | "0" disables | Turns off the guard that protects Mac.apk's own JVM/shim/ANGLE executable pages from being de-executed by a guest mprotect() call. | bisect |
| MACAPK_MUTEX_FAIR | adaptive default: for the first ~90s of runtime every contended unlock sleeps ~50ms to hand off; afterward only a sustained (>=4) contended-unlock streak gets a scaled sleep | "0" disables entirely; "1" always sched_yield(); an integer N>1 always usleep(N) us; unset = adaptive boot/steady behavior | Fixes a macOS-scheduler mutex-fairness bug (a tight-looping unlocker can starve a co-consumer, since macOS gives no wakeup preemption) by handing a starved waiter a real scheduling window on a contended unlock. | bisect |
| MACAPK_MUTEX_HANDOFF | OFF: contended-unlock fairness uses the v599-v619 usleep-based streak mechanism (MACAPK_MUTEX_FAIR) | any non-"0" value | Replaces the sleep-based heuristic with a fair, direct grant-word handoff (parking_lot/WebKit-style unlock_fair) so no thread can barge in ahead of the waiter that was granted the lock. | bisect |
| MACAPK_MUTEX_OWNER_AWARE | the guest mutex parks via os_sync_wait_on_address (UL_COMPARE_AND_WAIT), giving the kernel no priority-inheritance information | any non-"0" value, read once at process startup | Switches the mutex to park via the raw UL_UNFAIR_LOCK ulock protocol so the kernel can resolve the owner and apply priority inheritance, fixing priority-inversion starvation. | bisect |
| MACAPK_NO_BOUNDED_DRAWABLE | on the Vulkan/MoltenVK path, [CAMetalLayer nextDrawable] for the guest's own layer is bounded by a hard timeout to avoid drawable-pool exhaustion deadlock | any set value | Disables that bounded/timeout-guarded acquire, reverting to a plain passthrough call. | bisect |
| MACAPK_NO_DECOUPLE | on the native-EGL Vulkan path, the guest gets an OFFSCREEN pbuffer (not a real window surface) and eglSwapBuffers does an async PBO readback -- the MCPE 54% stall fix | any set value | Opts out of the offscreen-decouple fix, giving the guest a real window surface again. | bisect |
| MACAPK_NO_DISPLAY_SYNC | the CAMetalLayer's displaySyncEnabled stays at its default (vsync-gated) on the ANGLE-Metal path | any set value | Sets displaySyncEnabled = NO, an opt-in escape hatch removing vsync-gating from the free-drawable wait inside eglSwapBuffers. | bisect |
| MACAPK_NO_EH_REGISTER | each loaded image's .eh_frame FDEs are registered with the host libunwind so guest C++ exceptions unwind across host frames | any set value | Disables that registration, so a guest __cxa_throw that would unwind into a host frame instead hits END_OF_STACK and aborts. | bisect |
| MACAPK_NO_GUEST_TEXT_PATCH | the stack-canary neutralization and MIDR_EL1-read .text patches are applied to the guest image | any set value | Skips those .text rewrites, leaving the guest's own code byte-unmodified, to test whether an integrity check that self-hashes its .text detects our patches as tampering. | diag |
| MACAPK_NO_LOCAL_JUMPSLOT | an intra-module JUMP_SLOT relocation naming a local symbol has its GOT slot bound to that local definition | any non-empty, non-"0" value | Kill switch that disables binding intra-module PLT/GOT slots to their local definitions, for bisecting a regression back to that pass. | bisect |
| MACAPK_NO_NMB | the JVMTI NativeMethodBind callback (feeds the ARM32 jlong/jdouble re-marshal and Stage-6 argument-packing thunk) is installed | any set value | Skips installing that callback entirely, for A/B bisection of the re-marshal machinery. | bisect |
| MACAPK_NO_NMB_THUNK | on Stage 6, a name-bound native with stack arguments gets an Apple-PCS<->AAPCS64 repacking thunk installed | any set value | Disables installing that thunk, for bisecting a regression (its absence reproduces a JVM-fatal SIGSEGV on multi-argument native methods). | bisect |
| MACAPK_NO_PRELOAD | AppClassLoader eagerly preloads every native library packaged in the APK before the app's own loadLibrary calls | any set value | Skips that eager preloading pass, to isolate whether a load-order/timing issue is caused by the preload itself. | diag |
| MACAPK_NO_REGCACHE | the ARM32 lifter's block-local SSA register cache is used | "1" | Bypasses the register cache, falling back to a direct load/store-per-access register-file model, isolating the cache when bisecting a codegen regression. | bisect |
| MACAPK_NO_WAKE_KICK | after a wake-from-sleep NSWorkspace notification, every live MTKView is forced to redraw immediately | any non-empty value other than "0" | Disables that forced redraw kick; there is normally no reason to set this. | bisect |
| MACAPK_NPROC_OVERRIDE | Bionic sysconf CPU-topology answers report the host's real online/configured CPU count | an integer CPU count | Overrides the reported CPU count so a guest that branches on perceived core count can be tested against a different topology. | diag |
| MACAPK_NULL_RECOVERY_OFF | ON: an arm64-native NULL-deref zeroes the destination register and advances PC instead of crashing | any set value | Disables that default-on recovery so the first unexpected NULL deref crashes cleanly with a real .ips backtrace instead of being silently masked. | bisect |
| MACAPK_OSWAIT_CAP | the OSWAIT trace log caps at 5,000,000 printed park/wake events | an integer count | Overrides the print cap for the (separately gated) OSWAIT diagnostic trace of futex-backed park/wake events. | diag |
| MACAPK_OSWAIT_FLOOR | the OSWAIT trace only logs addresses at/above the libunity allocator-arena floor (0x300000000) | a hex address (0 traces everything) | Overrides that address floor, so engines whose sync words live outside the Unity arena can still be traced. | diag |
| MACAPK_OVERLAY_IMPLICIT_FADE | the overlay layer's Core Animation fade is explicit, avoiding a present-smear-during-motion artifact | exactly "1" | Restores the old implicit-fade behavior, for a one-run A/B against the explicit-fade fix. | bisect |
| MACAPK_PARKWAKE | OFF: no generic idle-worker-pool-cond detection/broadcasting runs | any set value | App-agnostic generalization of MACAPK_WAKE_BGPOOLS: detects a thread repeatedly re-parking on the same (cond,mutex) pair and periodically issues a spurious (POSIX-legal) broadcast to keep the box non-quiescent. | bisect |
| MACAPK_PARKWAKE_NOBCAST | the PARKWAKE broadcaster thread issues its periodic broadcasts | any set value | Isolation mode: the broadcaster starts but never broadcasts, leaving only the busy-spinner threads' CPU churn as PARKWAKE's live effect. | diag |
| MACAPK_PARKWAKE_SPIN | 0: no busy-spinner threads are spawned | an integer K | Spawns K host threads that hard-spin on a core while any idle-pool cond is fresh, reproducing WAKE_BGPOOLS's sustained-CPU-churn effect generically. | bisect |
| MACAPK_PARKWAKE_WINMS | 6000 ms re-entry window for the idle-pool-cond detector | an integer (milliseconds) | Overrides the time window within which two re-entries into the same wait are treated as the idle-pool-worker polling signature. | diag |
| MACAPK_PATH_REMAP_VADDRS | inactive, zero translation-time cost | a comma/space-separated list of hex vaddrs | Per-APK diagnostic stub: replaces a synth at a listed vaddr with a tail-call to the cocos2d-x fullPathFromRelativePath shim, coercing a relative asset name into an absolute path. | diag |
| MACAPK_PAUSE_WHEN_HIDDEN | ON: while the window is minimized/occluded, the MTKView present/drive tick throttles to a low rate (default 2 Hz) instead of free-running off-screen | "0" disables (state still tracked) | Mirrors real Android's SurfaceFlinger spending ~0 work on an invisible window, without ever fully stopping the guest's render/simulation thread. | bisect |
| MACAPK_PLOOP_CENSUS | OFF, no instrumentation installed | any set value | Discovers Unity's flattened player-loop function-pointer table and pointer-swaps every slot to a counting wrapper, logging which Update nodes the engine itself drives vs which Mac.apk must drive. | diag |
| MACAPK_PLT_REDIRECT | a non-FreeType .so's intra-module PLT call to a local symbol still goes through the no-op PLT stub | any non-empty, non-"0" value | Opts every library into routing such calls through the local body instead; unmasks a lifter mis-translation in libgdx.so's render path, so it stays off outside FreeType by default. | diag |
| MACAPK_PLT_REDIRECT_FREETYPE | the FreeType-specific intra-module PLT redirect does not run | any non-empty, non-"0" value (also needs the image detected as FreeType) | Opt-in redirect of a FreeType lib's own FT_/ft_-named intra-module PLT calls; known to also corrupt the sibling libgdx.so's gdx2d decode via shared guest state, kept opt-in for investigation. | diag |
| MACAPK_PLT_REDIRECT_STRONG_ONLY | the PLT redirect applies to weak and strong symbols alike | any non-empty, non-"0" value | Excludes STB_WEAK-bound symbols from the redirect set, to bisect which symbol's redirect breaks something. | diag |
| MACAPK_PLT_REDIRECT_VMAX | 0xFFFFFFFF (no upper bound) | a hex vaddr | Upper bound of the local-target-vaddr range eligible for the PLT redirect, paired with VMIN. | diag |
| MACAPK_PLT_REDIRECT_VMIN | 0 (no lower bound) | a hex vaddr | Lower bound of the local-target-vaddr range eligible for the PLT redirect. | diag |
| MACAPK_POLL_GUEST | no guest-memory polling thread starts | comma-separated hex guest addresses | Samples the given 32-bit guest words and prints every value change (plus the pointee's first word if it looks like a pointer), independent of both translator backends. | diag |
| MACAPK_POLL_MS | 100 ms sample interval for MACAPK_POLL_GUEST | an integer (milliseconds, 0 = spin) | Tightens the guest-memory poller's sample interval, to distinguish "the store never landed" from "it landed and was clobbered soon after". | diag |
| MACAPK_PRINT_DESTINATION | PrintManager uses the host's default CUPS destination logic | "<cups-queue-name>", "default", or "none" | Selects where a guest print job is spooled: a named CUPS queue, the host default, or "none" to fail the job. | user |
| MACAPK_PRIO_POOL_BOOST | off | any truthy integer | Lifts every setpriority-managed WorkerPool thread to at least QOS_CLASS_USER_INITIATED, compensating for macOS's strict-tier scheduler putting a nice-0 pool worker in the same bucket as a boot-time thread storm. | bisect |
| MACAPK_PROCESS_SELF_KILL | OFF: Process's self-kill idioms (ProcessPhoenix/ACRA/game-exit paths) are empty no-op bodies | "1" | Makes those self-kill paths actually send the kill signal (AOSP-faithful), pending a fleet A/B before it becomes unconditional. | bisect |
| MACAPK_PROCFS_STAT | OFF: a synthesized /proc file reports the real host temp-file's stat() values | any set value | Makes stat/lstat/fstat on synthesized procfs files and Android /data-dir paths report Linux-procfs-faithful values (size 0, proc dev 0x13, uid/gid 0, or the /data partition's measured identity) instead. | bisect |
| MACAPK_PROC_FULL_MAPS | ON: /proc/self/maps reports the full real region census, not just guest PT_LOADs | "0" restores the pre-v1.0.1000 guest-PT_LOAD-only output | Lets a regression in any maps consumer be bisected in one run against the old, narrower maps content. | bisect |
| MACAPK_PROC_SMAPS_TTL_MS | 100 ms | an integer (milliseconds) | Sets the cache lifetime of the synthesized /proc/self/smaps census, since a real device's memory layout can't change faster than this. | dev |
| MACAPK_PROP_INJECT | the Bionic property store's contents come only from the normal boot-time fill | "k=v;k2=v2;..." | Applies the given key/value pairs once, the first time a guest walks the property store, to test whether an app's control flow is sensitive to a specific property value. | diag |
| MACAPK_PTRACE_EMU | OFF: a guest clone(flags=0) self-supervisor call returns -ENOSYS and the app tolerates the failure | any set, non-"0" value | Answers the guest's self-ptrace anti-debug scheme in-layer with a real fork()'d child servicing PEEK/POKE over a socketpair, with ATTACH honestly refused if a real debugger is already attached. | experiment |
| MACAPK_PUMP_CAPTURE | not installed | any set value, libminecraftpe-only | Log-only capture of the GS-coroutine WorkerPool pump's (pthread, TaskGroup) pair, so the pump thread can be identified before wiring the completion-pump redirect. | diag |
| MACAPK_PUMP_CAPTURE_ADDR | 0x13203b28 (the hardcoded TaskGroup::queue owner-branch offset) | a hex file-offset | Overrides the address MACAPK_PUMP_CAPTURE traps, for a build where that offset has moved. | diag |
| MACAPK_PUMP_DRAIN_GAMELOOP | the host completion-pump drains only enumerated background pools, never the MAIN-affine game-loop pool | any set value | Also drains the game-loop pool off-MAIN, on the theory the auth continuation may sometimes land there; risks running a MAIN-only task off-MAIN. | bisect |
| MACAPK_PUMP_DRAIN_HOST | the host completion-pump does not additionally drain background WorkerPools off-MAIN | any set value | Enumerates every background WorkerPool and keeps the host pump draining each off-MAIN, so a stranded auth/GS continuation runs even while MAIN is blocked. | bisect |
| MACAPK_QUERY_POLL_FLUSH | ON: a GL_TIMESTAMP query on ANGLE-Metal is answered from a real recorded host-monotonic timestamp with no mid-frame glFinish | "0" disables | Disables that timestamp-query synthesis, generic to any engine (e.g. bgfx) gating present on a GL_TIMESTAMP query on ANGLE-Metal. | bisect |
| MACAPK_QUIET_CLASSLOAD | per-class classload INFO log lines print | exactly "1" | Suppresses AppClassLoader's classload INFO logging, the primary path for quieting the hottest log lines. | dev |
| MACAPK_RAW_SYSCALL_REDIRECT | ON: a guest raw syscall() libc wrapper is redirected to __macapk_arm64_syscall so its own errno adapter runs on the result | "0" disables | Kill switch for the raw-syscall-wrapper redirect pass. | bisect |
| MACAPK_REAL_LOOPER | ON (default since v1.0.391): the main thread runs a real, blocking Looper.loop()/MessageQueue.next() model, and prepare()'d threads get a genuine per-thread queue | "0" reverts to the legacy per-frame Handler.drainGlobal model | Gates the whole AOSP-faithfulness campaign (real ViewRootImpl, Choreographer, Compose, HandlerThread, Unity's render-thread Looper) on a real Looper model instead of a per-frame drain stand-in. | bisect |
| MACAPK_RECOVER_GUEST_NULLCALL | a guest null-indirect-call fault landing at pc=0 is not recovered by this path | any set value | Recovers by returning from the null callee to its caller with x0=0, logging the calling il2cpp MethodInfo's class/method name so the null call site can be root-caused. | diag |
| MACAPK_RELOC_MEMO | ON: symbol-resolution results are memoized per load-pass, collapsing MCPE's dlsym-miss traffic ~6x with byte-identical output | "0" disables | Kill switch for that per-pass resolver memo (FMOD-prefixed names always bypass it regardless). | bisect |
| MACAPK_RENDER_STATE | OFF, no logging | any set value | Read-only, once-per-frame probe of libunity's player-started/first-frame/quit/GfxDevice lifecycle globals, to identify why nativeRender may skip the full player loop. | diag |
| MACAPK_RM_HOOK | not installed | any set value | Pure diagnostic entry-counter hook on Unity Addressables' ResourceManager::Update, to confirm whether Mac.apk drives the per-frame Addressables completion pump at all. | diag |
| MACAPK_RTC_EVENT_WAIT_CAP_MS | off (0/unset): webrtc's infinite rtc::Event::Wait has no liveness net | an integer (milliseconds), libminecraftpe-gated | Caps that one infinite cond_wait (by return address) so a timeout returns nonzero, making ProcessMessages re-drain instead of parking forever. | bisect |
| MACAPK_RWLOCK_SPIN | 0, off: a contended rwlock immediately parks | an integer iteration count (bare "1" = 20000) | Diagnostic instrument: spins on try-lock with sched_yield() before parking, to test whether Bionic-faithful parking starved the fairness mechanism the MCPE sign-in stall depends on. | diag |
| MACAPK_RWX_PAGE_POLICY | "w": when a host page unavoidably needs both W and X, write is dropped and exec kept | "x" flips the sacrifice | Drops PROT_EXEC instead, purely for diagnosis (a guaranteed crash on the next intra-image call). | diag |
| MACAPK_SCENE_INTEGRATE | OFF: Mac.apk's streamlined nativeRender never drives Unity's scene-integration step, so a "Loaded" scene never activates | any set value, libunity-only, byte-checked | Calls the engine's own PreloadManager step each frame (skipping while a load is in flight) so any completed scene gets integrated in-context. | bisect |
| MACAPK_SCHED_YIELD_FAIR | OFF: sched_yield() maps to libSystem's weak Darwin yield | any non-"0" value | Gives sched_yield() Linux-faithful semantics via thread_switch depress-yield; a drift-controlled A/B refuted it as the MCPE stall's fix (that stall is a mutex-handoff starvation, not CPU contention). | bisect |
| MACAPK_SCROLL_NO_MOMENTUM | ON: a trackpad's macOS momentum/fling tail is ignored, so a synthetic drag ends the instant fingers lift | "0" restores macOS momentum scrolling | Owner-tuned trackpad-feel knob picked after an A/B on Subway Surfers' lane-steering. | user |
| MACAPK_SCROLL_SCALE | 0.5 | a float in [0.05, 4.0] | Scales the synthetic drag travel a trackpad gesture is turned into; the owner picked 0.5 over 1.0 ("100% better") for swipe-segmenting games. | user |
| MACAPK_SELF_CAPTURE | ON: the runtime's own in-process frame-capture machinery is active | "0" disables | Turns off self-capture entirely; rendercheck tooling then falls back to macOS screencapture. | dev |
| MACAPK_SELF_CAPTURE_LAYER | ON (when MACAPK_SELF_CAPTURE is also on): a CAMetalLayer's nextDrawable is swizzled to capture the presented drawable | "0" disables just this layer-capture path | Opts out of per-layer drawable capture specifically, leaving other self-capture sources active. | dev |
| MACAPK_SETPRIORITY_MAP | ON: a guest setpriority(PRIO_PROCESS, tid, nice) call is resolved via the tid registry and applied as a real macOS per-thread QoS override | "0" logs only, does not change priorities | Lets a guest's Android per-thread nice calls (which macOS's PID-based setpriority cannot express) restore the intended Android thread-priority ordering. | bisect |
| MACAPK_SHIM_DIR | falls back to the sidecar directory's .shimdir file, then gives up | a directory path containing libmacapk-bionic-shim.dylib | Third-priority manual override of where the elfexec helper finds the Bionic shim dylib, for a hand-run invocation outside the normal process tree. | dev |
| MACAPK_SIGNALING_REDRIVE_MS | off (0/unset), libminecraftpe-only | an integer (milliseconds) | Candidate fix for a token-async state stuck by a lost scheduling race: captures each stuck state via a NotifyB trap, then a watchdog replays its transition plus the executor submit after the delay. | bisect |
| MACAPK_SIGNIN_DRAIN | OFF: only MAIN (via the other MAIN_DRAIN mechanisms) ever drains the sign-in WorkerPool P | any set value | The "emulator-grounded hop-B": a dedicated host thread reproduces the emulator's concurrent multi-worker drain of P, only while MAIN is actively parked in the sign-in wait. | bisect |
| MACAPK_SKIP_LIBS | every native library the guest loads is loaded normally | a comma-separated list of library names (e.g. "fmod") | Forces the listed libraries to use the empty stub fallback instead of being loaded/translated. | dev |
| MACAPK_KEEP_APP_CRASHHANDLER | Off: Mac.apk's own crash-recovery net owns SIGSEGV/SIGBUS/SIGABRT/SIGFPE/SIGILL/SIGTRAP | any set value | Restores the guest app's own signal handler for those signals instead of leaving them owned by Mac.apk's crash-recovery net/embedded JVM; may break the runtime's own recovery net. New, generalized name for the legacy MACAPK_UNITY_KEEP_CRASHHANDLER (both OR'd together at the same check). | diag |
| MACAPK_LAZY_DEX | Off: classes.jar is eagerly converted at install | any set value | Skips the eager DEX-to-classes.jar conversion at install, deferring to per-class lazy conversion instead. | experiment |
| MACAPK_LIFTED_SEGV_PC_GATE | Some default gating behavior for a lifted function's SIGSEGV PC handling | 0 | Kill switch: `=0` restores the old unconditional behavior this gate replaced; effect not further documented at the site beyond that. | bisect |
| MACAPK_LIFTER_EMIT_STORE_TRACE | Off: the lifter does not emit store-tracing instrumentation | 1 | Emits a store-trace call in lifted code at lift time, part of the same watchpoint infrastructure as `MACAPK_WATCH_ADDR`/`MACAPK_WATCH_WRITE_VADDR`. | diag |
| MACAPK_LIFTER_PC_WARN_ALL | Off: only a filtered subset of PC-related warning lines print during lifting | 1 | Restores printing every PC-warning line instead of the default filtered subset. | diag |
| MACAPK_MAIN_DRAIN | Off: the MAIN-thread cooperative-drain injection is not installed | any set value | Installs a cooperative-drain trap on MAIN that helps deliver the GS sign-in completion (a WorkerPool P task); part of the v650 MCPE 54%-sign-in-stall fix family. Implied by `MACAPK_MAIN_DRAIN_PUMP`. | experiment |
| MACAPK_MAIN_DRAIN_AFFINE | Off: WorkerPool P's MAIN-affinity is not enforced by this mechanism | any set value | Lightly enforces that pool P's tasks stay MAIN-affine inside the `MACAPK_MAIN_DRAIN` capture trap, matching the emulator's own MAIN-affine scheduling. | experiment |
| MACAPK_MAIN_DRAIN_ALL | Off | any set value | Injects the MAIN-drain mechanism for all captured pools rather than a narrower default set. | experiment |
| MACAPK_MAIN_DRAIN_CAP | A built-in default cap | an integer, parsed via `strtoull` (base 0) | Overrides the `MACAPK_MAIN_DRAIN` mechanism's cap parameter. | experiment |
| MACAPK_MAIN_DRAIN_NODRAIN | Off (v660): the MAIN-side pump dispatches completion queues off-MAIN | 1 (or any value not starting with '0') | Makes the MAIN-side pump dispatch its per-call completion queues ON MAIN itself instead of off-MAIN; documented combo with `MACAPK_XAC_CAPTURE`+`MACAPK_SIGNIN_DRAIN`. | experiment |
| MACAPK_MAIN_DRAIN_PUMP | Off | any set value | The v652 "last-mile fix": drives completion-queue dispatch from MINECRAFT MAIN via a watchdog inject rather than a host thread; implies `MACAPK_MAIN_DRAIN` (installs the same capture+watchdog traps). | experiment |
| MACAPK_MAIN_DRAIN_WD | A built-in default watchdog value | an integer, parsed via `strtoull` (base 0) | Overrides the `MACAPK_MAIN_DRAIN` mechanism's watchdog ("wd") parameter. | experiment |
| MACAPK_MAIN_DRAIN_WPRUN | A built-in default | an integer, parsed via `strtoull` (base 0) | Overrides the "wprun" parameter used by the heavy per-task `WorkerPool::run` trap (see `MACAPK_MAIN_DRAIN_WPRUN_TRAP`). | experiment |
| MACAPK_MAIN_DRAIN_WPRUN_TRAP | Off: the heavy per-task `WorkerPool::run` trap is not installed | any set value | Installs a heavy per-task trap on `WorkerPool::run` (a specific file offset), a finer-grained companion to the per-drain `MACAPK_MAIN_DRAIN` mechanism. | experiment |
| MACAPK_MAIN_DRAIN_XACONLY | Off | 1 (or any value not starting with '0') | Makes the MAIN-side pump dispatch ONLY the queues fed by `MACAPK_XAC_CAPTURE`, instead of every captured completion queue including the driver's useless composites. | experiment |
| MACAPK_MALLINFO_RSS | Off (current, since v1.0.614): RSS is not reported via the old `task_info` path | 1 | Restores the pre-v1.0.614 behavior of reporting RSS via `mallinfo`'s `task_info`-based path. | bisect |
| MACAPK_MAP_APK_SPEC | Off: the app's own APK/split files are not mapped into the process | a spec string | DIAGNOSTIC: maps the app's APK/split files into the process per the given spec, for testing. | diag |
| MACAPK_MAX_FPS | 120 | an integer (FPS cap) | Caps the measured-panel-rate present/drive cadence (the SAME value paces both the present loop and the guest drive tick); a 60Hz panel reports 60, a ProMotion/144/240Hz panel is capped at this ceiling. | user |
| MACAPK_MCPE_BGTASK_WATCH | Off | any set value | STEP 1 read for the 54% signaling-stall investigation: watches MCPE's BackgroundTask state transitions (libminecraftpe-gated), filtering the flood of other-task transitions. | diag |
| MACAPK_MCPE_BURST_THROTTLE_US | Off (0/unset) | integer microseconds | Diagnostic/fix: throttles a persistent guest-code burst by the given microseconds at one specific site, libminecraftpe-gated. | experiment |
| MACAPK_MCPE_TGQ_PROBE | Off | any set value | Probes `TaskGroup::queue` admission (patches the same word at file offset 0x132043d0 as another mechanism, so the two must not be combined). | diag |
| MACAPK_MEASURE_SYMBOL_RAM_MS | Off: no RAM measurement | an integer (ms) | Arms a t3-syms RAM-measurement hook for the given duration. | diag |
| MACAPK_MEMPRESSURE_FLUSH_QUARANTINE | Off: the free-quarantine (see `MACAPK_FREE_QUARANTINE_*`) is not flushed under memory pressure | 1 | Opts in to flushing the free-quarantine backlog when a memory-pressure event fires. | experiment |
| MACAPK_MEMPRESSURE_RESPONDER | ON: a memory-pressure responder runs | 0 | Disables the whole memory-pressure responder. | bisect |
| MACAPK_SPLASH_FORCE | OFF: Crossy Road's SplashIntro state-2 gate stays blocked | any set value; Crossy-Road-specific diagnostic, only while state==2 | Writes the [+0x70] gate byte directly, proving the splash CAN advance past that one gate (a second gate follows); never shipped, deleted once the real per-frame DirectorUpdate/Timeline fix lands. | diag |
| MACAPK_SPLASH_SKIP | OFF: Crossy Road's SplashIntro boot coroutine spins forever on IsAnimationLoopingIntroComplete | a mode string (e.g. "complete" or "state5"); libil2cpp-only, byte-checked | Overwrites IsAnimationLoopingIntroComplete's body with `mov w0,#1; ret` so the boot coroutine proceeds past the splash, answering only whether a second wall exists behind it. | diag |
| MACAPK_SP_DRAIN_UNGATED | the submit-pool drain thread only drains its captured pools while MAIN is actively in the sign-in wait | any set value | Restores the old, crash-prone behavior of draining always regardless of app phase (risking an off-MAIN general-pool `__cxa_pure_virtual` crash). | bisect |
| MACAPK_STRCHECK | OFF: a String-returning JNI CallStatic*ObjectMethod* does not self-probe its result | any non-"0" value | Probes every freshly-returned String-typed JNI global ref immediately (GetStringUTFChars) to pin whether heap/oop corruption happened before or after that call, for the HCR getFilesDirectory wild-write hunt. | diag |
| MACAPK_STUB_VADDRS | inactive, zero translation-time cost | a comma/space-separated list of hex vaddrs | Replaces a lifter synth starting at a listed vaddr with a trivial `ret 0` body, to bisect a lifter bug by stubbing the suspected corruption source. | diag |
| MACAPK_SUBMIT_POOL_DRAIN | OFF: a WorkerPool the final auth completion submits to may have no live drainer | any set value (or MACAPK_AUTH_POOL_DRAIN) | Captures the target pool at WorkerPool::submit and keeps a host thread draining it off-MAIN via the cooperative drain-loop, a v655 54%-stall-fix candidate at the WorkerPool layer. | bisect |
| MACAPK_SUBMIT_POOL_LOGONLY | the submit-pool drain thread actually drains its captured pools | any set value | Logs submitting-thread names for every WorkerPool::submit hit without starting the drain thread, an F7 verification mode avoiding a concurrent-destruction crash. | diag |
| MACAPK_SURFACEVIEW_Z_BELOW | OFF: the guest EGL/SurfaceView layer is pinned above the framework Canvas overlay | exactly "1" | EXPERIMENT: moves the guest EGL layer below the framework canvas overlay, matching AOSP's default SurfaceView z-order, for apps whose Canvas UI was hidden by their own GLSurfaceView. | experiment |
| MACAPK_SWAP_FINISH | OFF: no glFinish() forced before a swap | any set value | Tests the drawable-recycle-deadlock-prevention hypothesis by calling glFinish() before every swap of the guest's own EGL surface, capping in-flight frames to ~1. | experiment |
| MACAPK_TLS_DESC | ON (since v1.0.962): R_AARCH64_TLSDESC relocations resolve via a real resolver plus a guest mrs->mov#0 rewrite | "0" or "false" disables | Bisection escape hatch; without it an image carrying TLSDESC (e.g. Duolingo's rive/rlottie libs) has its resolver stay 0 and SIGSEGVs at pc=0x0 inside JNI_OnLoad instead of loading. | bisect |
| MACAPK_TRACE_EXIDX | off | any set value | Traces the ARM32 signal path's parsing of the .ARM.exidx exception-index unwind table. | diag |
| MACAPK_TRACE_EXTERN_TARGET | off | a hex vaddr | Names every external-call site whose resolved target matches the given address in the lifter's log, so a specific synth's callers can be identified. | diag |
| MACAPK_TRACE_PCJT | off | any set value | Logs the ARM32 lifter's PC-relative jump-table discovery for both idioms it recognizes (a bx-dispatched table and an ARM-mode add-pc offset-word table), including base/stride/entry-count and accept/skip. | diag |
| MACAPK_TRACE_VADDRS | off | a comma-separated list of hex vaddrs | Emits a generic per-engine entry-trace call (r0-r3, sp) at the start of any lifted function whose start vaddr is listed, decided at lift time. | diag |
| MACAPK_TRACE_WORK | returns "" (no tracing) | any set value | Default-off, generic (no per-APK branch) diagnostic for MacApkJobScheduler's scheduled-job activity. | diag |
| MACAPK_TRAP_INLINE_SYSCALLS | ON: a guest inline `svc #0` macOS refuses (SIGSYS) is caught and forwarded to a narrow set of hand-implemented syscalls (sigaltstack/rt_sig*/tkill/tgkill/gettid/getpid) | "0" disables | Lets Breakpad/Crashpad-using apps (Minecraft, many games) run out of the box; an unhandled syscall number returns honest -ENOSYS rather than a fake success. | bisect |
| MACAPK_UNITY_KEEP_CRASHHANDLER | a runtime-owned signal's guest sigaction() is recorded but not wired to the host | any set value | Legacy alias/twin of MACAPK_KEEP_APP_CRASHHANDLER (originally Unity-specific), honored identically. | diag |
| MACAPK_UNRESOLVED_CONTINUE | OFF: a guest call through an unresolved JUMP_SLOT PLT import aborts the process after logging the call site and every unresolved symbol | any set value | Downgrades that abort to "log once, return 0", letting a debugging session walk past one missing symbol to discover the next; explicitly never the shipped answer. | diag |
| MACAPK_V8_FLAGS | "--jitless" (avoids a W^X-toggle/I-cache codegen corruption that SIGSEGVs MCPE's cohtml/V8 in TurboFan) | any V8 flag string; "" disables the override entirely | Overrides the flag string passed to a V8-embedding guest's own SetFlagsFromString before V8::Initialize freezes them; a no-op for non-V8 guests. | bisect |
| MACAPK_VERIFY_SYMBOL_HASH | off: the hash-walk dynsym lookup() is not cross-checked | "1" | TEMPORARY verification mode rebuilding the old eager symbol map in a scope-local copy and asserting the hash-walk lookup returns identical results, including identical NOT-FOUND for known-absent names. | diag |
| MACAPK_VMSPACE_PROBE | off | "1" | Diagnostic-only report of the process's address-space layout via mach_vm_region_recurse, checking the sub-16 GiB region census relevant to guest-pointer/host-mapping placement. | diag |
| MACAPK_WAITSITE_LOG | off (and only active once separately armed after CrashlyticsBootStage#1) | any set value | Logs the deduplicated caller return-address of every distinct wait-primitive call once armed, so a deadlocking libunity wait-wrapper can be identified by subtracting the module's load base. | diag |
| MACAPK_WAKE_BGPOOLS | off | any set value | REFUTED as a fix (mechanism description kept accurate): level-triggers every background WorkerPool captured by MACAPK_PUMP_DRAIN_HOST's park-site hook while MAIN is in the sign-in wait; a controlled A/B showed no reliable benefit. | diag |
| MACAPK_WAKE_MODE | "full" (set the work flag AND broadcast the cond on every captured pool) | "full", "bcast", "flag", or "nudge" | Ablation switch for MACAPK_WAKE_BGPOOLS distinguishing work-stealing (targeted drain) from mere scheduler perturbation. | diag |
| MACAPK_WAKE_SIGNIN_MS | off (0/unset) | an integer (milliseconds) | The Fable-confirmed 54% sign-in fix: every N ms, level-triggers the sign-in pool's own worker cond so a parked worker drains the stranded `deliver` task off-MAIN (running it on MAIN would deadlock on the completion mutex). | bisect |
| MACAPK_WAKE_SUBMIT | off | any set value | v672 targeted nudge: also wakes the ACTUAL submit-target WorkerPools (captured via MACAPK_SUBMIT_POOL_DRAIN) during the sign-in wait, rather than only the bgpark-captured siblings an overlap experiment showed are different pools. | bisect |
| MACAPK_WATCH_ADDR | the lifter's per-store trace call does nothing | a hex guest address | Logs every traced 32-bit guest store whose target lies within +/- MACAPK_WATCH_ADDR_MASK bytes of this address, to pinpoint which lifted instruction writes a specific location. | diag |
| MACAPK_WATCH_ADDR_MASK | 7 (an 8-byte slop window) | a hex byte count | Sets the +/- window around MACAPK_WATCH_ADDR (or MACAPK_WATCH_INDIRECT's resolved address) within which a traced store is logged. | diag |
| MACAPK_WATCH_GUEST_ADDRS | the guest-global watcher is not installed | comma-separated hex guest vaddrs | Samples the given static guest globals every MACAPK_WATCH_GUEST_MS and logs each change, for a Stage-6 guest, complementing the ARM32-only store-trace mechanism. | diag |
| MACAPK_WATCH_GUEST_LIB | the guest-global watcher does not arm for any library | a substring of the target library's debug name | Restricts MACAPK_WATCH_GUEST_ADDRS's sampling to only the named guest library's load. | diag |
| MACAPK_WATCH_GUEST_MS | 1000 ms sample period | an integer (milliseconds) | Sets the sample period for MACAPK_WATCH_GUEST_ADDRS. | diag |
| MACAPK_WATCH_GUEST_N | 400 max samples | an integer sample-count cap | Bounds the total samples MACAPK_WATCH_GUEST_ADDRS takes so a long run cannot flood the log (it only ever reads, so this cannot corrupt anything). | diag |
| MACAPK_WATCH_INDIRECT | MACAPK_WATCH_ADDR (if any) is a fixed address | a hex address of a stable guest global that holds the address to watch | Watches whatever object the given global currently points at, rather than a fixed address, so a heap victim whose address changes run to run can still be pinned. | diag |
| MACAPK_WATCH_VALUE | logs every traced store within the watch window regardless of value | an integer | Filters MACAPK_WATCH_ADDR's logged stores to only those writing this specific value. | diag |
| MACAPK_WATCH_WRITE_VADDR | no watchpoint page is armed | a hex guest vaddr | mprotects the 16 KiB host page containing that vaddr to PROT_READ, so the first (or, re-armed, every) lifted store to it SIGSEGVs into a handler that logs the writer (symbol + backtrace + decoded value), then re-opens the page and lets the store retry. | diag |
| MACAPK_WEBVIEW_PROBE_FILE | the WebView page-state trace timer never evaluates an ad hoc question | a filesystem path | Whenever that file's mtime changes, its contents are evaluated as JavaScript in the live WebView page and the result printed as a PROBE log line. | diag |
| MACAPK_WEBVIEW_SAMEDOC | ON: a same-document (fragment-only) navigation delivers doUpdateVisitedHistory + onPageFinished, matching AOSP/Chromium | "0" disables | A/B lever for that navigation-delivery fix (Aurora Store's Google sign-in depends on it and would otherwise hang on the last hop). | bisect |
| MACAPK_WEBVIEW_TRACE | off: no WebView navigation/page-content logging | any set, non-"0" value | Logs every finished page's title/URL/user-agent/visible text plus ongoing in-page script activity between navigations, the only way to inspect WKWebView content since it's a native NSView sibling the Canvas frame-dump tooling can't see. | diag |
| MACAPK_WP_CAP_END_MS | 130000 ms (the cap window self-lifts at 130s) | an integer (milliseconds) | End of the time window during which MACAPK_WP_WORKER_CAP's worker cap is active. | diag |
| MACAPK_WP_CAP_START_MS | 6000 ms (the cap engages 6s in, past early boot) | an integer (milliseconds) | Start of the time window during which MACAPK_WP_WORKER_CAP's worker cap becomes active. | diag |
| MACAPK_WP_DRAIN_MS | off (0/unset); libminecraftpe-gated | an integer (milliseconds) | The 54%-signaling-stall fix at the WorkerPool level-trigger layer: captures each pool at its worker-park site, then a watchdog sets its work flag and broadcasts its cond every N ms so a parked worker re-runs dispatch and drains a stranded cross-thread post. | bisect |
| MACAPK_WP_DRAIN_SITE | 0x1325cfe4 (the current build's worker-park file offset) | a hex file-offset | Overrides the untimed worker-park address MACAPK_WP_DRAIN_MS captures its target pool from, for a build where that offset has moved. | diag |
| MACAPK_WP_QUEUE_PROBE | off | any set value | Pure observer (never wakes anything): logs each MACAPK_PUMP_DRAIN_HOST-captured background pool's queue-emptiness/flag/state during the sign-in wait. | diag |
| MACAPK_WP_WORKER_CAP | off: every Bedrock WorkerPool worker runs concurrently as normal | an integer N (workers allowed per start-routine) | Non-viable as a shipped fix (wedges boot; the racing pool is boot-critical) but kept as a gated diagnostic: parks the (N+1)th+ instance of a thread-pool's workers at their idle wait, only during a time-gated window. | diag |
| MACAPK_X18_CACHE | ON: the x18-rewrite scan's clean/dirty decision is cached per-.so (keyed on a content hash), skipping a ~40s re-scan on a cache hit | "0" disables the cache | Forces a full x18-rewrite scan on every launch, for bisecting the cache's correctness or measuring true scan cost. | bisect |
| MACAPK_X18_DEBUG | off | any set value | Verbose per-instruction logging of the x18 platform-register rewrite pass's decisions plus a post-pass scan reporting every x18 instruction that still survives unrewritten. | diag |
| MACAPK_X18_DUMPWOFF | off | a hex byte-offset within the function window being analyzed | Dumps the computed use/def/livein/liveout register masks and successor edges for every instruction within +/-0x40 of the given offset, for hand-debugging the x18-rewrite liveness analysis. | diag |
| MACAPK_X18_FN | 0 (no function traced) | a hex file-offset | Traces the x18-rewrite pass's per-window decision (status/instructions-rewritten/thunked) for whichever function window contains this offset. | diag |
| MACAPK_X18_LIMIT | -1 (no limit: every eligible window is rewritten) | an integer N | Bisect knob (never for production): commits x18 rewrites only for the first N committing function windows, for narrowing which window's rewrite causes a regression. | diag |
| MACAPK_X18_LIMIT_INSN | -1 (no per-window instruction limit) | an integer M | On the boundary window selected by MACAPK_X18_LIMIT, commits only its first M instructions (by address) instead of the whole window. | diag |
| MACAPK_X18_PLAN_VERIFY | off: a cached dirty x18 plan is replayed (only recorded write-something windows re-analyzed) | "1" (or any value != "0") | Loads the cached plan but also runs the full scan, then asserts the two effect-window sets agree -- the in-process half of the proof that a replay is byte-identical to a full re-scan. | diag |
| MACAPK_X18_POOL_BAREHINT | the x18 detour-thunk pool is placed via a reachability-checked closest-first search within +/-64 MB of the executable segment | "1" | Reproduces the pre-v1.0.762 bare-mmap-hint placement with no reachability check, which the kernel can silently ignore under ASLR, reproducing the historical "Subway residual x18 ~1-in-6" nondeterminism -- kept only to A/B that claim. | bisect |
| MACAPK_X18_REWRITE_GENERIC | ON: the generic x18 platform-register rewrite pass runs on every loaded image's executable segment | "0" opts out | The app-agnostic fix for XNU zeroing the reserved x18 register on EL0 return; the last line of defense on any host below the macOS 26.4 x18-entitlement floor, since the kernel-level preserve_x18 gate this pass overlapped with was deleted at v1.0.1018. | bisect |
| MACAPK_X18_THUNK | ON: an x18 web with no free GPR to rename into gets lowered to a SIMD-register detour thunk | "0" disables thunk lowering (also drops the thunk pool's mmap reservation) | Kill switch for detour-thunk lowering; without it those nofree webs simply stay residual x18, covered only by the older prefault/recovery mitigations. | bisect |
| MACAPK_XAC_CAPTURE | OFF; HttpClient-lib-gated | any set value | Traps XAsyncComplete (the one place every HTTP completion is submitted) to capture the GS completion's ACTUAL queue with no offset guessing, feeding it into both the completion-pump dispatch set and the dedicated MAIN_DRAIN_XACONLY set. | bisect |
| MACAPK_XSUBMIT_PUMP | OFF; libminecraftpe-gated | any set value | The v655 fix: captures each auth XTaskQueue and starts a dedicated host thread dispatching both ports of every captured queue, so sign-in continuations run off-MAIN while MAIN sits in its wait. | bisect |
| MACAPK_XTQ_SERIALIZE | not installed: MCPE's (Manual,Manual) XTaskQueues are pumped only by the guest's own per-frame driver, unreliable once MAIN blocks | an integer mode 0-3 (Manual=0, ThreadPool=1, SerializedThreadPool=2, Immediate=3; an out-of-range value clamps to 2); HttpClient-lib-gated | Overrides libHttpClient's XTaskQueueCreate to force the completion port (and, with MACAPK_XTQ_SERIALIZE_PORTS=both, the work port) into SerializedThreadPool mode, matching Android's effectively single-threaded work port. | bisect |
| MACAPK_XTQ_SERIALIZE_PORTS | "comp" (only the completion port is serialized) | "comp" or "both" | With MACAPK_XTQ_SERIALIZE set, also serializes the work port when set to "both". | bisect |
## Networking / GMS / FCM

| Flag | Default (unset) | Accepted values | Effect | Audience |
| --- | --- | --- | --- | --- |
| MACAPK_ADDRCONFIG | Enabled -- DNS lookups are filtered to match AOSP libcore's AI_ADDRCONFIG behavior | 0/false disables; anything else/unset = enabled | Default ON, AOSP-faithful filtering of DNS results by which address families the host actually has routes for. MACAPK_ADDRCONFIG=0 disables the filtering so every lookup passes through unfiltered, for A/B without a rebuild. | bisect |
| MACAPK_ALPN | Disabled -- platform SSLSockets get no ALPN negotiation | any non-null value enables (checked via `!= null`) | When set, serves ALPN for sockets produced by the platform's own (boot/platform-classloader) TLS stack, mirroring what gates OkHttp's Android10SocketAdapter on a device. Default off because it changes OkHttp's wire protocol, multiplexing and delivery threading fleet-wide and is not yet measured. | experiment |
| MACAPK_CRASHLYTICS_TRACE | Off -- Crashlytics calls are silently no-op'd | any non-null value enables | Prints every Crashlytics call (custom keys/logs, including the boot-stage markers some apps funnel through it) to stderr as "[crashlytics-trace]" lines, showing how far boot progressed. | diag |
| MACAPK_GMS_AUTH_APP | "com.android.vending" | a package name string | Overrides the "app"/"callerPkg" identity field sent on every Google /auth token request, so the owner can run that identity experiment deliberately, without a rebuild. | experiment |
| MACAPK_GMS_AUTH_SIG | Our own platform certificate's SHA-1 | a signature SHA-1 hash string | Overrides the "client_sig"/"callerSig" identity field sent on every Google /auth token request, paired with MACAPK_GMS_AUTH_APP for the same deliberate, explicit identity experiment. | experiment |
| MACAPK_GMS_DIR | `~/Library/Application Support/Mac.apk/gms` | a directory path | Overrides where the GMS state file (signed-in account, checkin android_id/security_token, cached tokens) is stored on disk. | dev |
| MACAPK_GMS_FCM | Enabled (default ON since v1.0.1179) -- the in-process FCM/push backend is live | 0/false disables, restoring the pre-v1.0.1179 contract | Gates whether com.google.android.gms answers as present with the c2dm SEND permission and a resolvable register service, i.e. whether Firebase getToken() can actually register through the bound PushRegisterService seam. =0 restores the old contract where that path is unreached. | bisect |
| MACAPK_GMS_FINSKY_UA | Mac.apk's own honest User-Agent is sent to Google's licensing endpoint | 1/true switches to the Finsky (Play Store client) User-Agent string | Swaps in the real Play Store client's "Android-Finsky/..." User-Agent so any difference in Google's response between the two identities can be measured; a deliberate act by whoever sets it, never the default. | experiment |
| MACAPK_GMS_HONEST_IDENTITY | Off -- for the licensing token scope only, the Play Store's own client_sig/app identity is substituted (owner-authorized, scope-gated concession) | 1/true restores Mac.apk's own certificate for that request | Restores our own honest identity on the licensing token request instead of the Play Store's, which is how Google's refusal (UNREGISTERED_ON_API_CONSOLE) is reproduced and the honest posture demonstrated. | bisect |
| MACAPK_GMS_HONEST_MISSING | The "tuned" contract: availability() answers SUCCESS (provider present, each service fails honestly at its own call site) and gmsPackageInstalled() answers false | 1/true forces the fully-absent contract (SERVICE_MISSING); 0/false restores the legacy full spoof (gms package reported installed and permission-holding) | Three-way A/B switch over whether com.google.android.gms is present. =1 is a diagnosis-only fully-absent posture; =0 is the legacy pre-v1.0.574 spoof, kept only so that old behavior stays reachable for comparison. | bisect |
| MACAPK_GMS_KEYCHAIN | Off -- GMS secrets live only in the 0600 properties file | 1/true enables | Opt-in switch to also read/write GMS secrets via the macOS Keychain; kept opt-in because a Keychain item's ACL binds to the code signature, and the build re-signs the runtime every time, so a rebuilt binary reading an older item triggers an interactive access prompt that can hang a headless or Dock-launched run. | dev |
| MACAPK_GMS_PRESENT | Enabled (default ON since v1.0.1175) -- Mac.apk's own in-process GMS layer (real signed-in Google account, checkin identity, com.android.vending package + licensing) is live | 0/false disables, restoring the GMS-absent contract | Master gate for Mac.apk's own GMS layer; it does not change com.google.android.gms's own absent/package status, only what com.android.vending and AccountManager expose. =0 restores the pre-flip absent contract for A/B. | bisect |
| MACAPK_ICON_ROUND | Enabled -- square, icon-sized decoded bitmaps with no explicit transform get the Android adaptive-icon rounded-square treatment, and the app's own Dock icon is masked to macOS's continuous "squircle" shape | 0 disables both (a sibling var, MACAPK_ICON_ROUND_N, tunes the squircle exponent) | Makes icons read as native by default: GlideRescue rounds square icon-sized images lacking their own transform, and cocoa_bridge masks the app's own Dock icon to macOS's superellipse corner. MACAPK_ICON_ROUND=0 disables both. | dev |
| MACAPK_NET_HOST_TRUTH | Enabled -- ConnectivityManager answers from the real host (default route, live transport, scutil) | 0 restores the old constant answer ("always WIFI, always connected") | Labelled bisect gate for the host-truth networking change, present so a future session can A/B it in one run instead of re-deriving the boundary. | bisect |
| MACAPK_OFFLINE | Off -- network state and DNS follow the host truthfully | 1/true forces an airplane-mode posture | Forces the framework to report a fully offline/de-Googled device (no default network) regardless of the host's real connectivity, and redirects JVM DNS lookups to a nonexistent hosts file; the reliable fix for apps that hang in a racy online-boot network handshake, and restores the old isolated-network behavior for users who explicitly want it. | user |
| MACAPK_PACKAGE | Unset -- if android.app.Application's singleton can't supply a package name, "the running package" resolves to null | a package name string | Fallback source for "the running package" that LicensingService's caller-attribution check (enforceCaller) needs, used when there is no live Application singleton to ask. | dev |
| MACAPK_TASK_DIAG | Off -- no census is kept | any non-null value enables | Records each TaskCompletionSource's creation stack and logs any still incomplete after a grace period, turning a silent async-gate stall into a named component; pure diagnostic, no behavior change. | diag |
| MACAPK_VERSION | `"1.0"` | a version string | Overrides the version component of the honest `"Mac.apk/<version> (Linux; U; Android ...)"` User-Agent string sent on every Google HTTP request. | dev |
## Logging & tracing

*(Trace/diag flags whose primary reader lives in a more specific subsystem file are kept with that subsystem's table instead -- e.g. `MACAPK_TRACE_SQL` is in Install & storage, `MACAPK_TRACE_TOUCH` is in Launch & window. This table holds the flags whose reader is a cross-cutting/hub file with no more specific subsystem home, plus the two master switches.)*

| Flag | Default (unset) | Accepted values | Effect | Audience |
| --- | --- | --- | --- | --- |
| MACAPK_ARM32_JNI_TRACE_RET | tracing off | 0/1 (any set value enables; getenv()!=null) | Prints the raw ARM32-to-JNI return value (hex, low byte, jboolean interpretation) for every name-bound (dlsym-resolved) native call return, to tell a guest-true/low-byte-zero value apart from HotSpot's narrow-type read. | diag |
| MACAPK_ARM64_INIT_VERBOSE | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs each Stage-6 arm64 image's DT_INIT call and every .init_array[] entry invocation with its target address and file offset. | diag |
| MACAPK_CANARY_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Names every file offset the stack-canary-neutralization pass rewrites, and afterward reports any stack-check stub left un-neutralized. | diag |
| MACAPK_CLOCK_TRACE | tracing off | 0/1 (any set value enables; macapk_envp() presence check) | Logs which POSIX clock IDs the guest queries via clock_gettime and the values returned. | diag |
| MACAPK_CORO_TRACE | tracing off (default OFF per comment) | 0/1 (any set value enables; getenv()!=null) | Traces detected coroutine-yield points during arm64 native loading. | diag |
| MACAPK_CRIT_TRACE | tracing off | 0/1 (any value except "0"/empty enables) | Traces GetPrimitiveArrayCritical/ReleasePrimitiveArrayCritical JNI calls: calling-thread stack info, handles, byte counts, and (for releases under 64 bytes) the guest buffer contents. | diag |
| MACAPK_CXA_THROW_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Un-suppresses the normally-filtered per-call trace log for __cxa_throw so every guest C++ throw gets logged. | diag |
| MACAPK_DEV_TRACE | tracing off | 0/1 (any value except "0"/empty enables) | Master diagnostic switch (the Swift launcher's "Verbose Trace" toggle / --dev-trace CLI flag): a logical OR that force-enables MACAPK_TRACE_BL (mode 1) and MACAPK_TRACE_JNI_HANDLES without setting them individually, at roughly 1 fps cost for HCR-class games; despite the comment's broader claim ("every trace function in this file"), only those two call sites actually check it in code. This is the "one toggle enables a family" master switch, distinct from MACAPK_LOG_LEVEL (a log-severity threshold, see that row). | diag |
| MACAPK_DIRECTOR_DUMP | tracing off | 0/1 (any set value enables; getenv()!=null) | Periodically (frames 1-3, then every 600) dumps a cocos2d-x v2 CCDirector's PlayableGraph bucket-head and pending-change-list pointers, to show whether any graph is registered. | diag |
| MACAPK_DLSYM_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs every guest dlsym(handle, name) call with its resolved result. | diag |
| MACAPK_DRAWABLE_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Installs the CAMetalLayer nextDrawable swizzle with per-acquire logging, for headless diagnosis of drawable acquisition on any Metal backend. | diag |
| MACAPK_DTASK_TRACE | tracing off | 0/1 (any value except "0"/empty enables) | Adds per-registration logging to the Core::DeferredTask mutex-fairness-exemption registry. | diag |
| MACAPK_DUMP_DIRECTOR | tracing off | 0/1 (any value except "0"/empty enables; also requires MACAPK_DIRECTOR_VADDR set) | At fixed frame counts (1/60/120/300/600), dumps the first 256 bytes of a cocos2d-x v2 CCDirector instance located at the user-supplied MACAPK_DIRECTOR_VADDR. | diag |
| MACAPK_DUMP_DRAWS | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs full GL draw context (bound textures/buffers, generated ids) at several glGen*/draw shim call sites. | diag |
| MACAPK_DUMP_DRAWS_RANGE | disabled (no range set) | Value: "<lo>[:<hi>]" decimal draw-index range (hi defaults to lo+200 if omitted) | Companion to MACAPK_DUMP_DRAWS: logs every draw call inside the given index window instead of only the first few plus periodic samples. | diag |
| MACAPK_DUMP_FBO_PNG | disabled (dump_at=-1) | Value: integer swap-count to dump at (also requires MACAPK_DUMP_UNITY_SURFACE=1) | One-shot glReadPixels of the entire default framebuffer at the given swap number, written out as a PPM, to see what the engine actually produced. | diag |
| MACAPK_DUMP_GUEST_FRAME | disabled (dumpN=-1) | Value: integer swap-count to dump at | One-shot glReadPixels of the guest's presented default-framebuffer surface at the given swap number, written out as a PPM; independent of MACAPK_DUMP_UNITY_SURFACE. | diag |
| MACAPK_DUMP_MATS | tracing off | 0/1 (any set value enables; getenv()!=null) | Dumps the full 16-element glUniformMatrix4fv MVP matrix for the first 8 distinct shapes seen, to classify ortho vs perspective vs perspective*lookAt. | diag |
| MACAPK_DUMP_RAISE | tracing off | 0/1 (any set value enables; getenv()!=null) | For the first 4 guest raise(sig) calls, prints the host backtrace so the lifted-code caller chain into raise (e.g. an ARM AEABI idiv-by-zero stub) is visible. | diag |
| MACAPK_DUMP_SSCANF | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs each sscanf call's format/args and result (always for the first 100 calls or any zero-match call, capped at 3000 lines total). | diag |
| MACAPK_DUMP_SYMS | disabled | Value: output file path | Dumps every loaded image's defined dynsyms (name + value) to the given path, as a load-identity verification tool. | diag |
| MACAPK_DUMP_UNITY_SURFACE | tracing off | Mixed: strict "1" only at the FBO-dump gate (:30149); any set value at the eglGetProcAddress (:31390) and eglSwapBuffers-route (:31699) logging sites | Logs the distinct EGL/GL proc names Unity resolves via eglGetProcAddress and routes its eglSwapBuffers through a diagnostic alias that samples the surface Unity is actually presenting, to tell "renders black" apart from "renders but the layer never displays it"; when exactly "1", also gates the MACAPK_DUMP_FBO_PNG one-shot dump. | diag |
| MACAPK_DUMP_VERTS_MAX | disabled (cap stays 16) | Value: positive integer vertex cap | Raises the cap on how many vertices of a glBufferSubData batch MACAPK_DUMP_DRAWS-style logging dumps, from the default 16. | diag |
| MACAPK_EGL_TRACE | tracing off | 0/1 (any value except "0"/empty enables) | Logs each EGL init-path call's result on the transparent-passthrough EGL shims, so a failing init call is visible. | diag |
| MACAPK_EPOLL_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Traces epoll registrations and epoll_wait returns. | diag |
| MACAPK_EXC_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Installs a JVMTI Exception event callback that logs every distinct (by throw-site) exception class, throwing method, and whether/where it is caught, to surface silently-swallowed exceptions. | diag |
| MACAPK_EXC_TRACE_FILTER | disabled (no filter) | Value: substring | Narrows MACAPK_EXC_TRACE's output to throwing-class/exception-class names containing the given substring. | diag |
| MACAPK_FAIR_DIAG | tracing off | 0/1 (any set value enables; getenv()!=null) | Runs a per-thread census of which threads actually pay the mutex-fairness usleep tax. | diag |
| MACAPK_FAULT_DUMP | tracing off | 0/1 (any set value enables; getenv()!=null) | For a non-lifted, non-recovered SIGBUS (capped at 12 occurrences), dumps the raw ucontext registers and a guarded frame-pointer chain walk before handing the fault to the JVM. | diag |
| MACAPK_FBO_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Traces glBindFramebuffer calls. | diag |
| MACAPK_FMOD_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Traces the FMOD-real boundary, wrapping the real-FMOD-path createSound calls for diagnosis. | diag |
| MACAPK_FRAME_PACING_DIAG | tracing off | strict "1" only | Reports how often the frame-pacing grid's wait engaged and how long it held versus the grid's target rate, to check whether the grid is actually the binding constraint. | diag |
| MACAPK_FRAME_TRACE | tracing off | strict "1" only | Logs raw per-present INTERVAL samples every 600 presents for the guest's own eglSwapBuffers, and separately a per-frame interval trace in the Cocoa present path, for frame-pacing diagnosis. | diag |
| MACAPK_FUTEX_TRACE | tracing off | 0/1 (any set value enables; macapk_envp() presence check) | Logs futex-backed pthread cond signal/broadcast and wait events (guest and host), capped per call site. | diag |
| MACAPK_GFX_HARVEST | tracing off | 0/1 (any set value enables; System.getenv()!=null) | Records every instrumented Canvas/Path/Matrix/Paint draw call's name, classified REAL/STUB/CAUGHT, yielding a complete draw-API usage-plus-gap inventory on dump(). | diag |
| MACAPK_GL_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Reports draw-calls-per-present averaged over the last 60 presents (an alias-free draw counter), printed every 60th eglSwapBuffers. | diag |
| MACAPK_IL2CPP_TRACE | disabled (256-call cap mode) | Value: positive integer invoke-count interval (any set-but-non-positive value falls back to 20000) | Switches the IL2CPP method-invoke diagnostic from a one-time 256-call cap to a steady-state per-method invoke histogram with a top-30 snapshot every N invokes. | diag |
| MACAPK_INPUT_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs how long a synchronous input call blocked the AppKit/JVM main thread. | diag |
| MACAPK_JITMAP_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Uncaps the JIT/code-region mprotect log (default caps at 24 lines) and logs the code-region layout for PROT_EXEC mappings. | diag |
| MACAPK_JNI_HANDLE_TRACE | tracing off | 0/1 (any value except "0"/empty enables) | Logs the jobject handle at both the producer (CallStaticObjectMethod*) and consumer (GetStringUTF*) sites, to tell a handle-threading bug apart from a ref-lifecycle bug. | diag |
| MACAPK_LAYER_TREE_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Traces the CALayer tree / layer-reassert diagnostic (AGDK on-demand native-EGL-layer path). | diag |
| MACAPK_LOG_LEVEL | 0 = emit everything (NativeLog.MIN_LEVEL) when run directly; AOSP-facing android.util.Log.isLoggable() instead defaults to AOSP's INFO(4) floor when unset | Integer severity threshold (VERBOSE=2, DEBUG=3, INFO=4, WARN=5, ERROR=6); not a 0/1 toggle, parsed with Integer.parseInt and falling back to the default on a parse failure | Sets the minimum android.util.Log / NativeLog severity actually emitted (anything below the threshold is dropped before the native print call, skipping the JNI crossing); this is NOT the trace-family master switch (that is MACAPK_DEV_TRACE) but a severity floor, and setting it to 7 (above ERROR) silences NativeLog.print entirely, which offline harnesses use to dodge UnsatisfiedLinkError in a bare JVM with no native lib loaded. | diag |
| MACAPK_LOUD_CATCH | tracing off | 0/1 (any set value enables; System.getenv()!=null) | Makes every instrumented catch(Throwable ignored){} swallow site log the caught exception (warn-once per site+class+message) instead of failing silently. | diag |
| MACAPK_MPROTECT_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs mprotect calls, as a separate view alongside the JIT-map log. | diag |
| MACAPK_MUTEX_DIAG | tracing off | 0/1 (any set value enables; getenv()!=null) | Arms a 500ms timeout on the first contended wait per Bionic-shim mutex and prints [mutex-diag]/[mutex-stall] lines (capped at 40) naming the owner when a wait times out. | diag |
| MACAPK_NET_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs each BSD-socket-shim call (socket/connect/bind/getaddrinfo/get-setsockopt/...) for diagnosing Bionic-to-macOS constant translation. | diag |
| MACAPK_NMB_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs each J/D-signature ARM32 JNI thunk registration, and on the name-based native-method-bind remarshal path, each guest native resolved with no ABI divergence. | diag |
| MACAPK_ONEDS_SQLITE_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs the bundled sqlite3 allocator's xMalloc calls (pointer + size), capped at 40 lines, read once rather than per-malloc. | diag |
| MACAPK_OP_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Installs three entry-point observers (Complete / InvokeCompletionEvent / RegisterForDeferredCallbackEvent) on Unity Addressables' AsyncOperationBase lifecycle. | diag |
| MACAPK_OSWAIT_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Traces os_sync park/wake calls, correlated by which address is parked. | diag |
| MACAPK_PLOOP_DUMP | tracing off | 0/1 (any set value enables; getenv()!=null) | Periodically dumps the RenderDragon player-loop node table scan. | diag |
| MACAPK_PRESENT_TRACE | tracing off | strict "1" only | Aggregates and reports, every 300 presents, the per-phase cost (malloc+memcpy, CGImage build, snapshot memcpy, CALayer contents commit) of the bitmap present path. | diag |
| MACAPK_PRINTF_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs any float-bearing printf-family format string and its output when the result contains an exponent (e/E +/-), to catch denormal-zero mis-formatting. | diag |
| MACAPK_PRIO_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs each setpriority(which, who, nice) call with the resolved thread name and mapping status, capped at 64 lines. | diag |
| MACAPK_PTRACE_EMU_TRACE | tracing off | 0/1 (any value except "0"/empty enables) | Traces the in-layer ptrace-emulation ATTACH and tracee-status decisions. | diag |
| MACAPK_RAW_SYSCALL_TRACE | tracing off | 0/1 (values starting with "0" disable; any other value, including empty-but-set, enables) | Names every raw (non-by-name) syscall a redirected wrapper makes, spelling out the path for file-opening syscalls, capped at 200000 lines. | diag |
| MACAPK_RM_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Traces the Addressables ResourceManager::Update completion-pump hook installation and its continuation address. | diag |
| MACAPK_SHADER_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Wraps glCompileShader/glLinkProgram to read back compile/link status and dump the info log on failure only, with periodic pass/fail counts. | diag |
| MACAPK_SPLASH_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Traces a Unity SplashIntro::Update / BootController boot-flow state machine via byte-checked prologue hooks. | diag |
| MACAPK_SWAPPY_DIAG | tracing off | 0/1 (any set value enables; macapk_envp() presence check) | Diagnoses Swappy frame-pacing present-tick behavior periodically and at specific gate checks. | diag |
| MACAPK_TLS_TRACE | tracing off | 0/1 (any value except "0"/empty enables) | Logs pthread_setspecific/getspecific calls with the calling thread, key, and value. | diag |
| MACAPK_TRACE_BL | tracing off | 0/1 (any value except "0"/empty enables; also force-enabled by MACAPK_DEV_TRACE) | Logs every lifted ARM32 BL (branch-link) call's from/to addresses and r0-r3, capped at 4096 hits per from/to pair. | diag |
| MACAPK_TRACE_BL_DEREF_R0 | tracing off | 0/1 (any value except "0"/empty enables) | Extends MACAPK_TRACE_BL to also dereference and print *(r0+0/+4/+8) as a guest pointer, for chasing std::vector-shaped corruption. | diag |
| MACAPK_TRACE_BL_STR | tracing off | 0/1 (any value except "0"/empty enables) | Extends MACAPK_TRACE_BL to also print r0-r2 interpreted as guest C strings. | diag |
| MACAPK_TRACE_BL_TO | disabled | Value: hex target address | Narrows MACAPK_TRACE_BL's logging to only BL calls whose target matches the given address (mode 2). | diag |
| MACAPK_TRACE_CONNSTART | tracing off | 0/1 (any set value enables; getenv()!=null) | Traces MCPE's connect-starter, reading [this+0xb0] at a fixed file offset, libminecraftpe-gated. | diag |
| MACAPK_TRACE_CORO_STEP | tracing off | 0/1 (any set value enables; getenv()!=null) | Traces MCPE's Game-Services access-coroutine step function at a fixed file offset, libminecraftpe-gated. | diag |
| MACAPK_TRACE_CORO_STEP_ADDR | disabled (uses default offset) | Value: hex address override | Overrides the default traced file offset used by MACAPK_TRACE_CORO_STEP. | diag |
| MACAPK_TRACE_DISCOVERY | disabled | Value: comma-separated hex vaddr list | Dumps the translator's final lift/no-lift verdict plus every gating predicate for each watched guest vaddr, at translation time. | diag |
| MACAPK_TRACE_DISPATCH | tracing off | 0/1 (any set value enables; getenv()!=null) | Traces MCPE's XTaskQueueDispatch-driver call, showing on a stall whether the driver ever dispatches the Completion port (the decisive 54%-stall diagnostic), libminecraftpe-gated. | diag |
| MACAPK_TRACE_DISPATCH_ADDR | disabled (uses default offset 0xad773ac) | Value: hex address override | Overrides MACAPK_TRACE_DISPATCH's default traced file offset. | diag |
| MACAPK_TRACE_FORWARDER | tracing off | 0/1 (any set value enables; getenv()!=null) | Traps libHttpClient's forwarder to discriminate whether it is genuinely alive, libHttpClient-gated. | diag |
| MACAPK_TRACE_FORWARDER_ADDR | disabled (uses default offset) | Value: hex address override | Overrides MACAPK_TRACE_FORWARDER's default traced file offset. | diag |
| MACAPK_TRACE_GL | tracing off | strict "1" only | Installs Unity's GL/EGL present hooks and does the FBO-0 readback without injecting an extra eglSwapBuffers, to observe Unity's natural render frequency/content undisturbed. | diag |
| MACAPK_TRACE_GLDRAW | tracing off | strict "1" only | Takes a one-shot snapshot of the entire GL draw state the first few times a glDraw* call fires. | diag |
| MACAPK_TRACE_GLVIEWPORT_STACK | tracing off | strict "1" only | Dumps the calling Java stack the first few times glViewport is invoked. | diag |
| MACAPK_TRACE_GUEST_ADDR | disabled | Value: hex guest file-offset | Installs a tracepoint that intercepts guest execution at the given file offset. | diag |
| MACAPK_TRACE_GUEST_JNIARGS | tracing off | 0/1 (any set value enables; getenv()!=null) | When the armed guest-code tracepoint site is a blr to a JNI function, prints x1-x3 (and for *FieldID/*MethodID calls, the decoded name/signature C strings). | diag |
| MACAPK_TRACE_GUEST_LIB | disabled (no library filter) | Value: substring of a loaded image's debug name | Restricts MACAPK_TRACE_GUEST_ADDR/OFFS tracepoints to the matching library. | diag |
| MACAPK_TRACE_GUEST_OFFS | disabled | Value: comma-separated hex file-offset list | Names which sites within the MACAPK_TRACE_GUEST_LIB-selected library get a tracepoint installed. | diag |
| MACAPK_TRACE_HC | tracing off | 0/1 (any set value enables; getenv()!=null) | Runs a libHttpClient completion census to find where an auth-completion drops, libHttpClient-gated. | diag |
| MACAPK_TRACE_HOSTWAKE | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs only HOST-originated (non-guest) pthread_cond signal/broadcast wakes; a lighter-weight alternative to MACAPK_FUTEX_TRACE that does not perturb boot timing enough to miss the sign-in HTTP phase. | diag |
| MACAPK_TRACE_INDIRECT_TO | disabled | Value: hex target address | Logs every indirect (blx) call whose target matches the given address. | diag |
| MACAPK_TRACE_JNI_ENTRY | disabled (no substring) | Value: non-empty substring | Logs every lifted-JNI-thunk entry whose export name contains the given substring, numbered, capped at 4096 lines. | diag |
| MACAPK_TRACE_JNI_HANDLES | tracing off | 0/1 (any value except "0"/empty enables; also force-enabled by MACAPK_DEV_TRACE) | Traces every JNI handle make/resolve/release. | diag |
| MACAPK_TRACE_MC | tracing off | 0/1 (any set value enables; getenv()!=null) | Traces MCPE's connect-stall detection and connect continuation, libminecraftpe-gated. | diag |
| MACAPK_TRACE_MC_MICRO | tracing off | 0/1 (any set value enables; getenv()!=null) | Installs only the rare MCPE signaling sites (RespH/McpeHttpIssue/TokenRUN) instead of the full MACAPK_TRACE_MC set. | diag |
| MACAPK_TRACE_MC_MIN | tracing off | 0/1 (any set value enables; getenv()!=null) | Installs only the 6 key MCPE chain sites, for lower census latency than the full trace. | diag |
| MACAPK_TRACE_NOTIFYB | tracing off | 0/1 (any set value enables; getenv()!=null) | Runs the NotifyB state probe, libminecraftpe-gated. | diag |
| MACAPK_TRACE_REMAP | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs any .apk-path string, and separately every path, passed through the guest path-remap/relabel logic. | diag |
| MACAPK_TRACE_SURFACE_SIZE | tracing off | 0/1 (any set value enables; getenv()!=null) | Traces the window/surface size measured at each boot, used to derive the AOSP-faithful surface-size formula. | diag |
| MACAPK_TRACE_TOKPOST | tracing off | 0/1 (any set value enables; getenv()!=null) | Logs when a submitted WorkerPool task's RUN function matches the Token's terminal-transition RUN (the Token-RUN post detector), and records submit call-site return addresses in a ring buffer, libminecraftpe-gated. | diag |
| MACAPK_TRACE_VIEWPORT | tracing off | 0/1 (any value except "0"/empty enables) | Logs glViewport/glScissor calls (and their bound FBO) only on change, for a native-EGL (Stage-6) guest whose calls bypass the ARM32 shim's own viewport trace. | diag |
| MACAPK_X18_DIAG | tracing off | 0/1 (any set value enables; getenv()!=null) | On a fault whose address has the x18==0 small-offset signature, logs the FFT buffer base register, its arena/reserve membership, and page residency before recovery clobbers registers. | diag |
| MACAPK_X18_PLAN_DUMP | disabled | Value: output directory path | Writes base-independent evidence (the exact guest code and state) for an x18-handling differential comparison. | diag |
| MACAPK_XAC_TRACE | tracing off | 0/1 (any value except "0"/empty enables) | Logs each XAsyncComplete-captured GS completion queue, its impl, and thread id. | diag |
| MACAPK_XSUBMIT_TRACE | tracing off | 0/1 (any set value enables; getenv()!=null) | Installs the XAL submit-choke tracepoint in log-only mode (as opposed to MACAPK_XSUBMIT_PUMP's active-fix mode), libminecraftpe-gated. | diag |
